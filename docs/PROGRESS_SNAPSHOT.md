# FlowDesk Progress Snapshot

## Purpose

This document tracks implementation progress against the full FlowDesk roadmap. Update it whenever work changes implementation status, release gates, user-facing readiness, or known blockers.

This snapshot is not a substitute for tests, conformance evidence, or release approval. It is a planning artifact that keeps the roadmap state visible between sessions.

## Update Rule

Every non-trivial FlowDesk work session must update this file before concluding if any of the following changed:

1. Code, tests, docs, packaging, installer behavior, conformance evidence, or release gates.
2. A phase’s estimated progress, blocker list, or readiness interpretation.
3. The distinction between prototype, local adapter, production registration, and user-ready behavior.
4. Any critical review finding that changes plan priority or scope.

If no progress fields changed, the final response should explicitly say the progress snapshot was checked and did not need an update.

## Snapshot: 2026-05-21

This snapshot tracks implementation against the full roadmap, not only passing tests. Percentages are approximate readiness estimates for each phase’s exit criteria.

| Area | Progress | Evidence | Remaining gap |
|---|---:|---|---|
| Phase 0 Bootstrap workspace | 100% | Workspace, packages, build/test scripts, docs, and no-OMO/no-production-dispatch scaffolding exist. | None for scaffold. |
| Phase 1 Core contracts | 90% | Release 1 contracts, validators, schema artifacts, fixtures, Guard/fake-runtime/status/retry/audit/state tests, production-eligible minimum tool registry metadata, durable workflow-state reload/fail-closed recovery tests, and inert later-release scaffold contracts for top-tier multi-perspective reviewer bindings, lane plans, and binding inventories exist. | Persisted lane/delegation contracts need product hardening beyond the current Release 1 state reload path. |
| Phase 2 Policy, usage, audit | 74% | Config/policy/effective-policy, non-dispatch permission, provider health/usage fail-closed helpers, an internal provider-native usage collector for Claude OAuth, Codex/OpenAI live usage, and Gemini Code Assist quota, audit/debug write-intent tests, validated durable `.flowdesk` state materialization, durable workflow-state reload, durable session-evidence apply/reload, malformed persisted-state fail-closed handling, and durable redacted bootstrap artifact materialization with batch-level prevalidation exist. | Real config loading, credentialed/live collector integration, provider health collector expansion, and debug bundle implementation remain partial. |
| Phase 3 OpenCode plugin command path | 95% | Plugin package, package dry-run smoke evidence, public npm publication of `@flowdesk/core@0.1.0` and `@flowdesk/opencode-plugin@0.1.0`/`0.1.1`, isolated public-registry install/module-load smoke for both versions, active-profile registry dependency smoke for the prior `0.1.0` state, active-profile migration smoke promoting `@flowdesk/opencode-plugin` to `0.1.1` with nested `@opencode-ai/plugin@1.15.6` and `@opencode-ai/sdk@1.15.6`, FDS-1 schema conversion probe, command-backed handler evaluators, Release 1 non-dispatch production registration metadata, production-ready command-backed readiness checks, default-on safe local non-dispatch command tools, explicit diagnostic-only opt-out, opt-in durable `.flowdesk` state materialization and reload wired through server options, fail-closed durable write/reload handling, tool-boundary injected local permissions, portable `/flowdesk-*` command file materialization, `flowdesk-install-release1` package bin wiring, typed-bound Release 1 bootstrap installer library with derived profile-root binding, installer-only durable pending/consumed confirmation ledger, one-shot confirmation consumption, symlink-hardened ledger path checks, command-file rollback on durable artifact or ledger failure, `flowdesk_chat_intake`, default-on text-only `chat.message` FlowDesk card steering, confirmation-before-run steering guard, pending-approval workflow state writes, typed-approval chat-run acceptance, and `/flowdesk-doctor` Release 1 readiness diagnostics exist. Tests pass. | Real dispatch/provider/runtime/lane/fallback/hard-chat authority remains intentionally later-gated. |
| Phase 4 OpenCode conformance | 58% | OpenCode 1.14.40 PoC, local OpenCode 1.14.50 / `@opencode-ai/plugin@1.3.12` steering inspection, OpenCode 1.15.x dependency compatibility check with `@opencode-ai/plugin@1.15.6`, isolated OpenCode 1.15 Release 1 package smoke, public-registry `@flowdesk/opencode-plugin@0.1.1` install smoke, active OpenCode profile migration to `0.1.1` with nested `@opencode-ai/plugin@1.15.6` and `@opencode-ai/sdk@1.15.6`, FDS-1 runtime-closed fixture/probe evidence under the updated package line, Release 1 packaging/bootstrap smoke evidence, Release 2 live OpenCode 1.14.50 provider/model/audit/gate/echo/telemetry smoke evidence, internal collector unit evidence, redacted live credentialed provider-native usage collector smoke for Claude, OpenAI/Codex, and Gemini Code Assist, isolated current-tarball command/chat smoke evidence, and OpenCode SDK/plugin/auth-plugin source-surface inspection exist. Anthropic pure vs non-pure headless behavior is classified, pinned usage-authority evidence has a stricter core contract, SDK prompt/promptAsync subtask surfaces are typed, plugin hard chat no-reply/cancel authority remains unproven, and auth-plugin introspection must be sanitizer-based. | Durable production conformance command/persistence for usage evidence, command alias proof, blocking/no-reply proof, actual runtime lane observability, registered highest-tier reviewer lane proof with same-model multi-agent perspective support, arbitrary agent/model override proof, external auth plugin policy/doctor gating, and broader pinned-version runtime conformance evidence remain incomplete for later gates. |
| Phase 5 Managed dispatch beta | 53% | Core Release 2 fail-closed managed-dispatch beta gate evaluator, evidence contracts, validation helpers, targeted core tests, durable session evidence apply/reload, doctor-visible opt-in production enablement evaluation, configured-verification result artifact validation and doctor-visible wiring, durable sanitized-auth capture result validation and doctor-visible raw-plugin-object sanitization refs, sanitizer-backed token-free external-auth/provider policy artifact validation and doctor-visible wiring, validated production approval decision diagnostics and doctor-visible approval state refs, pinned usage-authority evidence validation, all-model auth/usage-missing exclusion diagnostics, internal provider-native collector logic ported from DEX Conductor/OpenUsage patterns for Claude OAuth usage, Codex/OpenAI live usage, and Gemini Code Assist quota, redacted live credentialed collector smoke proving usage_acquired/quota/reset evidence for tested Claude/OpenAI/Gemini paths, a separate opt-in OpenCode SDK client adapter boundary with fake-client tests, explicit opt-in server tool wiring for guarded actual SDK calls, live OpenAI SDK/provider smoke evidence, direct Anthropic Sonnet/Opus diagnostic success, non-pure Anthropic SDK success, OpenCode model metadata inspection, FlowDesk audit/gate fail-closed smoke evidence, live-call audit ordering smoke, schema-valid runtime echo/telemetry smoke, isolated current-tarball production enablement evaluation smoke, public registry install proof, source-surface proof for SDK prompt subtask/session metadata plus auth-plugin sanitizer requirements, and refreshed npm auth/publish evidence now exist. The default Release 1 server still does not enable real dispatch. | Production approval issuance/source authority and actual runtime lane conformance proof remain later-gated or blocked. |
| Phase 6 Managed chat and recovery | 50% | Core chat routing outcomes, default-on steering hook, visible text-only FlowDesk card suggestions with confirmation codes, in-memory duplicate suppression for repeated non-confirmation steering cards before command-backed state mutation, execution-like request hold-for-confirmation behavior, pending-approval workflow state, typed confirmation consumption, TTL/scoped/one-shot/cancellable pending confirmation gating in the local adapter, stronger approval classifier, boundary-injected local permissions, and schema-valid chat-routed retry/abort/resume/usage/export-debug diagnostics exist. | Intent detector split, broader abnormal-use recovery UX, durable suggestion preferences, todo continuation supervisor implementation, safe post-turn/pre-turn continuation conformance, and blocking conformance remain missing. |
| Phase 7 Operational intelligence | 0% | Schemas/concepts only. Top-tier multi-perspective reviewer lanes are now a normative planned Release 2.5 or Release 3 entry capability, not implemented behavior; the plan now requires same-day exact provider-qualified model availability cache use instead of abstract model labels. | Advisory evaluation, proposal fan-out, daily exact-model availability cache implementation, registered top-model internal subtask review, same-model multi-agent perspective review, score ledgers, and reference packs are later gates. |
| Phase 8 Federated score registry | 0% | Documentation only. | Explicit opt-in registry design and implementation are later gates. |

## Overall Assessment

Release 1 foundations are substantially implemented, with default safe local command-backed chat usable without opt-in, Release 1 minimum tools promoted to production-eligible non-dispatch command-backed registration, public npm publication complete for `@flowdesk/core@0.1.0` and `@flowdesk/opencode-plugin@0.1.0`, isolated public-registry install/module-load smoke passing, active OpenCode profile registry dependency smoke passing, and user-facing bootstrap docs updated to the public install path. A 2026-05-20 active-environment verification confirmed that the repo build output loads against local OpenCode 1.14.50/plugin dependencies, exposes the Release 1 command-backed tools, and direct doctor/usage/plan/fail-closed-run/approved-fake-runtime-run/status paths work with disabled runtime authority flags. A follow-up patch resolved the chat-routed `/flowdesk-doctor` schema mismatch by making `persist_report` a declared optional boolean in the FDS-1 doctor request schema artifact, with direct handler and chat-routed regression coverage; direct and chat-routed doctor/status/usage/plan-run smoke now pass with runtime authority flags disabled. The repository is published to GitHub at `https://github.com/astasdf1/flowdesk`, the active OpenCode profile now resolves FlowDesk dependencies from the public npm registry while retaining the `@flowdesk/opencode-plugin` plugin entry, and registry publication is no longer the public-distribution blocker for version `0.1.0`. Release 2 now has a core managed-dispatch beta eligibility gate, a separate opt-in adapter boundary that can call the injected OpenCode SDK client only after gate eligibility, explicit opt-in server wiring for guarded actual SDK calls, live OpenAI provider smoke evidence through OpenCode 1.14.50, direct Anthropic Sonnet/Opus diagnostic reachability, non-pure Anthropic SDK success, model catalog metadata inspection, fail-closed audit/gate smoke evidence, live-call audit ordering evidence, schema-valid runtime echo/telemetry evidence, pinned usage-authority evidence validation, all-model auth/usage-missing exclusion diagnostics, an internal provider-native usage collector with unit evidence for Claude OAuth, Codex/OpenAI, and Gemini Code Assist, and redacted live credentialed collector smoke for tested Claude/OpenAI/Gemini usage/quota/reset paths. Release 2/2.5 now also has core production enablement evaluation and approval-decision contracts that combine reloadable durable session evidence, pre-dispatch audit refs, configured verification refs, external auth/provider policy refs, lane conformance refs, explicit approval decisions, blocker labels, and non-blocking uncertainty labels while preserving `dispatch_authority_enabled: false`. Default server behavior remains Release 1 non-dispatch. Remaining production work is wiring these contracts into a production entrypoint, collecting real OpenCode lane observations, and deciding how much incomplete runtime proof to tolerate per opt-in policy. Registered highest-tier reviewer fan-out plus arbitrary agent/model override routing now has a concrete reviewer lane conformance observation artifact for same-model multi-perspective separation, but no default runtime lane launch is enabled.

The full multi-release roadmap is roughly one quarter complete because Releases 2-4 are intentionally unstarted.

## Release 2/2.5 Implementation Confidence and Uncertainty Policy

Release 2 and Release 2.5 work should proceed incrementally even when some OpenCode runtime conformance details are not yet fully proven. Lack of complete proof is not, by itself, a blocker when the feature can run safely behind explicit opt-in gates, fail-closed checks, durable evidence capture, and clear doctor/status reporting. Uncertain behavior must be documented as an evidence limitation and kept out of default Release 1 authority claims.

### Expected Implementable Areas

1. Durable production-path evidence persistence for usage authority, runtime echo, telemetry correlation, configured verification results, approval decisions, and pre-dispatch audit records is implementable using the existing `session-evidence.ts`, state-store, audit, and validator patterns.
2. A fail-closed production approval state machine is implementable as a core contract before dispatch is enabled; it should default to deny and only later permit conditional dispatch when required evidence is present.
3. Doctor-visible production enablement states are implementable: `disabled`, `blocked`, `configured`, `approved`, and `dispatch-capable` should be represented as diagnostic state, not as implicit dispatch authorization.
4. External auth/provider policy and doctor gating are implementable with conservative provider/model/account/auth-scope binding checks and explicit non-dispatchable diagnostics when evidence is missing or stale.
5. Configured verification integration is implementable by requiring production-path verification config to be read, validated, persisted, and treated as blocking only for the guarded dispatch path.
6. Same-model multi-perspective reviewer separation is implementable at the FlowDesk artifact layer by storing distinct perspective bindings, prompt hashes, lane refs, output refs, and verdict attribution even when the concrete provider-qualified model id is the same.

### Runtime-Conformance Areas To Prove While Building

1. Actual OpenCode `subtask: true` lane lifecycle observability remains uncertain until exercised against the active OpenCode SDK/runtime and recorded as evidence.
2. Injected SDK reviewer lane launch is expected to be feasible, but the exact available runtime echo fields, parent/subtask identifiers, timeout semantics, and failure metadata depend on OpenCode API behavior.
3. Provider/model/account/auth binding can be enforced by FlowDesk policy, but how much of that binding can be independently echoed by OpenCode or external auth plugins remains an evidence question.
4. Cancellation, hard chat blocking, and no-reply authority must remain explicitly unclaimed unless a first-class OpenCode boundary is proven. If they are not proven, FlowDesk may still run the feature with softer status/diagnostic behavior and document the limitation.
5. Runtime telemetry may require FlowDesk-created correlation artifacts if OpenCode does not expose all desired lane observability fields directly.

### Non-Blocking Rule

If a Release 2/2.5 feature works in practice but a conformance proof is incomplete, do not block implementation solely for that reason. Instead, keep the feature opt-in, preserve default Release 1 non-dispatch behavior, store the best available durable evidence, mark the uncertain part in doctor/status/debug output, add negative tests for the safety boundary, and record the exact missing proof in this snapshot or a linked conformance note.

## Active Critical Plan Updates

1. Release 1 must not implement broad OMO-style invisible prefix injection.
2. Chat routing must use conservative, transparent intent handling: leave general chat alone, suggest visibly, route only explicit/high-confidence FlowDesk requests, and block later-gate unsafe FlowDesk routes.
3. Execution-like natural language must stop at confirmation or plan readiness before any guarded dry-run or fake-runtime state change.
4. User-facing copy should avoid internal terms such as `manage`, `non-dispatch`, `adapter`, `pre-spike`, and `fake-runtime` unless a diagnostic/debug surface explicitly needs them.
5. Durable local adapter state must fail closed or surface a blocking error when `.flowdesk` writes fail; command success must not mask failed durable materialization or leave mutated in-memory state ahead of durable state.
6. Pending chat-run confirmations must require exact scoped binding for captured session/source fields and expose a user-completable confirmation path in visible chat steering.
7. Critical review on 2026-05-19 found and the follow-up patch resolved two Release 1 safety blockers: bootstrap durable artifact materialization now validates the full batch before any write, and duplicate chat steering suppression now runs before command-backed state mutation for suppressed non-confirmation cards.
8. Critical review on 2026-05-19 found and the follow-up patch addressed bootstrap installer blockers: installer confirmation now binds target profile, profile root ref, install plan, rollback plan, actor, expiry, and exact typed phrase; command files are rolled back when durable bootstrap artifact writes fail.
9. Critical review on 2026-05-19 found and follow-up patches partially addressed the bootstrap installer profile-binding blocker: `profileRootRef` is now derived from the actual selected `profileRootDir`, the exact typed phrase binds target profile, profile-root ref, confirmation ref, and install plan, direct replay to another profile root is rejected before writes, and successful confirmation refs are consumed once per installer process.
10. The Release 1 bootstrap installer now persists installer confirmations in a lightweight durable ledger under `.flowdesk/bootstrap/confirmations/<confirmationRef>.json`, claims `pending` before bootstrap artifact writes, promotes successful installs to `consumed`, rejects pending/consumed or binding-mismatched ledgers before writes, stores only a typed phrase hash, and fails closed with command rollback if ledger persistence fails. Remaining installer work is CLI/profile wiring and user-facing bootstrap flow integration.
11. Critical review on 2026-05-19 found and follow-up patch resolved durable ledger hardening blockers: installer confirmation now creates a durable `pending` claim before bootstrap artifact writes so incomplete attempts block replay after restart, promotes the ledger to `consumed` only after durable bootstrap artifacts succeed, rejects matching `pending` and `consumed` ledgers before writes, and rejects symlinked or root-escaping confirmation ledger directories.
12. The Release 1 installer now has an explicit `flowdesk-install-release1` CLI/package bin that previews the exact typed approval phrase without writing files, installs only after exact approval, writes portable command files and redacted bootstrap artifacts through the hardened installer library, and reports safe next actions while keeping production registration and provider/runtime dispatch disabled.
13. The OpenCode server plugin now defaults to safe local command-backed tools and visible `chat.message` steering without provider calls, runtime execution, real dispatch, fallback authority, lane launch, or hard chat cancellation/no-reply authority. Explicit `localNonDispatchAdapter: false` plus `naturalLanguageRouting: false` preserves diagnostic-only mode, and `fds1SchemaConversionProbe: true` remains isolated unless local/chat modes are explicitly requested.
14. Release 1 production registration has been promoted only to non-dispatch command-backed registration: schema registry and command manifest minimum tool metadata are production-eligible, the plugin scaffold reports `release1-non-dispatch-command-backed`, readiness checks pass with zero blocked checks, pre-spike sandbox probe registries remain empty/non-authorizing, and tests verify real OpenCode dispatch, provider calls, runtime execution, actual lane launch, fallback authority, and hard chat cancellation/no-reply authority stay disabled.
15. Release 1 packaging/bootstrap smoke evidence was recorded on 2026-05-19: `npm pack --dry-run --json` passes for `@flowdesk/core` and `@flowdesk/opencode-plugin`, packed files exclude test outputs and `.tsbuildinfo`, the plugin package exposes `flowdesk-install-release1`, and user-facing README/quickstart/manual docs now describe preview/no-write and exact-approval bootstrap flow.
16. The Release 2 managed-dispatch beta core gate layer now exists only as a fail-closed eligibility evaluator in `@flowdesk/core`: it requires managed-dispatch beta policy mode, fresh dispatchable usage, fresh dispatchable provider health, GuardApprovedDispatch, trusted provider/model binding, trusted runtime echo/conformance metadata, sufficient telemetry correlation, durable pre-dispatch audit, configured verification, no fallback or hard-chat authority, and ambiguity quarantine. Release 1 real dispatch remains blocked, and no plugin adapter dispatch is enabled.
17. The Release 2 managed-dispatch beta plugin adapter now exists as a separate opt-in module only: it accepts the core evidence bundle plus a bounded dispatch request, evaluates `evaluateManagedDispatchBetaGuardBoundaryV1` before any SDK call, requires the request model to match Guard approval, uses only injected `client.session.prompt`/`promptAsync`, omits `noReply`/fallback/tool authority, and is covered by fake-client tests. It is not wired into default server tools, so Release 1 remains non-dispatch by default.
18. Release 2 live environment evidence was collected on 2026-05-19: OpenCode 1.14.50 can start a headless server, create a session, and dispatch through `client.session.prompt` to `openai/gpt-5.4-mini-fast` with provider/model identity and expected model text returned. Anthropic OAuth reaches the provider API; direct CLI diagnostics succeed for tested Sonnet/Opus ids, tested Haiku ids return provider model-not-found, pure headless SDK mode fails because it skips the local external Anthropic auth/provider plugin, and non-pure headless SDK mode succeeds for Anthropic Sonnet.
19. Release 2 provider metadata and usage-source evidence was recorded on 2026-05-19: OpenCode verbose model metadata exposes catalog fields but not account quota/reset authority; OpenAI and Anthropic have documented admin/rate-limit/header surfaces, Google Cloud has Cloud Quotas/Monitoring surfaces for supported project/service quota metrics, and DEX Conductor/OpenUsage usage signals are acknowledged as viable only after a pinned FlowDesk conformance binding proves exact source authority. This local shell has no provider/admin keys and OpenCode SDK responses do not forward provider rate-limit headers.
20. Release 2 audit, echo, telemetry, auth-exclusion, and opt-in dispatch wiring evidence was recorded on 2026-05-19: a FlowDesk-bound audit/gate smoke writes redacted audit evidence while preserving disabled authority flags and blocking managed-dispatch beta eligibility when usage evidence is missing; a live-call ordering smoke proves durable audit write before live OpenAI SDK prompt; SDK response/session correlation validates the current runtime echo and telemetry schemas with redacted source refs; every concrete provider family now maps absent auth or real usage/quota/reset evidence to non-dispatchable model exclusion diagnostics; pinned usage-authority evidence is required by the managed-dispatch gate with redaction-safe opaque scope refs, concrete non-alias provider-qualified model id, auth profile ref, auth evidence ref, credential scope ref, account boundary ref, quota evidence ref, actual usage acquisition, reset time, and reset bucket; and the default server remains Release 1 non-dispatch while an explicit opt-in beta tool can call only the guarded injected SDK adapter. Production Release 2 dispatch remains blocked until live pinned usage conformance, provider collectors, durable production-path echo/telemetry persistence, configured verification production integration, external auth plugin policy/doctor integration, and production approval exist.
21. Release 2 internal provider-native usage collection was added on 2026-05-19 by porting the working DEX Conductor/OpenUsage collector patterns into `@flowdesk/core`: Claude OAuth usage, Codex/OpenAI live usage, and Gemini Code Assist quota produce redacted Usage Snapshot, Provider Health Snapshot, and managed-dispatch usage-authority evidence only when auth-bound usage/quota/reset data is actually acquired. Disabled acquisition, missing auth, missing fetch, refused provider responses, stale/insufficient/unknown buckets, and unavailable scope refs fail closed as non-dispatchable without emitting usage authority evidence. This is unit-tested local collector logic, not production live conformance; live credentialed provider/model/account/auth binding remains required before Release 2 dispatch can be approved.
22. Release 2 live credentialed provider-native usage smoke was recorded on 2026-05-19: approved local auth surfaces allowed FlowDesk's internal collector to acquire and validate `provider_native` usage authority evidence for tested Claude, OpenAI/Codex, and Gemini Code Assist paths. The smoke exposed and fixed two collector issues: Claude keychain discovery now matches the DEX-compatible `Claude Code-credentials` service naming, and Claude bucket selection now chooses the first positive-remaining bucket with parseable reset evidence when the five-hour bucket lacks reset evidence. The final redacted smoke passed for `claude-weekly`, `openai-gpt-5h`, and `gemini-pro-daily` reset buckets. Production enablement still requires durable production-path evidence persistence, configured verification, doctor/profile policy, and release approval.
23. Planning update on 2026-05-19: FlowDesk critical review must fan out to every registered highest-tier available reviewer/model lane as extensions of the canonical `reviewer` profile in a planned Release 2.5 or Release 3 entry gate. The model set comes from the active registry and Policy Pack rather than a hard-coded Claude/GPT/Gemini floor. If only one highest-tier model is registered and available, FlowDesk must still perform multi-perspective review by assigning multiple reviewer agents or perspective bindings to that same concrete model. The plan requires explicit registered/available/highest-tier predicates, fan-out budget/concurrency/quota/timeout/retry caps, and a redacted reviewer binding inventory snapshot so the run cannot silently shrink or substitute lower-tier models under pressure. Production orchestration for those lanes and arbitrary agent/model override lanes must use internal OpenCode `subtask: true` lanes or injected SDK/client paths with explicit `agent` and concrete provider-qualified `model` binding, never nested `opencode run`. The capability is not implemented, verified, installed, or release-approved.
24. Inert core scaffolding for top-tier multi-perspective review was added on 2026-05-19 in `@flowdesk/core`: `flowdesk.top_tier_reviewer_binding.v1`, `flowdesk.top_tier_reviewer_lane_plan.v1`, and `flowdesk.top_tier_review_binding_inventory.v1` schemas are registered as later-release artifacts with fail-closed validators that require canonical `reviewer` profile, concrete non-alias provider-qualified model ids, `highest_tier_eligible: true`, `dispatch_authority_enabled: false`, perspective and inclusion-state enums, bounded inventory budgets, and rejection of authority-key smuggling. Tests cover single-model multi-perspective fan-out, multi-model fan-out, and fail-closed shapes. No runtime dispatch, lane launch, or doctor wiring is enabled.
25. Reviewer output schema `flowdesk.top_tier_review_verdict.v1` was added on 2026-05-19 in `@flowdesk/core` as a later-release artifact with fail-closed validators: verdict_label ∈ {pass, changes_required, blocked, inconclusive} is review output only, root-level `dispatch_authority_enabled: false`, `source` is a bounded lowercase registry binding label rather than a fixed Claude/GPT/Gemini enum, perspective is registry-shared with reviewer lane plans, bounded findings ≤ 20 with severity and category enums, bounded required_fixes ≤ 20, redaction guard rejects raw payload/credential/path markers, and authority keys (`approve_dispatch`, `trusted`, `guard_approved_dispatch`, etc.) are blocked. Tests cover same-source multi-perspective verdicts, future top-tier source labels, authority-key rejection, malformed source/severity/uncertainty/verdict labels, and unknown property rejection. No runtime emission path is enabled.
26. Planned mode-field contract `FlowDeskPlannedModeFieldV1` was added on 2026-05-19 in `@flowdesk/core` with typed `label`, `state`, and `dispatch_authority_enabled: false` fields, plus `flowDeskPlannedModeFieldToString` serializer and `createFlowDeskPlannedTopTierMultiPerspectiveReviewModeFieldV1` factory. Validators reject unknown labels, forbidden authority-implying states (`enabled`, `active`, `dispatch_ready`, `approved`, `authorized`), and dispatch authority claims. The serialized canonical entry `top_tier_multi_perspective_review_mode=planned` now appears in the `/flowdesk-doctor` `opencode_plugin_compatibility` section refs so users can see the planned reviewer readiness without enabling any runtime authority. Doctor handler and core validator tests cover the new contract.
27. Opt-in fake-runtime reviewer lane probe was added on 2026-05-19 in `@flowdesk/core` as a pure-function evaluator `evaluateFlowDeskTopTierReviewerLaneProbeV1` plus typed request/result schemas `flowdesk.top_tier_reviewer_lane_probe.request.v1` and `flowdesk.top_tier_reviewer_lane_probe.result.v1`. The probe supports `subtask_true_command_lane` and `injected_sdk_client` channels, requires `agent_id: "reviewer"`, concrete non-alias provider-qualified model id, perspective from the reviewer perspective enum, six evidence refs (auth/usage/quota/provider_health/runtime_echo/telemetry), `fake_runtime: true`, and `dispatch_authority_enabled: false`. Outcomes are `probe_pass`, `probe_fail_closed` (with failure_label and any one of the evidence slots missing), or `probe_invalid`. Probe results always carry `provider_call_made: false` and `lane_launch_made: false`. A guard test ensures the compiled probe build does not reference `@opencode-ai/sdk`, child process surfaces, or `opencode run`. The probe is not wired into any default server tool, plugin handler, or doctor section; it is exercised only through direct calls in tests/probes.
28. Durable session evidence persistence scaffolding was added on 2026-05-19 in `@flowdesk/core` (`session-evidence.ts`): `FLOWDESK_SESSION_EVIDENCE_CLASSES` (`usage_authority`, `runtime_echo`, `telemetry_correlation`) and `.flowdesk/sessions/<workflowId>/evidence/<class>/<evidenceId>.json` path builders, `prepareFlowDeskSessionEvidenceWriteIntentV1` which validates schema_version per class, workflow alignment, and runs the redaction guard before producing a temp-then-rename write intent with `realOpenCodeDispatch/actualLaneLaunch/providerCall/runtimeExecution: false`, and `reloadFlowDeskSessionEvidenceV1` which fails closed on root-escape, symlinked evidence roots, malformed JSON, schema_version mismatch, workflow_id mismatch, and path-shaped evidence ids. Tests cover empty directory, multi-class success, malformed JSON, schema mismatch, workflow mismatch, path-shaped evidence ids, and symlinked directories. No actual filesystem writes occur in production wiring yet.
29. Active-environment verification on 2026-05-20 found FlowDesk is not installed in the real active OpenCode profile yet: `~/.config/opencode/opencode.json` has no FlowDesk plugin entry, `@flowdesk/opencode-plugin` is absent from active profile dependencies, and `flowdesk-install-release1` is not on PATH. The repo build output still loads successfully as an OpenCode plugin module against local OpenCode 1.14.50 and `@opencode-ai/plugin@1.3.12`, exposes 11 tools including the 9 Release 1 command-backed tools plus chat intake and pre-spike doctor, and direct command-backed doctor/usage/plan/run/status verification works with `realOpenCodeDispatch`, `actualLaneLaunch`, `providerCall`, and `runtimeExecution` all false. The initial verification found a chat-routed `/flowdesk-doctor` mismatch where `routedToolRequest` includes `persist_report` but the FDS-1 schema artifact rejected it as an unknown property.
30. The chat-routed `/flowdesk-doctor` mismatch was resolved on 2026-05-20 by declaring `persist_report` as an optional boolean field in `flowdesk.doctor.request.v1` schema artifacts while preserving the existing semantic validator and non-dispatch behavior. Regression coverage now proves direct doctor requests with `persist_report` and chat-routed `/flowdesk-doctor` succeed. Targeted plugin tests, full `npm run typecheck`, full `npm test` (237/237), LSP diagnostics on touched files, direct command-backed smoke, chat-routed doctor/status/usage smoke, and active-profile installer preview all pass. Active install remains pending exact typed confirmation and post-install smoke.
31. GitHub upload and publish-readiness work on 2026-05-20 created `https://github.com/astasdf1/flowdesk`, removed hard-coded Gemini OAuth client id/secret literals from the provider usage collector, rewrote git history with `git-filter-repo` so GitHub push protection no longer detects those literals, and pushed sanitized history. Gemini OAuth client id/secret are now supplied through collector acquisition config or `FLOWDESK_GEMINI_OAUTH_CLIENT_ID` / `FLOWDESK_GEMINI_OAUTH_CLIENT_SECRET`, and tests cover the configured path. At that time, packages were public release candidates at `0.1.0` with repository/license/publishConfig metadata and `npm publish --dry-run` passed for both workspaces, but actual npm publish still awaited npm auth and `@flowdesk` scope publish authority. That historical blocker was resolved by the 2026-05-21 public publication in item 38.
32. Active OpenCode profile installation was completed through local tarballs on 2026-05-20 before registry publication was available: `@flowdesk/core@0.1.0` and `@flowdesk/opencode-plugin@0.1.0` were installed in `~/.config/opencode`, and `opencode.json` included `@flowdesk/opencode-plugin`. Active-profile module smoke loaded the plugin, exposed the Release 1 local command-backed and chat-intake tools, reported Release 1 production readiness true for non-dispatch command-backed behavior, and verified chat-routed `/flowdesk-doctor` and status with `providerCall`, `runtimeExecution`, and `actualLaneLaunch` false. This historical active-profile smoke used local tarballs; later public registry install proof is recorded in item 38.
33. Active OpenCode profile installation was made durable on 2026-05-20 before npm registry publication by packing `@flowdesk/core@0.1.0` and `@flowdesk/opencode-plugin@0.1.0` into `~/.config/opencode/flowdesk-packages/` and changing active profile dependencies to `file:./flowdesk-packages/...` instead of temporary `/var/folders/...` tarballs. `npm install` in `~/.config/opencode` succeeded with zero vulnerabilities, `npm list @flowdesk/core @flowdesk/opencode-plugin --depth=0` showed both packages at `0.1.0`, fixture-backed direct doctor/status smoke succeeded, chat-routed `/flowdesk-doctor` and Korean status smoke succeeded, and all tested paths kept `providerCall`, `runtimeExecution`, and `actualLaneLaunch` false. Full repository verification passed with `npm run typecheck` and `npm test` (237/237). The public-distribution blocker noted at that time was resolved by the 2026-05-21 public publication in item 38.
34. Release 2/2.5 production enablement foundations were implemented on 2026-05-20. `@flowdesk/core` now exposes `production-enablement.ts` with `flowdesk.production_approval_decision.v1` and `flowdesk.production_enablement_evaluation.v1` pure contracts. The evaluator reports `configured` when durable evidence and policy refs exist but approval is absent, `dispatch_capable` only after explicit approval and required evidence refs, records incomplete OpenCode conformance as non-blocking uncertainty when opted in, and fails closed for broken reloads, blocked evidence records, missing configured verification, missing external auth/provider policy, denied approval, approval/evidence drift, or malformed lane conformance refs. `reviewer-lane-conformance.ts` adds `flowdesk.top_tier_reviewer_lane_conformance_observation.v1` for actual or partial reviewer lane observations, including same-model multi-perspective separation via distinct lane ids, prompt-hash refs, output refs, runtime echo refs, telemetry refs, and parent/subtask refs while keeping dispatch and hard-chat authority false. `/flowdesk-doctor` diagnostic refs now expose production enablement as disabled/opt-in/evidence-tracked with fail-closed approval, configured verification, and external auth/provider policy gates. Verification passed with `npm run typecheck`, `npm test` (246/246), and clean LSP diagnostics on changed source/test files. Remaining uncertainties are actual OpenCode `subtask: true` lifecycle observability, exact injected-SDK runtime echo metadata, external auth plugin introspection depth, and hard chat cancel/no-reply authority.
35. Isolated current-tarball evidence was recorded on 2026-05-20 in `docs/conformance/2026-05-20-release2-isolated-current-tarball-smoke.md`: the current workspace build packed `@flowdesk/core@0.1.0` and `@flowdesk/opencode-plugin@0.1.0`, installed them into a throwaway profile with `@opencode-ai/plugin@1.3.12`, and loaded the plugin against OpenCode CLI 1.14.50 with 11 expected tools. Direct portable-command doctor/status, chat-routed `/flowdesk-doctor`, Korean status chat intake, and the `chat.message` visible card hook succeeded with provider calls, runtime execution, and actual lane launch all false. An invalid direct `input_mode: "command"` doctor request failed closed at schema validation. The packaged production enablement evaluator reported a complete synthetic evidence bundle as `configured` but not dispatch-ready because approval was missing and OpenCode lane conformance remains partial, while missing evidence produced `blocked` with fail-closed blocker labels.
36. Release 2 production-evidence wiring was added on 2026-05-20 and recorded in `docs/conformance/2026-05-20-release2-production-evidence-wiring.md`: `applyFlowDeskSessionEvidenceWriteIntentsV1` now durably applies validated session-evidence write intents with temp-then-rename writes, strict workflow/class/schema/path alignment, and disabled runtime authority flags; reload-after-write tests cover usage authority, runtime echo, and telemetry correlation evidence. The local OpenCode adapter can now opt into production enablement diagnostics with a durable root and explicit refs, causing `/flowdesk-doctor` to surface evaluated production state, blockers, uncertainties, doctor refs, and `production_dispatch_authority_enabled=false` without changing default Release 1 non-dispatch behavior. Full verification passed with `npm run typecheck` and `npm test` (251/251). At that time, npm publish was still blocked by token/2FA requirements; the blocker and fresh registry-install smoke gap were resolved by the 2026-05-21 public publication in item 38.
37. OpenCode source-surface inspection was recorded on 2026-05-21 in `docs/conformance/2026-05-21-release2-opencode-source-surface-inspection.md`: SDK typings prove `session.create(parentID)`, `session.children`, session agent/model metadata, prompt/promptAsync `SubtaskPartInput`, SDK `noReply`, and SDK `session.abort` API surfaces, while `session.command` subtask parts, literal runtime `subtask: true`, actual lane lifecycle behavior, trusted runtime echo, and telemetry pass-through remain unproven. Plugin typings still do not expose first-class hard chat no-reply/cancel return authority. Claude/Gemini auth plugin inspection proves useful metadata surfaces only after strict allowlist sanitization because account/auth objects carry access tokens, refresh tokens, OAuth codes/state/verifiers, callback URLs, API keys, headers, and debug bodies. No provider calls, runtime execution, lane launches, or production authority changes were made.
38. Public npm registry publication completed on 2026-05-21 after the `flowdesk` npm organization/scope was created and a refreshed token at `/Users/bagel_macpro_055/Documents/work/projects/npm_tk.txt` gained `@flowdesk` org access. `npm org ls flowdesk --json` confirmed `astasdf` as `owner`, `npm publish --workspace @flowdesk/core --access public` published `@flowdesk/core@0.1.0`, and `npm publish --workspace @flowdesk/opencode-plugin --access public` published `@flowdesk/opencode-plugin@0.1.0`. Fresh-cache registry checks returned version `0.1.0` for both packages, dist-tags show `latest: 0.1.0`, access status is public, and a fresh temp-profile `npm install @flowdesk/core@0.1.0 @flowdesk/opencode-plugin@0.1.0` succeeded. The earlier blocker was stale/insufficient token scope plus npm cache propagation; public distribution is now available for version `0.1.0`.
39. Release 1 local adapter lane observability fixture parity was restored on 2026-05-21 after the core fake-runtime lane contract started requiring `laneObservabilityRef` whenever optional lane output is emitted. `packages/opencode-plugin/src/local-adapter.ts` now supplies a deterministic `laneObservabilityRef` for fake-runtime context, persists matching `observability_ref` and observability refs in local lane records, and keeps Release 1 non-dispatch behavior, disabled fallback authority, and unclaimed hard chat cancel/no-reply authority unchanged. Verification passed with clean LSP diagnostics on touched plugin files, `npm test -- --test-name-pattern "lane observability|fake-runtime|injected sdk lane observation|status"` (256/256 passing), `npm run typecheck`, and full `npm test` (256/256 passing).
40. `README.md` was rewritten on 2026-05-21 to present FlowDesk as a published OpenCode plugin and safety harness influenced by Sakana AI's Conductor framing, while clearly distinguishing the current Release 1 non-dispatch harness from later usage-based model selection and runtime lane orchestration goals. The README now includes npm install/config instructions, command list, safety boundary, roadmap checklist aligned to this snapshot, and a gated model-selection explanation that does not claim production dispatch authority.
41. `docs/QUICKSTART.md`, `docs/USER_MANUAL.md`, and `docs/OPENCODE_CONFORMANCE_PLAN.md` were updated on 2026-05-21 to replace stale private/local-only install wording with the public `npm install @flowdesk/core@0.1.0 @flowdesk/opencode-plugin@0.1.0` path while preserving Release 1 bootstrap approval, non-dispatch behavior, and later-gated runtime dispatch/lane authority.
42. Isolated public-registry install smoke was recorded on 2026-05-21 in `docs/conformance/2026-05-21-release1-public-registry-install-smoke.md`: a throwaway profile installed `@flowdesk/core@0.1.0`, `@flowdesk/opencode-plugin@0.1.0`, and `@opencode-ai/plugin@1.3.12` from npm, loaded the plugin module directly, exposed the 11 expected Release 1 tools plus `chat.message`, and verified direct status plus Korean chat-intake routing to `flowdesk_status` while keeping provider calls, runtime execution, actual lane launch, fallback authority, and hard cancel/no-reply authority disabled. This supersedes public-distribution uncertainty only; managed dispatch and runtime lane conformance remain later-gated.
43. Active OpenCode profile registry migration was recorded on 2026-05-21 in `docs/conformance/2026-05-21-release1-active-profile-registry-smoke.md`: `~/.config/opencode/package.json` now depends on `@flowdesk/core: "0.1.0"` and `@flowdesk/opencode-plugin: "0.1.0"`, the active lockfile resolves both packages to registry tarballs, `opencode.json` retains the `@flowdesk/opencode-plugin` plugin entry, and direct active-profile server-export smoke exposed the 11 expected Release 1 tools plus `chat.message` while direct status and Korean chat-intake routing preserved disabled provider calls, runtime execution, actual lane launch, fallback authority, and hard cancel/no-reply authority. This completes public-package active-profile migration only; managed dispatch, production model selection, and runtime lane conformance remain later-gated.
44. Release 2 configured-verification gate integration was added on 2026-05-21 and recorded in `docs/conformance/2026-05-21-release2-configured-verification-gate.md`: `@flowdesk/core` now exposes `flowdesk.configured_verification_result.v1` with fail-closed validation for workflow/ref alignment, bounded labels/evidence refs, redaction, and explicit false provider/runtime/lane/dispatch authority flags. `evaluateFlowDeskProductionEnablementV1` now requires a validated configured-verification result when a configured verification ref is present, blocks missing/failed/mismatched/malformed results, folds evidence refs only from valid matched artifacts into the non-authorizing production evaluation, and surfaces result/ref diagnostics through `/flowdesk-doctor` only after successful validation and explicit production-enable opt-in. Verification passed with targeted configured-verification tests, `npm run typecheck`, full `npm test` (262/262), and clean LSP diagnostics on touched TypeScript files. This closes only the configured-verification production-integration gap; real dispatch, provider/model selection, actual lane launch, sanitizer-backed external auth/provider policy, release approval, and hard chat cancel/no-reply authority remain later-gated.
45. Release 2 external-auth/provider policy gate integration was added on 2026-05-21 and recorded in `docs/conformance/2026-05-21-release2-external-auth-provider-policy-gate.md`: `@flowdesk/core` now exposes `flowdesk.external_auth_provider_policy_result.v1` for already-sanitized token-free metadata with fail-closed validation for workflow/ref alignment, concrete provider/model binding, auth/profile/scope/account refs, sanitizer refs, bounded labels/evidence refs, explicit false raw-auth/token persistence flags, and explicit false provider/runtime/lane/dispatch authority flags. `evaluateFlowDeskProductionEnablementV1` now requires a validated external-auth/provider policy result when external-auth and provider policy refs are present, blocks missing/failed/mismatched/malformed/token-shaped results, folds evidence refs only from valid matched artifacts into the non-authorizing production evaluation, and surfaces result/ref diagnostics through `/flowdesk-doctor` only after successful validation and explicit production-enable opt-in. Targeted verification passed with external-auth/provider policy, production enablement, and doctor/server wiring tests (269/269 after concrete model-alias rejection coverage). This closes only the token-free sanitizer-backed policy artifact and diagnostic-wiring slice; release approval, real dispatch, provider/model selection, actual lane launch, and hard chat cancel/no-reply authority remain later-gated.
46. Planning update on 2026-05-21: multi-model or top-tier multi-perspective review planning must use exact provider-qualified model ids from a redacted same-day availability cache, not abstract labels such as Claude Opus, GPT, or Gemini Pro. The cache is refreshed at most once per local day for the same active profile, registry, Policy Pack, OpenCode version, and account/auth boundary, and is refreshed early only when those inputs change. If no current cache proves exact available model ids, FlowDesk must block or ask for discovery rather than silently substituting labels, lower-tier models, hidden gateways, or stale ids. This is a planning requirement only; the cache and real reviewer lane launch remain unimplemented later gates.
47. Release 2 production approval state diagnostics were added on 2026-05-21 and recorded in `docs/conformance/2026-05-21-release2-production-approval-state-gate.md`: validated `flowdesk.production_approval_decision.v1` inputs can now surface non-authorizing `approval_decision` and `approval_ref` fields through production enablement and `/flowdesk-doctor`, denied approvals remain visible while blocked, required-ref drift remains blocked, invalid or forged approval decisions are not echoed, and the server production enablement option now passes `approvalDecision` into the core evaluator for validation. Targeted verification passed with production enablement, doctor diagnostics, and server option tests (271/271 in the targeted command run), with clean LSP diagnostics on touched TypeScript files. This closes only doctor-visible approval state-machine wiring; production approval issuance/source authority, real dispatch, actual lane launch, provider/model selection, and hard chat cancel/no-reply authority remain later-gated.
48. Release 2 sanitized-auth capture gate integration was added on 2026-05-21 and recorded in `docs/conformance/2026-05-21-release2-sanitized-auth-capture-gate.md`: `@flowdesk/core` now exposes `flowdesk.sanitized_auth_capture_result.v1` for durable token-free sanitized auth capture metadata with fail-closed validation for workflow/ref alignment, concrete provider/model binding, auth/profile/scope/account refs, durable capture and sanitizer refs, bounded labels/evidence refs, explicit false raw-auth/raw-plugin-object/token persistence flags, and explicit false provider/runtime/lane/dispatch authority flags. `evaluateFlowDeskProductionEnablementV1` now requires a validated sanitized-auth capture result when a sanitized capture ref is present, blocks missing/failed/mismatched/malformed/token-shaped/raw-plugin-shaped results, folds evidence refs only from valid matched artifacts into the non-authorizing production evaluation, and surfaces result/ref diagnostics through `/flowdesk-doctor` only after successful validation and explicit production-enable opt-in. Targeted verification passed with sanitized-auth capture, production enablement, doctor diagnostics, and server option tests (277/277 in the targeted command run). This closes only artifact validation and doctor-visible raw-plugin-object sanitization wiring; production approval issuance/source authority, real dispatch, actual lane launch, provider/model selection, and hard chat cancel/no-reply authority remain later-gated.
49. Planning update on 2026-05-21: todo continuation supervisor is now a planned managed-chat/recovery capability, not implemented behavior. It must supervise only FlowDesk-owned durable workflow task records and valid checkpoints, classify no-output/missing-verdict/aborted sub-agent lanes as incomplete or failed, preserve background-invocation versus continuation-session id separation, and use bounded retry plus visible `/flowdesk-status`, `/flowdesk-resume`, `/flowdesk-retry`, or `/flowdesk-abort` safe next actions. Hidden OMO-style system directive injection or permissionless auto-continuation is blocked until a pinned OpenCode post-turn or pre-turn control surface proves safe behavior; even then, continuation cannot approve dispatch, widen scope, bypass confirmation, or replace QA/security/review verdicts.
50. OpenCode 1.15 compatibility check on 2026-05-21 found the local CLI at `1.15.5` while npm latest for `opencode-ai`, `@opencode-ai/plugin`, and `@opencode-ai/sdk` was `1.15.6`. `packages/opencode-plugin` now pins `@opencode-ai/plugin@1.15.6` exactly, bringing the lockfile SDK dependency to `1.15.6`; `npm run typecheck`, full `npm test` (277/277), and direct built-plugin module load with 11 tools plus `chat.message` passed without FlowDesk source changes. This is dependency/source-compatibility evidence only. It does not promote runtime authority: isolated profile plugin load, FDS-1 schema conversion, chat hook blocking/no-reply behavior, SDK prompt/promptAsync lane observability, provider/model echo, telemetry, auth-provider metadata, real dispatch, actual lane launch, fallback, and hard cancel/no-reply remain separately gated until pinned OpenCode 1.15.x runtime conformance is recorded.
51. Release 1 OpenCode 1.15 isolated smoke was recorded on 2026-05-21 in `docs/conformance/2026-05-21-release1-opencode-1.15-isolated-smoke.md`: packed workspace `@flowdesk/core@0.1.0` and `@flowdesk/opencode-plugin@0.1.0` installed into a throwaway npm profile with `@opencode-ai/plugin@1.15.6` and `@opencode-ai/sdk@1.15.6`, direct module load returned plugin id `flowdesk`, 11 tools, and `chat.message`, direct pre-spike doctor/doctor/status/usage checks succeeded, Korean chat-intake routed to `flowdesk_status`, `chat.message` steering appended visible FlowDesk text without `noReply`/`cancel`/`stop`, and FDS-1 fixture/probe checks preserved runtime-closed unknown-property rejection. Provider calls, runtime execution, actual lane launch, fallback authority, and hard cancel/no-reply authority remained false.
52. Release 1 `@flowdesk/opencode-plugin@0.1.1` publish prep was recorded on 2026-05-21 in `docs/conformance/2026-05-21-release1-0.1.1-publish-prep.md`: the plugin package version was bumped to `0.1.1` solely to carry the exact `@opencode-ai/plugin@1.15.6` dependency pin, `npm pack --dry-run --json --workspace @flowdesk/opencode-plugin` produced `flowdesk-opencode-plugin-0.1.1.tgz` with 37 entries and no bundled dependencies, and an isolated install of the packed tarball loaded plugin id `flowdesk`, 11 tools, `chat.message`, `@opencode-ai/plugin@1.15.6`, and `@opencode-ai/sdk@1.15.6`. Direct `npm publish` and `NODE_AUTH_TOKEN` retries both returned `EOTP`; publication only completed after writing the local npm token into a temporary `.npmrc` and passing it through `--userconfig`.
53. Release 1 `@flowdesk/opencode-plugin@0.1.1` publish completion was recorded on 2026-05-21 in `docs/conformance/2026-05-21-release1-0.1.1-publish-completion.md`: the package is now public on npm with `latest: 0.1.1` and exact `@opencode-ai/plugin@1.15.6` dependency; a fresh public-registry install smoke in a throwaway profile loaded plugin id `flowdesk`, 11 tools, `chat.message`, direct doctor/status/usage, and Korean chat-intake routing with `providerCall`, `runtimeExecution`, `actualLaneLaunch`, `fallbackAuthority`, and `hardCancelOrNoReplyAuthority` all false; the active OpenCode profile at `~/.config/opencode` migrated from `@flowdesk/opencode-plugin@0.1.0` to `0.1.1` with `package.json`/`package-lock.json` backups stored as `.bak.2026-05-21-flowdesk-0.1.1`, nested `@opencode-ai/plugin@1.15.6` and `@opencode-ai/sdk@1.15.6` resolved under FlowDesk while a separate top-level `@opencode-ai/plugin@1.3.12` remains for other plugins, and active-profile module-load smoke passed with the same disabled authority flags. Real dispatch, actual lane launch, fallback, and hard cancel/no-reply remain later-gated.
