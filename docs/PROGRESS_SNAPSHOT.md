# FlowDesk Progress Snapshot

## Purpose and Update Rule

This is the compact canonical dashboard for FlowDesk roadmap status. It is not a substitute for tests, conformance evidence, durable session evidence, or release approval.

Update this file whenever code, tests, docs, packaging, installer behavior, conformance evidence, release gates, blockers, or user-facing readiness changes. If a work session changes only historical detail, append or correct the relevant archive entry rather than expanding this dashboard. Historical archive files must not be silently rewritten; if a claim is superseded, add a dated correction note.

If no progress fields changed during a non-trivial work session, the final response should state that this snapshot was checked and did not need an update.

## Current Canonical State (2026-06-07)

- **Wake delivery address live-session hardening** (2026-06-07): Updated `packages/opencode-plugin/src/server.ts` so `completionWakeMainSessionConfigFromOptions()` captures the live OpenCode `ctx.sessionID`/session id when available and exposes it as a parent-session wake fallback; updated `packages/opencode-plugin/src/completion-wake-main-session.ts` so rows missing `parentSessionRef` can still route wake prompts through that captured main-session address; updated `packages/opencode-plugin/src/completion-ui-cache.ts` so workflow wake-ready rows inherit `parentSessionRef` from the merged sidebar rows before falling back to raw current rows. Verification from this session was not run yet (commands require approval).

- **Wake Reliability comprehensive restore** (2026-06-07): Updated `packages/opencode-plugin/src/agent-task-runner.ts` so async task launches always write `agent_task_child_session` index evidence before returning `task_launched`; hardened `packages/opencode-plugin/src/completion-wake-main-session.ts` model parsing so wake prompts always use a valid fallback model (`openai/gpt-5.5`) when row/config parsing fails and restored the main-session active/busy status deferral before dispatch; updated `packages/opencode-plugin/src/completion-ui-cache.ts` so workflow wake-ready rows can inherit `parentSessionRef` from existing wake cache rows when rebuilt from incomplete lane rows. Verification: `npm run build --workspace @flowdesk/opencode-plugin` clean.

- **Wake/Notification Deadlock fix** (2026-06-07): Updated `packages/opencode-plugin/src/completion-wake-main-session.ts` so `consumeFlowDeskCompletionWakeForMainSessionV1` no longer suppresses parent wake prompts when the main session appears active. Wake dispatch now relies on OpenCode's internal session prompt queuing while preserving FlowDesk's global 10s cooldown and strict 1-prompt-per-consume-cycle cap to avoid flooding. Verification: `npm run build --workspace @flowdesk/opencode-plugin` clean.

- **Wake Aggregation parent-session scoping fix** (2026-06-07): Updated `packages/opencode-plugin/src/completion-ui-cache.ts` so workflow-level completion wake-ready aggregate rows inherit `parentSessionRef` from constituent lane evidence even when an earlier context record lacks `parent_session_ref` and the child-session/lifecycle record carries it. Added a focused cache regression test. Also hardened `packages/opencode-plugin/src/server.ts` live context session extraction to accept `ctx.sessionID`, `ctx.sessionId`, or `ctx.session_id` before binding agent-task parent session IDs. Verification from this session was not run yet (commands require approval).

- **Phase 8 federated registry publish boundary hardening (R2)** (2026-06-07): Hardened `flowdesk_federated_registry_publish` in `packages/opencode-plugin/src/server.ts` with explicit runtime validation for `dryRunResult` and `target` before calling the GitHub publication adapter, removed the prior `as never` boundary cast, and added a clean redacted invalid-input envelope for malformed tool payloads. Added a focused server test covering malformed input rejection without publication/advisory result leakage. Verification: `npm run build --workspace @flowdesk/opencode-plugin` clean.

- **Phase 7 Task 18/20 specialist source registers and output contracts** (2026-06-07): Added advisory-only/non-authorizing specialist reference-pack contracts in `packages/core/src/operational-intelligence/specialist.ts`: `FlowDeskSpecialistSourceRegisterV1` (`flowdesk.specialist_source_register.v1`) for approved patent and medical-device source registers, `FlowDeskSpecialistFindingV1` (`flowdesk.specialist_finding.v1`) for specialist task findings, and `FlowDeskHumanReviewBoundaryV1` (`flowdesk.human_review_boundary.v1`) requiring human sign-off boundaries before any product-decision use. Registered the new schema artifacts in `schema-registry.ts` and exported the creators/validators from the OI barrel. Added focused unit tests covering valid patent/medical-device registers, closed-schema rejection, authority-smuggling rejection, human-review-required finding invariants, and sign-off boundary invariants. Verification from this session was not run yet (commands require approval).

- **Phase 8 GitHub/OAuth connector foundation stabilization** (2026-06-07): Fixed the opencode-plugin Phase 8 build breakage and aligned the GitHub federated registry connector/publisher foundation with the core `operational-intelligence/federated.ts` contracts. `federated-registry-connector.ts` now performs token/gh-cli discovery without exposing raw token values, builds core-valid connector capability and token-free GitHub OAuth architecture descriptors, exposes the always-blocked/non-authorizing gate evaluator wrapper, and adds `publishToGitHubV1` for later-gated GitHub issue/PR-comment publication through the REST API. The new publisher keeps `FlowDeskFederatedPublicationResultV1` records advisory-only while allowing a real network call only when `allowActualRemoteWrite=true` and `connectorGateSatisfied=true`; default Release 1 flow skips the write. `server.ts` registers `flowdesk_federated_registry_publish` as a wrapper tool with the same explicit remote-write guard. `federated-registry-publisher.ts` now composes publication intent, capability, and preflight records via core creator functions instead of hand-rolled schema objects. Connector tests cover env-token discovery, missing-tool/auth-missing states, capability descriptor invariants, token-free OAuth architecture, blocked gate authority flags, skipped publication, mocked PR-comment posting, mocked issue creation, and API failure handling; server tests cover the wrapper's advisory skip path. Verification from this session was not run yet (commands require approval); prior verification: `npm run build --workspace @flowdesk/opencode-plugin` clean; `node --test packages/opencode-plugin/dist/federated-registry-connector.test.js` **6/6 pass**. Remote GitHub writes remain blocked by default with no dispatch, provider-call, runtime, lane-launch, fallback, or record-level remote-write authority enabled.

- **Phase 7 Completion Summary (R3-S1~S6.5, S13.5~S18, Wake Sync)** (2026-06-07): Successfully completed all ten core slices of Phase 7 (OI Schemas + Evidence Capture + Minimal Scorer) and integrated the Release 3 admission/execution foundation. (1) **Modularization**: `operational-intelligence.ts` split into 13 bounded submodules with barrel compatibility. (2) **Admission & Gates**: Added `FlowDeskSurplusUsageGateV1`, `FlowDeskR3AdmissionDecisionV1`, `FlowDeskR3FanoutReservationV1`, and `FlowDeskR3ReservationLifecycleEventV1` with validator-layer depth/node caps and non-inheritance invariants. (3) **Scoring Engine**: Implemented `scoreWorkflowProposal()` in `scoring-engine.ts` (0..100 dimension scores from usage data) and `buildOIAssignmentAdvisoryV1()` (outside selection engine). (4) **Executors**: Added pure-function executors `executeMultiVariantTestV1()` (single-model, 4 variants) and `executeMultiModelFanoutTestV1()` (multi-model, high-assurance variant) with `1..10` and `1..100` normalization and `FlowDeskMultiModelAggregationV1` election logic. (5) **Maintenance**: Added read-side and physical `compactFlowDeskAdvisoryLedgerV1()` compaction with TTL (14d) and per-signature capacity (20) pruning. (6) **Wake Sync**: Fixed wake-model drift via global-fallback and launch-time context recording. (7) **Capability Registry**: Seeded 12 model profiles (Opus 4.8→Flash Lite) with benchmark-calibrated initial scores. All work committed (up to `8a933cc`). Final verification: **@flowdesk/core 766/766, @flowdesk/opencode-plugin 553/553, total 1319 pass / 0 fail.**

- **R3-S6.5: Ledger Retention/Compaction Core** (2026-06-07): Implemented core pure advisory ledger compaction in NEW `packages/core/src/operational-intelligence/ledger-compaction.ts`. Added `FlowDeskLedgerCompactionSnapshotV1` (`flowdesk.ledger_compaction_snapshot.v1`) with entry counts, TTL/cap prune counts, pre/post byte sizes, trigger reason, SHA-256 JSONL hashes, and all advisory-only/non-authorizing authority flags. Implemented `compactFlowDeskAdvisoryLedgerV1()` with TTL prune then per-signature newest cap, linked-ledger invariant blocking (`sequence`/`previous_ledger_entry_id` unsupported), deterministic retained ordering, byte/hash metrics, and snapshot validation. Added streaming-style JSONL line decoder helper for routing advisory ledger entries. Registered/exported schema in `operational-intelligence/index.ts`, `schema-registry.ts`, and `schema-artifacts.ts`. Added 11 focused tests covering TTL, cap, linked-ledger blocking, byte/hash metrics, decode errors, authority smuggling, count invariants, no-prune hashes, and cutoff retention. Verification: `npm run build --workspace @flowdesk/core` clean; `node --test packages/core/dist/operational-intelligence.test.js` **255/255 pass, 0 fail**.

- **R3-S6: Routing Advisory + Ledger Retention** (2026-06-07): Implemented advisory-only read-side routing influence for exact model-selection ties in core OI. Added NEW `packages/core/src/operational-intelligence/routing-advisory.ts` with `FlowDeskLedgerRetentionPolicyV1` (`flowdesk.ledger_retention_policy.v1`, defaults: max_score_age_days=14, max_ledger_entries_per_signature=20, prune_mode=`ttl_then_cap`), `FlowDeskRoutingInfluencePolicyV1` (`flowdesk.routing_influence_policy.v1`, opt-in enabled flag, min_sample_threshold default 5, tie_quota_delta_percent=0), and `FlowDeskRoutingAdvisoryEvaluationV1` (`flowdesk.routing_advisory_evaluation.v1`) plus validators and pure `evaluateOIRoutingAdvisoryV1()` (signature filter → TTL prune → newest cap → per-model mean weighted_score/sample_count). Extended `selectModelForBlock()` with optional routing advisory + influence policy; advisory score is consulted only when quota and fitness are exact ties, influence is enabled, and sample thresholds are met; otherwise existing quota/fitness/deterministic ordering remains. Registered/exported the 3 schemas in `operational-intelligence/index.ts`, `schema-registry.ts`, and `schema-artifacts.ts`. Added 5 targeted tests: TTL pruning, cap pruning, advisory tie-break win, advisory ignored when quota/fitness not tied, deterministic fallback when advisory scores tie. Verification: `npm run build --workspace @flowdesk/core` clean; `node --test packages/core/dist/operational-intelligence.test.js` **244/244 pass, 0 fail**.

- **WAKE-MODEL-SYNC Phase 2: record parent model in agent_task_context** (2026-06-07): Implemented recording of the parent session's model ID in the `agent_task_context` evidence. (1) Updated `FlowDeskAgentTaskContextV1` interface and validator in `packages/core/src/retry-plan.ts` to include `parent_wake_provider_qualified_model_id`. (2) Updated `FlowDeskAgentTaskInputV1` and `executeFlowDeskAgentTaskV1()` in `packages/opencode-plugin/src/agent-task-runner.ts` to support and record the parent model ID. (3) Updated `flowdesk_agent_task_run` tool handler in `packages/opencode-plugin/src/server.ts` to extract the calling session's model ID from `ctx.model` (primary) or `options.model` (fallback) and pass it to the task runner. (4) Added focused test cases to `agent-task-runner.test.ts` and `server.test.js` verifying that the parent model is correctly identified and recorded. Verification: `npm run build --workspace @flowdesk/opencode-plugin` clean, `node --test packages/opencode-plugin/dist/agent-task-runner.test.js` passed (8 tests), focused `server.test.js` cases passed.

- **WAKE-MODEL-SYNC Phase 1: main-model-priority wake selection** (2026-06-07): Updated `completionWakeMainSessionConfigFromOptions()` within `packages/opencode-plugin/src/server.ts` so the wake path now prefers the current global `options.model` (the active main-session model) before any wake-specific `providerQualifiedModelId`, then defaults to `openai/gpt-5.5`. This keeps wake prompts aligned with the current main coordinator model after restart while still allowing a wake-specific fallback when the global model is absent. Added focused tests to `server.test.ts` covering both the main-model priority path and the wake-specific fallback path. Verification: `node --test packages/opencode-plugin/dist/server.test.js` **174/174 pass**.

- **Phase 7.5: Promotion of OI scores to real routing influence** (2026-06-07): Successfully promoted operational intelligence scores from advisory-only to active tertiary tie-breakers in the model selection engine. (1) **Model Selection Engine**: Updated `selectModelForBlock()` and `WorkingModelSelectionInput` in `model-selection-engine.ts` to accept `oiPerformanceScores` and use them as a tie-breaker when quota and fitness are tied. (2) **Workflow Assign Tool**: Integrated `evaluateOIRoutingAdvisoryV1()` into `workflow-assign-tool.ts` to fetch fresh OI mean scores before model selection. (3) **Optimization**: Removed redundant `writeAgentTaskChildSessionIndex()` call in `agent-task-runner.ts` (async mode). (4) **Watchdog Cleanup**: Improved zombie lane management in `stall-recovery.ts` — lanes in `inconsistent_finalizing_without_terminal` state are now automatically marked as `orphaned` after 1 hour (3600s). (5) **Phase 8 Foundation**: Added federated registry status to `flowdesk_doctor` (diagnostic surface only) to reflect GitHub/OAuth availability. Verification: `@flowdesk/core 766/766`, `@flowdesk/opencode-plugin 568/568` (total 1334 pass).

- **Phase 8: GitHub Connector & Federated Publication Foundation** (2026-06-07): Established the substantive GitHub publication logic and integration foundation. Progress: **90%**. (1) **Connector Discovery**: Implemented environment-aware token detection (`GITHUB_TOKEN`, `FLOWDESK_GITHUB_OAUTH_TOKEN`) and `gh` CLI state discovery in `federated-registry-connector.ts`. (2) **Publication Logic**: Added `publishToGitHubV1()` supporting PR comments and issue creation. Logic is fully functional but functional network calls are gated behind an explicit `allowActualRemoteWrite` flag for Release 1 safety. (3) **Advisory Alignment**: Updated `federated-registry-publisher.ts` to use core creators for `publication_intent`, `capability_descriptor`, and `publication_preflight`. (4) **Plugin Integration**: Added `flowdesk_federated_registry_publish` tool (opt-in only via `federatedRegistry.enabled`). (5) **Registry Evaluation**: Integrated core blocked-by-default connector gate evaluation into the plugin status surface. (6) **Type Integrity**: Fixed build regressions in `server.ts` and updated test fixtures across all federated contracts. Verification: Build clean, all 65 core OI tests and new plugin connector tests pass. Formal audit and community registry launch remain for final completion.

- **R3-S5: Multi-Model Fanout Executor** (2026-06-07): Implemented `executeMultiModelFanoutTestV1()` pure function (NO IO, NO writes) in NEW `packages/core/src/operational-intelligence/multi-model-fanout-executor.ts`. The executor takes a `multi_model_fanout` admission input + a `reserved` `FlowDeskR3FanoutReservationV1` + a `FlowDeskWorkflowPlanProposalSetV1` + at least 2 `FlowDeskModelCapabilityProfileV1` profiles, scores all models against the `high_assurance` variant via `scoreWorkflowProposal()`, computes `composite_score = clamp(0.6 * proposal_score + 0.4 * capability_score, 0, 1)` per model, elects the winner, and returns a `FlowDeskMultiModelFanoutResultEnvelopeV1` (schema `flowdesk.multi_model_fanout_result_envelope.v1`) containing a `FlowDeskMultiModelAggregationV1` (schema `flowdesk.multi_model_aggregation.v1`) sorted descending by composite_score. Key implementation details: (1) Step 1 validates `execution_mode === "multi_model_fanout"`, `reservation.status === "reserved"`, `selectedModels.length >= 2`, and no duplicate `model_refs` — any violation returns `execution_status: "failed"` + `reservation_consumed: false`; (2) Step 3 scores each model using `scoreWorkflowProposal()` — `proposal_score` = `advisory_score / 100` (0-1), `capability_score` = `complexity_handling_score / 10` (0-1), `composite_score = clamp(0.6*proposal_score + 0.4*capability_score, 0, 1)`; errors → `eligibility_status: "error"`; (3) Step 4 returns failed envelope if fewer than 2 scored; (4) Steps 5-6: sort by composite_score desc, tie-break (delta < 0.02): higher `capability_score` wins (`"capability_score_wins"`), else first-in-profile-order (`"first_in_profile_order"`); (5) Step 7: `election_confidence` = `delta >= 0.15 → "high"`, `>= 0.05 → "medium"`, else `"low"`; (6) `execution_status`: `"complete"` when no errors, `"partial"` when skipped_model_count > 0; (7) `reservation_consumed: true` on success (caller persists). All outputs carry `advisory_only: true`, `non_authorizing: true`, `dispatch_authority_enabled: false`.
