# FlowDesk Progress Snapshot

## Purpose and Update Rule

This is the compact canonical dashboard for FlowDesk roadmap status. It is not a substitute for tests, conformance evidence, durable session evidence, or release approval.

Update this file whenever code, tests, docs, packaging, installer behavior, conformance evidence, release gates, blockers, or user-facing readiness changes. If a work session changes only historical detail, append or correct the relevant archive entry rather than expanding this dashboard. Historical archive files must not be silently rewritten; if a claim is superseded, add a dated correction note.

If no progress fields changed during a non-trivial work session, the final response should state that this snapshot was checked and did not need an update.

- **Session finalization evidence runtime integration** (2026-06-12): The `workflow-session-finalization-evidence-runtime-20260612` lane failed with `no_output` but left salvageable diff. Salvaged patch adds `session_finalization_evidence` evidence class/path, writes additive ref-only session finalization evidence from stall-recovery on meaningful empty step-finish awaiting and safe final text transitions, dedupes repeated awaiting transition, stores `observed_text_ref`/`observed_text_char_count` only. Authority flags are `false`, and there is no task_result gating/status/debug integration yet. Verification: `npm run build --workspace @flowdesk/opencode-plugin && node --test packages/opencode-plugin/dist/async-lane.test.js packages/opencode-plugin/dist/stall-recovery.test.js packages/opencode-plugin/dist/session-finalization-evidence.test.js` → **98/98 pass**; broader `npm run build && node --test packages/opencode-plugin/dist/async-lane.test.js packages/opencode-plugin/dist/stall-recovery.test.js packages/opencode-plugin/dist/status-live-tool.test.js packages/opencode-plugin/dist/session-finalization-evidence.test.js` → **119/119 pass**; `git diff --check` passed.

- **Session finalization evidence contracts slice** (2026-06-12): Added pure `session-finalization-evidence.ts` contracts/evaluator and focused tests for session idle/status/output-presence finalization decisions. The evaluator preserves the committed demotion policy: empty `step-finish` remains awaiting, idle without text never captures, text with running or unknown tools fails closed, low confidence requires review, and `process_notes`/`tool_trace_only`/`empty` are not auto-synthesis-safe. A verifier found an initial raw-text evidence mismatch; fixed by replacing raw `latest_final_text`/`captured_text` with `observed_text_ref` and `observed_text_char_count` metadata only. Authority flags remain false and no OpenCode internal validation is claimed. Focused verification: `npm run build --workspace @flowdesk/opencode-plugin && node --test packages/opencode-plugin/dist/session-finalization-evidence.test.js` → **8/8 pass**. Remediation-focused verification: `npm run build --workspace @flowdesk/opencode-plugin && node --test packages/opencode-plugin/dist/session-finalization-evidence.test.js packages/opencode-plugin/dist/capture-finalization-v2.test.js packages/opencode-plugin/dist/async-lane.test.js packages/opencode-plugin/dist/stall-recovery.test.js` → **104/104 pass**; `git diff --check` passed.

- **Post-restart live probe evidence** (2026-06-12): Validated post-restart behavior for commit `f4f2e0a`. `workflow-live-demotion-probe-20260612` (`task-mqagmtw8`) captured `task_result` final_answer with `LIVE_DEMOTION_PROBE_SENTINEL` (`usableForSynthesis=true`); `workflow-live-demotion-race-probe-20260612` (`task-mqagnl77`) captured `task_result` final_answer with `RACE_PROBE_FINAL_SENTINEL` (`usableForSynthesis=true`). A later docs-update lane, `workflow-live-probe-progress-update-20260612` (`task-mqago8w9`), exercised the empty `step-finish` path: status evidence first recorded `awaiting_body_capture_empty` with `lastPartKind=step-finish`, `finalTextPresent=false`, and `stepFinishPresent=true`, then later terminalized as `task_result` final_answer (`usableForSynthesis=true`) instead of `no_output`.

- **Step-finish empty-body demotion stabilization** (2026-06-12): Updated `monitorChildSessionsV1` so an empty `turn_completed`/`step-finish` observation is treated as awaiting body capture/session quiescence rather than terminal `no_output`; retry/repair budget exhaustion alone no longer writes `task_failed`. The lane may still terminalize through independent hard caps such as `finalizingAbsoluteMaxMs` or other failure paths. Updated async/stall tests to assert non-terminal demotion and retained absolute-cap terminalization coverage. Focused verification: `npm run build --workspace @flowdesk/opencode-plugin && node --test packages/opencode-plugin/dist/stall-recovery.test.js packages/opencode-plugin/dist/async-lane.test.js` → **90/90 pass**. Remediation-focused verification: `npm run build --workspace @flowdesk/opencode-plugin && node --test packages/opencode-plugin/dist/async-lane.test.js packages/opencode-plugin/dist/capture-finalization-v2.test.js packages/opencode-plugin/dist/route-decision-guard.test.js` → **51/51 pass**; `git diff --check` passed. Full verification: `npm test` → **1399/1399 pass, 0 fail**.

- **Plugin remediation v2 Slice 2a verification** (2026-06-12): Verified Slice 2a pure capture finalization contracts and evaluator. Focused verification: `npm run build --workspace @flowdesk/opencode-plugin && node --test packages/opencode-plugin/dist/capture-finalization-v2.test.js packages/opencode-plugin/dist/route-decision-guard.test.js` → **11/11 pass**. Initial full `npm test` failed due to stale artifact `packages/opencode-plugin/dist/agent-task-output.test.js` after source test deletion; resolved by removing the stale artifact. Verification: `npm test` → **1399/1399 pass, 0 fail**. Note: Verifier FlowDesk lanes were unreliable (first `no_output`, retry `process_notes` only), necessitating bounded direct verification instead.

- **process_notes synthesis safety enforcement** (2026-06-11): Hardened the task_result evidence path so that `process_notes` (and `tool_trace_only`/`empty`) output_kind can **never** produce `usable_for_synthesis=true` or `safe_for_auto_synthesis=true` in persisted evidence. Three layered enforcement points: (1) `agent-task-output.ts` `observeFlowDeskAgentTaskOutputV1` — explicit named variable with invariant comment; (2) `agent-task-runner.ts` `buildFlowDeskCaptureSafetyMetadataV1` — `synthesisEligibleKind` guard makes `final_answer`-only synthesis eligibility the single structural gate; (3) `agent-task-runner.ts` `executeFlowDeskAgentTaskV1` evidence write — explicit post-spread re-enforcement clamp on `usable_for_synthesis` and `safe_for_auto_synthesis` keyed on `resolvedOutputKind`. Process-note text is still preserved in `result_text` for display. 5 new targeted tests added to `agent-task-runner.test.ts` (unit coverage for `buildFlowDeskCaptureSafetyMetadataV1`, `observeFlowDeskAgentTaskOutputV1`, and end-to-end `executeFlowDeskAgentTaskV1`). Also aligned the installed `flowdesk-main` template wording with the interface-first prompt assertions. Verification: `npm run build && node --test packages/opencode-plugin/dist/bootstrap-installer.test.js packages/opencode-plugin/dist/agent-task-runner.test.js` → **26/26 pass**; `npm test` → **1387/1387 pass, 0 fail**.

- **Body-capture late-final-text regression coverage** (2026-06-11): Added focused `async-lane.test.ts` coverage for the observed `turn_completed`/`step-finish` empty-body race: empty terminal-marker observations remain repairable before retry/absolute windows expire, late final text after an initially empty step-finish persists as `task_result`, and `no_output` terminalization occurs only after the bounded retry budget expires with no body. This is test-only coverage; no production behavior changed. Verification: `npm run build --workspace @flowdesk/opencode-plugin && node --test packages/opencode-plugin/dist/async-lane.test.js` → **40/40 pass**; `npm test` → **1388/1388 pass, 0 fail**.

- **Plugin remediation v2 Slice 1 — route decision guard** (2026-06-11): Added `route-decision-guard.ts` with a plugin-owned static agent capability projection, bounded task intent/capability classification, and route decision evidence/validator that blocks edit/test/implementation work from verifier-only primary lanes while allowing verify-only lanes and requiring split/implementation-capable primary handling for mixed implementation+verification work. All authority flags remain false and no OpenCode-internal validation claims are made. Focused verifier lane verdict: **pass**. Verification: `npm run build --workspace @flowdesk/opencode-plugin && node --test packages/opencode-plugin/dist/route-decision-guard.test.js` → **5/5 pass**; `npm test` → **1393/1393 pass, 0 fail**.

- **Release 3 OI score-ledger synthetic E2E coverage** (2026-06-11): Added `packages/opencode-plugin/src/score-ledger-e2e.test.ts`, a local advisory-only integration test that proves the previously identified process gap is connected end-to-end: 100 score-like routing advisory entries are appended into a score-ledger partition, the writer-created manifest advances from raw to sealed, the sealed partition computes a rollup, and the score-ledger GitHub publisher reaches the dry-run boundary with all authority flags still false. No production runtime behavior changed. Verification: `npm run build && node --test packages/opencode-plugin/dist/score-ledger-e2e.test.js` → **1/1 pass**; `npm test` → **1388/1388 pass, 0 fail**.

- **Interface-first cross-cutting feature guidance** (2026-06-11): Added concise rule wording to the project `flowdesk-main` prompt, bootstrap installer materialization template, and root repo guidance. Cross-subsystem work touching authority/gates, evidence schemas, provider/model selection, OI/GitHub data flows, status/doctor/debug, runtime/watchdog/session, chat/TUI behavior, or docs/conformance requirements must produce a design/interface artifact first, then split implementation into focused lanes. No runtime behavior changes beyond future installed prompt materialization.

- **Documentation patch: Release 3 final sign-off preparation** (2026-06-11): Applied documentation patches to README.md, .opencode/agent/flowdesk-main.md, and docs/USER_MANUAL.md to clarify advisory OI/provider-catalog capabilities for Release 3. Preserved all authority boundaries. No code changes.

- **Event hook child-session binding lookup optimization** (2026-06-11): `packages/opencode-plugin/src/event-hook-observer.ts` now keeps an in-memory `childSessionId -> workflow/lane/task` binding index with sliding TTL retention and a short miss debounce. On index miss it performs a single-flight filesystem fallback scan of `.flowdesk/sessions/*/evidence/agent-task-child-session`, indexing all discovered bindings so later sibling events avoid rescans. Irrelevant event types are filtered before binding lookup. Evidence semantics, completion wake/done behavior, progress/stall observations, and authority flags remain unchanged; no dispatch/fallback/write/main-chat authority added. Focused tests added in `event-hook-observer.test.ts`; verification pending until focused plugin tests are run.

- **Release 3 completion documentation update** (2026-06-11): Marked Release 3 as complete following score-ledger compatibility closure and verification. Fixed score-ledger `.flowdesk/oi` paths, manifest wrapper compatibility, sha256 hash refs, hook/input lag stabilization, and done wake fix. Clarified High 5/6/7 authority. Advisory-only semantics preserved; no new dispatch/fallback/write/main-chat suppression authority added. Old failed lane is superseded and not an active blocker. Verification: `npm run typecheck` clean; focused score-ledger **86/86 pass**; full `npm test` **1380/1380 pass**.


- **Release 3 OI score-ledger compatibility blockers closed** (2026-06-11): Unified score-ledger disk roots on `<rootDir>/.flowdesk/oi/score-ledger` and migrations on `<rootDir>/.flowdesk/oi/score-ledger-migrations`; partition writer now writes lifecycle-validator-compatible `FlowDeskScoreLedgerManifestV1` wrapper manifests while retaining direct-partition read compatibility for existing manifests; writer result/manifest `genesis_hash` and `chain_head_hash` now use `sha256-<64 hex>` refs accepted by validators. Added focused writer→lifecycle and writer→lifecycle→migration compatibility tests. Advisory-only semantics and authority flags unchanged. Verification: `npm run typecheck` clean; focused score-ledger node tests **47/47 pass, 0 fail**.

- **Slice E hook/input lag reduction** (2026-06-11): `chat.message` no longer awaits live provider usage acquisition; stale/missing provider-usage sidebar cache refresh is scheduled as a background single-flight update. Chat usage-card rendering now uses sidebar-cache-only reads and skips durable provider-usage snapshot fallback scans. Stall/status cards now use an in-hook cached/debounced status-live result rather than reloading full status/stall evidence on every message; explicit `/flowdesk-usage` and `/flowdesk-status` tool behavior remains unchanged. No dispatch, fallback, write, hard-chat, or authority changes. Verification pending until focused plugin tests are run.

- **High 7 abort path unification** (2026-06-11): `abortFlowDeskAgentTaskV1` now routes dev/beta SDK task aborts through the canonical typed `abortFlowDeskSessionWithDecisionV1` session-abort adapter before writing `task_failed` and terminal `lane_lifecycle` evidence. Diagnostic `/flowdesk-abort` remains non-runtime/non-SDK; `flowdesk_abort_cmd` remains a compatibility alias for command-backed diagnostic abort. No cross-family model substitution or hook-lag paths changed. Verification pending in this session unless noted in final response.

- **Stabilization: completion wake/done notification race fix** (2026-06-11): Fixed race condition in `packages/opencode-plugin/src/completion-wake-main-session.ts` where wake consumption could trigger before the `task_result` row existed due to main-session gate issues. Note: No new dispatch/fallback/write authority. Verification: `npm test packages/opencode-plugin/src/completion-wake-main-session.test.ts` → **1375/1375 pass, 0 fail**.

- **Duplicate test removed** (2026-06-09): Removed a duplicate test `local adapter loads project config and policy packs from disk for non-dispatch runs` from `packages/opencode-plugin/src/server.test.ts` at line 6852. The test name `local adapter fails closed when durable state cannot be written` in the prompt was a mismatch, but the line number was correct for the intended removal. Verification: `npm run build && node --test --test-name-pattern "local adapter fails closed when durable state cannot be written" packages/opencode-plugin/dist/server.test.js` passed, showing 1 test pass, 0 fail.

## Current Canonical State (2026-06-11)

- **Result-capture safety metadata slice** (2026-06-11): Added additive capture-safety metadata fields to `task_result.v1` / capture-path `task_failed.v1` records and validators (`capture_status`, `capture_confidence`, `observed_text_kind`, `final_body_observed`, `terminal_marker_observed`, `requires_coordinator_review`, `safe_for_auto_synthesis`, `display_as_uncertain_result`). `usable_for_synthesis` is now aligned conservatively with `safe_for_auto_synthesis`; process-note and partial/uncertain captures are preserved but not auto-synthesis-safe. Terminal-marker-without-current-body can preserve previously observed assistant text as uncertain output in the sync capture path; truly empty capture remains `no_output` / `task_failed`. Verification: `npm run build && node --test packages/opencode-plugin/dist/bootstrap-installer.test.js packages/opencode-plugin/dist/agent-task-runner.test.js` → **26/26 pass**; `npm test` → **1387/1387 pass, 0 fail**.

- **Terminology standardization: Main-chat suppression vs SDK-scoped noReply control** (2026-06-11): Implemented terminology standardization for 'hard chat cancellation' and 'noReply authority' to distinguish main-chat turn suppression from SDK-scoped child-session control. Updated `packages/opencode-plugin/src/server.ts` tool descriptions, `docs/USER_MANUAL.md`, `README.md`, `docs/IMPLEMENTATION_ROADMAP.md`, and `docs/OPENCODE_CONFORMANCE_PLAN.md`. No runtime behavior or authority changes. All hard main-chat suppression authority remains `false`.

- **Phase 7 (Operational Intelligence) 전체 완성** (2026-06-11): `npm run typecheck` → 0 errors; `npm test` → **1375/1375 pass, 0 fail** (기존 1115 + Phase 7 신규 260). S01~S27 전체 슬라이스 + S03 픽스 완료. 주요 구현: 9개 scoring dimension 실계산(S02), atomic ledger writer(S04), threshold gate pipeline(S05), fanout cadence(S06/S07), fail-closed gate(S08), OI MCP 5개 툴(S10), RFC 8785 JCS(S16), hash chain(S17), partition writer/lifecycle(S18/S19), ledger-rollup HHI/ESS/decay(S20), rollup safety tests EC#8/#9(S21), GitHub publisher(S22), conflict/retention policy(S23), GH Actions permission hardening(S24), Pages static export(S25), migration trigger(S26), external OIDC DB connector(S27). Release 1 gates active: dispatch disabled, authority redaction enforced, fail-closed behaviors, durable evidence persistence.

- **S03 테스트 픽스: operational-intelligence.test.ts 타입 에러 수정** (2026-06-11): `packages/core/src/operational-intelligence.test.ts` lines 8810–8912에 추가된 8개 테스트가 35개 typecheck 에러를 발생시키던 문제 수정. 원인: (1) `FlowDeskScoringEngineInputV1`에 존재하지 않는 `dispatchability` 필드 사용; (2) 필수 필드 `workflowId`, `proposalId`, `candidateRef` 누락; (3) `FlowDeskTaskBlockScoringV1`에 `scoring_version: "v1"` 사용 (존재하지 않는 필드); (4) `result.dimensions.X.score` 접근 (실제 구조는 `result.score!.score_dimensions`). 수정 내용: `dispatchability` 제거, 필수 필드 추가(opaque ID 형식), `blockScoring`에 `createFlowDeskTaskBlockScoringV1()` 팩토리 사용, `score_dimensions.find(d => d.dimension === "X")!.score` 패턴으로 결과 접근 교체. Verification: `npm run typecheck` → 0 errors; node --test (8개 신규 테스트) → 8/8 pass; `node --test packages/core/dist/operational-intelligence.test.js` → **276/276 pass, 0 fail** (리그레션 없음).

- **P7-S27: external-managed-db-connector.ts — GitHub Actions OIDC 외부 DB 연동** (2026-06-11): `packages/opencode-plugin/src/external-managed-db-connector.ts` 신규 작성 + `packages/opencode-plugin/src/external-managed-db-connector.test.ts` 신규 작성 (12개 테스트) + `docs/EXTERNAL_MANAGED_DB_SCHEMA.md` 신규 작성. (1) **`ingestToExternalManagedDbV1(input)`** — Gate 1: `allowActualRemoteWrite !== true` → `dry_run` (기본값); Gate 2: `idempotencyKey` 이미 seen → `idempotent_skip`; Gate 3: OIDC 보호 엔드포인트에 HTTP POST, 2xx → `ingested`, non-2xx → `blocked`(`http-error:<status>` 레이블), fetch throw → `blocked`(`fetch-error:<msg>` 레이블). (2) **보안 제약**: `weighted_mean_score` 등 raw 점수값은 페이로드에서 완전 제외; `confidence_bucket`(advisory label)만 전송; `advisory_only: true`, `dispatch_authority_enabled: false` 페이로드에 포함. (3) **`MigrationManifestV1` (S26 타입) 정의**: `schema_version: "flowdesk.migration_manifest.v1"`, `trusted_chain_head_invalidated?: boolean`; `trusted_chain_head_invalidated: true` 시 결과에 `cutover_required: true` 포함. (4) **`source_precedence: "local_first"`** — 항상 고정, 모든 경로에서 반환. (5) **인-프로세스 idempotency store**: `Set<string>` 기반, `_resetIdempotencyStoreForTest()` 테스트 전용 리셋 익스포트. (6) **권한 플래그**: `advisoryOnlyRecord: true`, `remoteWriteAuthorityEnabledInRecord: false`, `dispatchAuthorityEnabled: false` — 모든 경로에서 항상 false. (7) **테스트 12개**: `allowActualRemoteWrite=false` → `dry_run`, `allowActualRemoteWrite=true` + mock 2xx → `ingested`, 중복 `idempotencyKey` → `idempotent_skip`, `trusted_chain_head_invalidated=true` → `cutover_required: true`(dry_run+ingested 경로 각각), authority flags always false (dry_run), authority flags always false (ingested), mock 4xx → `blocked` with `http-error` label, fetch throw → `blocked` with `fetch-error` label, `source_precedence` 항상 `local_first` (3경로), raw `weighted_mean_score` payload 미포함 + `confidence_bucket` 포함 검증, manifest without invalidation → `cutover_required` absent, key-scoped idempotency. (8) **문서**: `docs/EXTERNAL_MANAGED_DB_SCHEMA.md` — OIDC 연동 구조, ingestion watermark, cutover 절차, idempotent replay 보장, migration manifest 연결, ingestion payload schema(excluded fields 포함), safety properties. Verification: `npx tsc -p packages/opencode-plugin/tsconfig.json --noEmit` → 0 errors (신규 파일 클린; 기존 `operational-intelligence.test.ts` 오류는 사전존재); `node --test packages/opencode-plugin/dist/external-managed-db-connector.test.js` → **12/12 pass, 0 fail**.

- **P7-S26: score-ledger-migration-trigger.ts — schema/policy hash 변경 시 migration manifest** (2026-06-11): `packages/opencode-plugin/src/score-ledger-migration-trigger.ts` 신규 작성 + `packages/opencode-plugin/src/score-ledger-migration-trigger.test.ts` 신규 작성 (12개 테스트). (1) **`evaluateMigrationTriggerV1(input: MigrationTriggerInputV1): MigrationManifestV1`** — schema/policy hash 비교 → `no_change` 시 디스크 I/O 없이 즉시 반환; 변경 감지 시 `<rootDir>/oi/score-ledger/*.manifest.json` 스캔; 영향 파티션 = `state !== "raw"` (sealed/immutable); immutable 파티션 존재 시 `trusted_chain_head_invalidated: true`; migration manifest를 `<rootDir>/oi/score-ledger-migrations/<migration_id>.json`에 원자 기록(tmp→renameSync). (2) **trigger reason 4가지**: `no_change`(변경없음), `schema_hash_changed`(schema만 변경), `policy_hash_changed`(policy만 변경), `both_changed`(둘 다 변경). policy 변경 감지는 both policyHash 인수가 모두 제공되어야 함(한쪽만 있으면 no policy change). (3) **디스크 포맷 이중 지원**: `FlowDeskScoreLedgerPartitionV1` 직접 포맷(S18 partition writer) + `FlowDeskScoreLedgerManifestV1` 래퍼 포맷(S19 lifecycle) 둘 다 인식. 알 수 없는 포맷은 조용히 스킵. (4) **권한 플래그**: `advisory_only: true`, `dispatch_authority_enabled: false` 모든 경로에서 리터럴 상수. (5) **테스트 12개**: no_change(sealed 파티션 있어도 스캔하지 않음), schema_hash_changed(sealed×2+raw×1 → affected=[sealed×2], raw 제외), immutable 파티션 → trusted_chain_head_invalidated=true, policy_hash_only_changed, both_changed, 파티션 없음 → migration_required=false, 전체 raw → migration_required=false, authority flags 검증, manifest wrapper 포맷(FlowDeskScoreLedgerManifestV1) 인식, migration 파일 oi/score-ledger-migrations/ 원자 기록+JSON 검증, policy 동일 → no_change, policy 한쪽만 제공 → no policy change. Verification: `npm run typecheck` — 신규 파일 0 errors(기존 `operational-intelligence.test.ts` 오류는 사전존재); `node --test dist/score-ledger-migration-trigger.test.js` → **12/12 pass, 0 fail**; `node ../../scripts/run-tests.mjs --mode functional --package opencode-plugin` → **460/460 pass, 0 fail** (리그레션 없음).

- **P7-S25: GitHub Pages aggregate summary export** (2026-06-11): `packages/opencode-plugin/src/rollup-static-export.ts` 신규 작성 + `packages/opencode-plugin/templates/github-workflows/flowdesk-pages.yml` 신규 작성 + `packages/opencode-plugin/src/rollup-static-export.test.ts` 신규 작성 (10개 테스트). (1) **`rollup-static-export.ts`** — `generateRollupStaticPageV1(input: RollupStaticExportInputV1): RollupStaticExportResultV1` 구현. **Redaction gate**: 입력 rollup을 JSON 직렬화 후 `FORBIDDEN_RAW_PAYLOAD_MARKERS` 스캔 (정규식 word-boundary 방식) → 매치 시 hard error throw. **Raw score 노출 금지**: `weighted_mean_score`, `p25`, `p50`, `p75`, `p90`, `decay_adjusted_mean`, `scorer_concentration` 등 수치 필드는 HTML에 절대 포함하지 않음. **안전 표시 필드만**: `confidence_bucket` (display label: "Low"/"Medium"/"High"/"Very High"), `sample_count`, `last_observation_at`만 표출. **HTML 구조**: `advisory_only: true | dispatch_authority_enabled: false` HTML 주석 필수 포함; advisory banner; inline CSS 전용 (외부 리소스 없음, `<link>` 태그 없음, `http://`/`https://` URL 없음). **권한 플래그**: `redactionApplied: true`, `advisory_only: true`, `dispatch_authority_enabled: false` 리터럴 상수. (2) **`flowdesk-pages.yml`** — `workflow_dispatch` 전용 트리거; 최소 권한 블록: `contents: write`, `pages: write`, `id-token: write`; `persist-credentials: false`; `npm ci --ignore-scripts`; `actions/checkout@v4` + `actions/setup-node@v4` + `actions/upload-pages-artifact@v3` + `actions/deploy-pages@v4`; Generate summary page 스텝은 현재 placeholder (`dist/pages/index.html` 생성) + 향후 실구현 주석 포함. `environment: github-pages` 설정으로 Pages 배포 탭 연결. (3) **테스트 10개**: (1) raw score 값 HTML 미노출 (weighted_mean_score/p25/p50/p75/p90 등 7개 값 체크), (2) FORBIDDEN_RAW_PAYLOAD_MARKERS 포함 rollup → throw (message에 "forbidden raw payload marker" 포함), (3) advisory_only 주석 HTML 포함 (`advisory_only: true` + `dispatch_authority_enabled: false`), (4) result authority flags 정확성 (advisory_only=true, dispatch_authority_enabled=false, redactionApplied=true), (5) 안전 필드 HTML 포함 (confidence_bucket 레이블, sample_count, last_observation_at), (6) 커스텀 pageTitle HTML 반영, (7) 4개 confidence bucket 레이블 렌더링 (Low/Medium/High/Very High), (8) pageTitle XSS HTML escape (`<script>` → `&lt;script&gt;`), (9) 기본 pageTitle 사용 확인, (10) 최소 유효 HTML 구조 (DOCTYPE/html/head/body/inline style, 외부 리소스 없음). (4) **검증**: `npm run typecheck` — 신규 파일 0 errors (기존 `operational-intelligence.test.ts` 오류는 사전존재; P7-S25 무관); `grep "rollup-static-export"` 결과 없음 → typecheck 에러 없음 확인.

- **P7-S10: OI MCP 툴 등록 인프라 + 5개 핸들러** (2026-06-11): `packages/opencode-plugin/src/oi-mcp-tools.ts` 신규 작성 + `packages/opencode-plugin/src/server.ts` additive 수정. (1) **`oi-mcp-tools.ts`** — `createFlowDeskOIOptInTools({ rootDir, oiConfig })` 팩토리 함수로 5개 advisory-only MCP 툴 생성: `flowdesk_oi_score_preview`(scoreWorkflowProposal 래핑, 영속화 없음), `flowdesk_oi_threshold_gate`(createFlowDeskScoreReuseThresholdGateV1 래핑, 기본 max_age 300초), `flowdesk_oi_ledger_list`(loadRoutingAdvisoryLedgerV1 호출, limit 클램핑 1..50), `flowdesk_oi_ledger_compact`(compactFlowDeskAdvisoryLedgerV1 직접 호출, triggerReason 불명 시 "manual" 정규화), `flowdesk_oi_session_summary`(loadRecentOISessionSummariesV1 async 호출, maxCount 클램핑 1..20). 모든 핸들러 응답에 `authority` 블록 포함 — `advisoryOnly: true`, 전체 authority flag false. (2) **`server.ts` additive 수정** — 툴 이름 상수 5개 추가(L415 근처): `flowdeskOIScorePreviewToolName`, `flowdeskOIThresholdGateToolName`, `flowdeskOILedgerListToolName`, `flowdeskOILedgerCompactToolName`, `flowdeskOISessionSummaryToolName`; `createFlowDeskOIOptInTools` import 추가; OI 툴 등록 블록 삽입(orchestrateTools 이후, uiProbe 이전): `oiConfig.exposeMcpTools === true && durableStateRoot !== undefined` 조건 하에 등록. (3) **테스트** `oi-mcp-tools.test.ts` 신규 — 22개 테스트: `createFlowDeskOIOptInTools` 2개(5개 툴 반환, 이름 상수 일치), `score_preview` 4개(valid→score_computed, authority flags, empty workflowId→score_failed, exhausted→degraded), `threshold_gate` 4개(valid→gate_evaluated, fresh→reuse, stale→recompute, authority flags), `ledger_list` 4개(빈 ledger, entries 존재, limit 준수, authority flags), `ledger_compact` 4개(empty→complete, entries→retained count, unknown reason→manual, authority flags), `session_summary` 4개(empty summaries, maxCount 클램핑, workflowId echo, authority flags). (4) **검증**: `npm run typecheck` — 신규 파일 0 errors(기존 `operational-intelligence.test.ts` 오류는 사전존재); `node --test packages/opencode-plugin/dist/oi-mcp-tools.test.js` → **22/22 pass, 0 fail**; `node scripts/run-tests.mjs` → 1333/1341 pass — 8개 실패는 사전존재 `scoreWorkflowProposal` 테스트, P7-S10 신규 22개 전부 통과.

- **P7-S24: GitHub Actions workflow permission 하드닝 + 템플릿** (2026-06-11): `packages/opencode-plugin/templates/github-workflows/flowdesk-score-ledger.yml` 신규 작성 + `scripts/audit-github-workflow-permissions.mjs` 신규 작성. (1) **`flowdesk-score-ledger.yml`** — `workflow_dispatch` 트리거 전용 (push/PR 트리거 없음); inputs: `partition_id`(required), `dry_run`(default true). 최소 권한 블록: `contents: read`, `issues: write`, `pull-requests: write` 명시; `write-all`/`admin` 없음. 보안 고려사항 인라인 주석: `persist-credentials: false`(토큰 디스크 저장 방지), `npm ci --ignore-scripts`(공급망 공격 방지), `GITHUB_TOKEN vs PAT` 가이드, `workflow_dispatch only` 이유. `Validate partition state` 스텝: `checkAndAdvancePartitionStateV1` 호출, `sealed`/`immutable`만 통과(`raw` → exit 1). `Publish` 스텝: `if: inputs.dry_run == false` 조건부, `GITHUB_TOKEN`은 해당 스텝에만 주입. `Upload artifacts` 스텝: `if: always()`, `if-no-files-found: warn`, `retention-days: 30`. (2) **`scripts/audit-github-workflow-permissions.mjs`** — `node:fs`/`node:path`/`node:process` 전용(서드파티 의존성 없음). `--dir <path>` (기본값: `packages/opencode-plugin/templates/github-workflows`), `--verbose`, `--help` 플래그. 7개 규칙 적용: Rule 1+2(`write-all`/`admin` 인라인 금지), Rule 3(`contents: write`는 publish/release/deploy 이름 패턴에만 허용), Rule 4(`actions: write` 무조건 금지), Rule 5(`packages: write` 무조건 금지), Rule 6(`secrets: read/write` 금지—secrets는 expression으로 접근), Rule 7(명시적 `permissions:` 블록 필수). per-job 권한 체크 포함. 종료 코드: 0=전체 통과, 1=위반 발견, 2=호출 오류. (3) **검증**: `npm run typecheck` — 새 파일(YAML/MJS)은 TypeScript 대상 외; 기존 `operational-intelligence.test.ts` 오류는 사전존재 (P7-S24 무관). 인라인 smoke test(`node -e`): `name: FlowDesk Score Ledger Publish`, `hasTopPermissions: true`, `topPermissions: {contents:read, issues:write, pull-requests:write}`, `violations: NONE (PASS)` 확인.

- **P7-S23: score-ledger-conflict-policy.ts — event_id 충돌, 중복 partition, retention/archive** (2026-06-11): `packages/opencode-plugin/src/score-ledger-conflict-policy.ts` 신규 작성. (1) **`resolveEventIdConflictV1(input)`** — event_id 충돌 해결: 동일 hash → `skip_duplicate`; 다른 hash + incoming strictly newer → `replace_if_newer`; 다른 hash + 시간 동일/역순/파싱 불가 → `quarantine`. (2) **`detectDuplicatePartitionV1(input)`** — partition 중복/충돌 감지: 동일 `partition_id` 존재 → `{ isDuplicate: true, conflictType: "same_id" }`; 동일 `genesis_hash` 다른 id → `{ isDuplicate: true, conflictType: "same_genesis" }` (fork 감지); 없음 → `{ isDuplicate: false, conflictType: "none" }`; same_id 검사 우선(same_genesis 앞). (3) **`evaluatePartitionRetentionV1(input)`** — retention/archive 정책 평가 (우선순위 순): Rule 1 — `pending_gate_promotion` 상태이면 `action: "keep"` 무조건; Rule 2 — `immutable` + age > `max_score_age_days` → `action: "archive"`, `archivePath: "archive/YYYY/partition-id.jsonl"` (YYYY는 `now` 기준); Rule 3 — `raw` + age > `sealing_window_seconds * 2` → `action: "delete"`; Rule 4 — 나머지(sealed 포함) → `action: "keep"`. (4) **권한 플래그**: pure 계산 함수, I/O 없음, dispatch/write/provider/runtime 권한 모두 없음. (5) **테스트** `score-ledger-conflict-policy.test.ts` 신규 — 19개 테스트: `resolveEventIdConflictV1` 6개(identical hash→skip, newer→replace, older→quarantine, equal time→quarantine, 파싱불가existing→quarantine, 파싱불가incoming→quarantine), `detectDuplicatePartitionV1` 5개(same_id, same_genesis, none, empty list, same_id priority), `evaluatePartitionRetentionV1` 8개(immutable+old→archive with correct YYYY path, immutable+fresh→keep, raw+stale→delete, raw+fresh→keep, pending_gate_promotion→keep, sealed→keep, archive year from now, custom max_score_age_days). Verification: `npx tsc -p packages/opencode-plugin/tsconfig.json --noEmit` → 0 errors (our files); pre-existing `operational-intelligence.test.ts` errors are unrelated; `node --test packages/opencode-plugin/dist/score-ledger-conflict-policy.test.js` → **19/19 pass, 0 fail**; related tests (lifecycle, writer, routing-advisory-pipeline) → **36/36 pass, 0 fail**.

- **P7-S08: routing-advisory-pipeline.ts — stale/malformed snapshot fail-closed gate** (2026-06-11): Added `filterValidLedgerEntries` fail-closed gate to `packages/opencode-plugin/src/routing-advisory-pipeline.ts` and extended `RoutingAdvisoryPipelineResultV1` with additive `excludedEntryCount?` and `excludedReasons?` fields. (1) **`filterValidLedgerEntries(entries, options?)`** — exported pure function that inspects each `FlowDeskRoutingAdvisoryLedgerEntryV1` and excludes any entry that fails ANY of 4 conditions: (a) `schema_validation_failed` — required fields (`signature_ref`, `model_ref`, `weighted_score`, `recorded_at`) missing or wrong type; (b) `score_out_of_range` — `weighted_score × 100` outside 0..100 (catches entries that pass the JSONL loader's structural check but have semantically invalid scores like 1.5 → 150); (c) `schema_hash_mismatch` — entry carries a `schema_hash` field that doesn't match `options.schemaHash` when provided; (d) `forbidden_payload_detected` — `validateNoForbiddenRawPayloads` returns errors. Returns `{ valid: FlowDeskRoutingAdvisoryLedgerEntryV1[]; excluded: Array<{ reason: string; index: number }> }`. (2) **Pipeline integration** — `evaluateAndRecordRoutingAdvisoryV1` now calls `filterValidLedgerEntries(rawEntries)` after loading the ledger. Only `filterResult.valid` entries proceed to signature filtering and gate evaluation. Since `hasEnoughSamples` uses `valid` entry counts, all-excluded → 0 samples < `minSampleCount` → forced `recompute` (fail-closed). `excludedEntryCount` and deduplicated `excludedReasons` are included in the result when non-zero (additive only). (3) **`RoutingAdvisoryPipelineResultV1`** extended with `excludedEntryCount?: number` and `excludedReasons?: string[]`. (4) **Tests** — 3 new tests in `packages/opencode-plugin/src/routing-advisory-pipeline.test.ts` under `filterValidLedgerEntries (P7-S08 fail-closed gate)` describe block: S08-1 (malformed entry excluded at unit level + pipeline integration via out-of-range score_out_of_range), S08-2 (schema_hash_mismatch exclusion with matching/missing hash entries), S08-3 (all 3 out-of-range-score entries excluded → pipeline forces recompute fail-closed). All 9 existing tests continue to pass (no regressions). Verification: `npm run typecheck` clean for target files (0 new errors; pre-existing errors in `operational-intelligence.test.ts` and `score-ledger-github-publisher.ts` are pre-existing); `node --test dist/routing-advisory-pipeline.test.js` → **14/14 pass, 0 fail**; full opencode-plugin functional suite → **385/385 pass, 0 fail**.

- **P7-S22: score-ledger-github-publisher.ts — sealed partition GitHub publish** (2026-06-11): `packages/opencode-plugin/src/score-ledger-github-publisher.ts` 신규 작성. (1) **Gate 계층**: Gate 1 — `partition.state` must be `"sealed"` or `"immutable"` (raw 입력 시 `blocked_labels: ["partition-not-sealed"]` 즉시 반환); Gate 2 — `allowActualRemoteWrite !== true` 시 네트워크 호출 없이 `dry_run_recorded` 반환; Gate 3 — `allowActualRemoteWrite === true` 시 `publishToGitHubV1` 위임(compaction lock 체크, redaction marker scan, AND-gate 조건 상속). (2) **JSONL 스캔**: `partitionJsonl` 콘텐츠에 `FORBIDDEN_RAW_PAYLOAD_MARKERS` 포함 시 `partition-jsonl-contains-forbidden-marker` 블록. (3) **Compaction lock 체크**: `durableStateRoot` 제공 시 `flowDeskCompactionLockPathV1(durableStateRoot)` 경로 체크; 없으면 cwd-relative fallback (back-compat). (4) **Dry-run 결과 합성**: `createFlowDeskGitHubDryRunPublicationResultV1`로 파티션 기반 synthetic dry-run result 생성; `attemptId` 제공 시 재사용, 없으면 신규 random 생성. (5) **권한 플래그**: `advisoryOnlyRecord: true`, `remoteWriteAuthorityEnabledInRecord: false`, `dispatchAuthorityEnabled: false`, `laneLaunchAuthorityEnabled: false` — 모든 경로에서 항상 false. (6) **인터페이스**: `ScoreLedgerGitHubPublishInputV1` (`partition`, `partitionJsonl`, `target`, `allowActualRemoteWrite?`, `surplusUsageGateRef?`, `minimizationPolicyRef?`, `guardApprovalRef?`, `attemptId?`, `durableStateRoot?`, `fetchImpl?`) / `ScoreLedgerGitHubPublishResultV1` (`status: "published" | "blocked" | "dry_run_recorded"`, `blocked_labels?`, `partitionId`, `partitionState`, `authority`). (7) **테스트** `score-ledger-github-publisher.test.ts` 신규 — 10개 테스트: raw 파티션 → `blocked: partition-not-sealed`, sealed + `allowActualRemoteWrite=false` → `dry_run_recorded`, immutable + `allowActualRemoteWrite=false` → `dry_run_recorded`, sealed + `allowActualRemoteWrite=true` + mock fetch → published/blocked 구조 검증, globalThis.fetch 교체로 실제 delegation 경로 검증, dry_run 경로 권한 플래그 모두 false, blocked 경로 권한 플래그 모두 false, compaction lock 보유 시 blocked(`compaction-lock-held`), JSONL에 forbidden marker 포함 시 blocked(`partition-jsonl-contains-forbidden-marker`), immutable + `allowActualRemoteWrite=true` 경로 권한 플래그 검증. Verification: `npm run typecheck` — 새 파일 0 errors (pre-existing `operational-intelligence.test.ts` 오류는 사전존재); `node --test dist/score-ledger-github-publisher.test.js` **10/10 pass, 0 fail**; 전체 functional 테스트 suite **383/385 pass** (2개 실패는 `routing-advisory-pipeline.test.js` filterValidLedgerEntries 사전존재 오류, 신규 리그레션 없음).

- **P7-S19: score-ledger-partition-lifecycle.ts — raw→sealed→immutable 상태 전환** (2026-06-11): `packages/opencode-plugin/src/score-ledger-partition-lifecycle.ts` 신규 작성. (1) **상태 전환 트리거**: `raw→sealed`: `age_seconds > sealing_window_seconds` AND `entry_count >= min_seal_threshold`; `sealed→immutable`: `sealed_at + immutable_grace_period_seconds` 경과. (2) **`checkAndAdvancePartitionStateV1(input)`** — `<rootDir>/.flowdesk/oi/score-ledger/<partitionId>.manifest.json` 로드 + `validateFlowDeskScoreLedgerManifestV1` 검증; `immutable` → `already_immutable`(no-op); `raw` 체크: 나이+카운트 조건 충족 시 `sealed_at` 설정 → manifest atomic write(tmp→renameSync); `sealed` 체크: grace period 경과 시 `immutable_at` 설정 → manifest atomic write; 조건 미충족 시 `conditions_not_met` reason 상세 설명 포함; manifest 없음 → `manifest_not_found`; write 실패 → `write_failed`(state 롤백). (3) **`advanceAllPartitionsV1(input)`** — `readdirSync` 기반 `*.manifest.json` 글로브; 각 파일에 `checkAndAdvancePartitionStateV1` 호출; 존재하지 않는 디렉토리 → 빈 배열; `.manifest.json`이 아닌 파일 무시. (4) **테스트** `score-ledger-partition-lifecycle.test.ts` 신규 — 12개 테스트: `raw→sealed`(age+count 충족), `raw 유지`(age 충족 but count 미달), `raw 유지`(count 충족 but age 미달), `sealed→immutable`(grace period 경과), `sealed 유지`(grace period 미경과), `immutable 변경 없음`(already_immutable), `manifest_not_found`(파일 없음), `raw 유지`(age+count 모두 미달), `sealed_at_missing` 처리, `advanceAll: 빈 디렉토리`, `advanceAll: 3개 파티션 일괄 전환`, `advanceAll: .manifest.json 아닌 파일 무시`. Verification: `npm run typecheck` 클린(0 errors); `npm test` **372/372 pass, 0 fail** (기존 360 + 신규 12, 리그레션 없음).

- **P7-S05: routing-advisory-pipeline.ts — threshold gate → routing advisory 적재 파이프라인** (2026-06-11): `packages/opencode-plugin/src/routing-advisory-pipeline.ts` 신규 구현 + `packages/opencode-plugin/src/workflow-assign-tool.ts` additive 연결. (1) **`routing-advisory-pipeline.ts`** — `RoutingAdvisoryPipelineInputV1` / `RoutingAdvisoryPipelineResultV1` 인터페이스 정의. `evaluateAndRecordRoutingAdvisoryV1(input)` 구현: Step 1. `rootDir` 빈 문자열/공백 → `skipped_no_rootdir`; Step 2. `currentScore` undefined/NaN → `skipped_no_score`; Step 3. `loadRoutingAdvisoryLedgerV1(rootDir)`로 기존 레저 로드 (fail-open); Step 4. `signatureRef`로 필터링 후 최신 항목 식별, `minSampleCount` pre-check (부족 시 즉시 `recompute`); Step 5. `createFlowDeskScoreReuseThresholdGateV1` 호출 — `gate_decision="reuse"` → `reuse_advisory` 반환(append 없음); Step 6. `recompute` → `appendRoutingAdvisoryEntryV1`로 새 entry 기록 → `new_advisory_appended`. `currentScore`(0..100) → `weighted_score`(0..1) 변환. `context_hash` 필드가 있는 경우 해당 값으로 context_match 평가, 없으면 `hash-previous-unknown`(→ 항상 mismatch). 모든 경로에서 `advisory_only: true`, `dispatch_authority_enabled: false` 리터럴 상수 보장. (2) **`workflow-assign-tool.ts` additive 연결** — `evaluateAndRecordRoutingAdvisoryV1` import 추가. `oiAdvisory` 타입에 optional `pipelineStatus?: string`, `pipelineGateDecision?: string` 추가. 기존 `buildOIAssignmentAdvisoryV1` 호출 이후 `evaluateAndRecordRoutingAdvisoryV1({workflowId, taskId, rootDir, currentScore: oiAdvisory.advisoryScore})` 호출 — 결과를 oiAdvisories에 additive 포함. 실제 모델 선택 로직에 영향 없음(advisory-only). (3) **`routing-advisory-pipeline.test.ts` 신규** — 9개 테스트: `skipped_no_rootdir`(빈 문자열/공백 각각), `skipped_no_score`(undefined/NaN 각각), 기존 entry 없음 시 `new_advisory_appended` + ledger 기록 검증, gate reuse 시 `reuse_advisory` + append 미호출, context hash 불일치 시 `new_advisory_appended`, 모든 status에서 authority flags false, minSampleCount 미달 시 recompute, 나이 임계값 초과 시 recompute, `ledgerEntryCount` 상태 반영. Verification: `npm run typecheck` 클린(0 errors); `npm test` **350/350 pass, 0 fail** (기존 341 + 신규 9, 리그레션 없음).

- **P7-S18: score-ledger-partition-writer.ts 구현 — raw partition append + hash chain 누적** (2026-06-11): `packages/opencode-plugin/src/score-ledger-partition-writer.ts` 신규 작성. (1) **인터페이스**: `ScoreLedgerPartitionWriterInputV1` (`rootDir`, `entry`, optional `partitionId`, optional `schemaHash`) / `ScoreLedgerPartitionWriterResultV1` (`status: "appended" | "partition_created" | "blocked"`, `partitionId`, `entryCount`, `chainHead`, `genesisHash`, optional `reason`). (2) **디스크 구조**: `<rootDir>/oi/score-ledger/<partitionId>.jsonl` (raw entry lines) + `<partitionId>.manifest.json` (`FlowDeskScoreLedgerPartitionV1` 메타데이터). (3) **플로우**: rootDir 없음 → `blocked`; manifest 존재하면 로드, 없으면 신규(state: "raw") 생성; manifest.state !== "raw" → `blocked`; `computeEventHashV1(entry)` 계산; `computeChainHashV1({ eventHash, previousHash: chain_head_hash })` 계산; `.jsonl`에 원자적 append (기존 내용 읽기 → 신규 라인 추가 → tmp→rename 원자 교체 패턴, S04 미러); manifest 갱신 (`entry_count++`, `chain_head_hash` 업데이트) + `createFlowDeskScoreLedgerPartitionV1` 경유 유효성 검증 + 원자적 manifest write. (4) **partitionId 자동 생성**: 미제공 시 UTC 날짜 기반 `"partition-YYYY-MM-DD"`. (5) **권한 플래그**: `advisory_only: true`, `non_authorizing: true`, `dispatch_authority_enabled: false`, `remote_write_authority_enabled: false` — manifest에 그대로 기록. (6) **테스트** `score-ledger-partition-writer.test.ts` 신규 — 10개 테스트: 첫 entry → `partition_created` entryCount=1 (파일+manifest 검증), 두 번째 entry → `appended` entryCount=2, chain_head_hash가 `computeChainHashV1` 결과와 일치, 3 entry 누적 체인 정확성, sealed manifest → `blocked`, immutable manifest → `blocked`, rootDir 없음 → `blocked`, 빈 rootDir → `blocked`, partitionId 자동 생성(date 패턴 검증), manifest 권한 플래그 검증. Verification: `npm run typecheck` 클린 (0 errors); `npm test` **360/360 pass, 0 fail** (기존 350 + 신규 10, 리그레션 없음).

- **P7-S07: multi-model fanout executor 배선 + oi-fanout-coordinator 신규** (2026-06-11): Two deliverables in this session. (1) **`packages/core/src/operational-intelligence/multi-model-fanout-executor.ts` 시그니처 확장 (additive)** — `ExecuteMultiModelFanoutTestV1Input`에 optional `cadenceDecision?: FlowDeskFanoutCadenceDecisionV1` 추가. 입력 타입 import에 `FlowDeskFanoutCadenceDecisionV1` from `./multi-model-fanout-cadence.js` 추가. 내부 "Step 1b"로 cadence gate 체크 삽입: `cadenceDecision` 존재 and `cadence_decision !== "allow"` 시 모든 모델을 `eligibility_status: "skipped", skip_reason: cadenceDecision.cadence_decision`으로 마크하고 `execution_status: "failed", reservation_consumed: false` 엔벨로프 반환. `cadenceDecision` 없으면 기존 동작 그대로 유지(additive only). (2) **`packages/opencode-plugin/src/oi-fanout-coordinator.ts` 신규 작성** — `OIFanoutCoordinatorInputV1` / `OIFanoutCoordinatorResultV1` 인터페이스 정의. `runOIFanoutCoordinatorV1(input)` 구현: Step 1. `evaluateFanoutCadenceDecisionV1` 호출; Step 2. `cadence_decision !== "allow"` → `blocked_cadence` 즉시 반환; Step 3. `selectedModels.length < 2` → `blocked_no_data` 반환; Step 4. `executeMultiModelFanoutTestV1` 호출 (cadenceDecision 전달); Step 5. 엔벨로프 status 기반 `advisory_completed`/`blocked_no_data` 반환. `OIFanoutCoordinatorResultV1` 권한 플래그 모두 리터럴 false: `advisory_only: true, dispatch_authority_enabled: false, provider_call_authority_enabled: false, real_opencode_dispatch: false`. (3) **`packages/opencode-plugin/src/oi-fanout-coordinator.test.ts` 신규** — 6개 테스트: `cadence allow → advisory_completed`(fanout 점수 계산, best_model_ref 확인), `blocked_cooldown → blocked_cadence`(fanoutResult absent, cooldown_remaining_seconds 확인), `advisory_completed 경로 권한 플래그 모두 false`, `blocked_cadence 경로 provider_call_authority_enabled false`, `모델 0/1개 → blocked_no_data`, `blocked_window_cap → blocked_cadence` (올바른 decision label). Verification: `npm run typecheck` 클린 (my new files 0 errors; pre-existing operational-intelligence.test.ts unterminated string literal은 사전존재 오류); `npm test --workspace packages/opencode-plugin` **339/339 pass, 0 fail** (기존 333 + 신규 6).

- **P7-S20: ledger-rollup.ts 구현 — sealed partition 집계 함수** (2026-06-11): `packages/core/src/operational-intelligence/ledger-rollup.ts` 신규 작성. (1) **핵심 제약 준수**: sealed/immutable partition만 허용 (raw 입력 시 즉시 `blocked_raw_partition`); cumulative sum 완전 금지, weighted mean만 사용; 모든 결과 `advisory_only: true, dispatch_authority_enabled: false, ranking_authority_enabled: false`. (2) **Named exports (테스트 직접 검증용)**: `computeWeightedMeanV1()` — per-entry decay (exponential, 2^(-age/halfLife)) 적용 가중 평균; `computePercentileV1()` — 선형 보간 (type 7, R notation); `computeEffectiveSampleSizeV1()` — Kish approximation ESS = (Σw)²/Σ(w²); `computeDecayAdjustedMeanV1()` — half-life decay 가중 평균. (3) **타입 정의**: `FlowDeskLedgerRollupResultV1` (schema_version, source_partition_ids, sample_count, effective_sample_count, weighted_mean_score, confidence_bucket, p25/p50/p75/p90, decay_adjusted_mean, last_observation_at, scorer_concentration, negative_signal_count, rollup_hash); `FlowDeskLedgerRollupBlockedV1` union (`blocked_raw_partition | insufficient_data`). (4) **computeLedgerRollupV1()** 메인 함수: 최소 3개 entries 요구 (미달 시 `insufficient_data`); `confidence_bucket` — `very_high` ≥50 ESS, `high` ≥20, `medium` ≥5, `low` 나머지; scorer_concentration = Herfindahl-Hirschman Index (Σshare², 0..1); negative_signal_count = weighted_score < 0.40 entries 수; rollup_hash = sha256 of canonical JSON. (5) **부동소수점 보호**: `CONFIDENCE_EPSILON = 1e-6`으로 경계값 ESS(4.9999...→5.0 인식) 처리. (6) **index.ts barrel export**: 5개 함수 + 3개 타입 추가. (7) **테스트** `ledger-rollup.test.ts`: 13개 테스트 — sealed 정상 rollup, immutable 정상 rollup, raw 차단(`blocked_raw_partition`), 부족 entry 0/1/2개(`insufficient_data`), weighted_mean ≠ cumulative sum(decay로 차이 증명), confidence_bucket 경계값(3/4→low, 5/20/50/100→medium/high/very_high), HHI 계산 정확도(1.0/0.5/0.25), computeWeightedMeanV1 단위 테스트, computePercentileV1 p0/p50/p100/단일/빈배열, computeEffectiveSampleSizeV1 동일/단일/불균등/빈배열, negative_signal_count 경계(40 기준), computeDecayAdjustedMeanV1 단위 테스트. Verification: `npm run typecheck` 클린 (0 errors); `node --test dist/operational-intelligence/ledger-rollup.test.js` **13/13 pass, 0 fail**.

- **P7-S17: score-ledger-hash-chain.ts 구현 — genesis/event/previous hash chain 계산기** (2026-06-11): `packages/core/src/operational-intelligence/score-ledger-hash-chain.ts` 신규 작성 (순수 함수, no I/O, no side effects). (1) `computePartitionGenesisHashV1(input)` — `sha256(schemaVersion + partitionId + schemaHash)`, 기본 prefix `"flowdesk.partition.genesis.v1"`, 64자 hex 반환. (2) `computeEventHashV1(entry)` — `canonicalJCSHash(entry)` 래퍼 (RFC 8785 JCS 정규화 후 SHA-256). (3) `computeChainHashV1(input)` — `sha256(eventHash + previousHash)` 문자열 연결 후 해시. (4) `buildPartitionHashChainV1(input)` — entries 배열에서 전체 체인 빌드, 빈 배열 → 빈 배열, 각 위치 `{ eventHash, chainHash }` 반환. (5) `verifyPartitionHashChainV1(input)` — 체인 무결성 검증, `{ valid: true }` 또는 `{ valid: false; failedAtIndex: number; reason: string }` 반환; 빈 entries+chain → valid, 길이 불일치 → `failedAtIndex: 0`. (6) `packages/core/src/operational-intelligence/index.ts` barrel export 추가 (5개 함수). 테스트 `packages/core/src/operational-intelligence/score-ledger-hash-chain.test.ts` 신규 — 16개 테스트: genesis hash 결정성/hex형식/정확한 공식/커스텀schemaVersion/partitionId 차이, event hash JCS동일성/결정성/키순서무관, chain hash 결정성/정확한공식/순서의존성, buildPartitionHashChainV1 3개엔트리/빈배열/단건, verifyPartitionHashChainV1 유효체인/중간변조(failedAtIndex:1)/빈체인/길이불일치/chainHash변조/순서변경. Verification: `npm run typecheck` 클린(0 errors); `npm test` 909/910 pass — 1개 실패는 사전존재 `ledger-rollup.test.ts` 리그레션(`confidence_bucket` 임계값 오류), P7-S17 신규 16개 전부 통과.

- **P7-S02: 9개 scoring dimension 실계산 함수 추가** (2026-06-11): `packages/core/src/operational-intelligence/scoring-engine.ts`에서 placeholder 값(65/70/80)을 `blockScoring` 데이터 기반 실계산으로 교체. (1) 9개 private 함수 추가: `computeGoalFitScore(scope)` — scope=5 최적, ±8/step 패널티; `computeTaxonomyFitScore(category)` — 카테고리별 lookup (`schema_only:85, implementation:80, integration:75, orchestration:70, security_boundary:70, design:60`); `computeVerificationCoverageScore(authoritySensitivity)` — `100 - as*5`; `computeRiskScore(novelty, coupling, authoritySensitivity)` — `100 - (n*5 + c*3 + as*2)/2`; `computeDependencyImpactScore(coupling)` — `100 - c*8`; `computeModelDiversityScore(count)` — `count*25 + 25` (capped 100); `computeSafetyScore(authoritySensitivity)` — `100 - as*8`; `computeSimplicityFitScore(complexity, scope, variantId)` — `100 - c*5 - (s-1)*3 ± 10 variant boost`; `computeDetailFitScore(complexity, scope, variantId)` — `50 + c*3 + (s-1)*2 ± 10 variant boost`. 모든 함수 `Math.max(0, Math.min(100, Math.round(...)))` clamp 적용. (2) `scoreWorkflowProposal()` 내부 dimension 계산 교체: `blockScoring` 존재 시 9개 실계산 함수 호출, 부재 시 기존 placeholder 값 유지 + `reason_ref`에 `-no-block-scoring` suffix 추가. (3) `computeConfidenceScore()` 확장: base (`available >= 7 → 80, >= 3 → 60, else 40`) + `blockScoring` 존재 시 +2 보너스 (`Math.min(100, ...)`). (4) 기존 placeholder 로직이 사용하던 불필요한 `blockScoringValidationFailed` 지역 변수 제거. (5) 영향받는 테스트 1건 업데이트: `blockScoring field is accepted when valid and confidence increases` — `confidenceDim.score 60→62`, `advisory_score 70→77` (실계산 반영). Verification: `npm run typecheck` 클린(0 errors); `npm test` **877/877 pass, 0 fail** (리그레션 없음).

- **P7-S16: RFC 8785 JCS Canonicalizer 구현** (2026-06-11): `packages/core/src/shared/jcs.ts` 신규 작성. RFC 8785 I-Serialization 핵심 부분집합 구현: (1) `canonicalizeJCS(value: unknown): string` — 재귀 직렬화기. Object key는 JS 기본 문자열 비교(UTF-16 code-unit 순서, RFC 8785 §3.2.3)로 정렬. 숫자는 `Number.prototype.toString()` 사용(정수/float 최단 roundtrip, `1e20 → "100000000000000000000"`). 문자열은 `"`, `\`, U+0000-U+001F만 `\uXXXX` escape(RFC 8785 §3.2.2), 나머지 verbatim. Array는 순서 유지. `undefined`/function/symbol/BigInt/non-finite number/UTF-16 surrogate 포함 문자열은 `TypeError` throw. (2) `canonicalJCSHash(value: unknown): string` — `node:crypto` SHA-256, 64자 소문자 hex 반환. (3) `packages/core/src/index.ts`에 `export * from "./shared/jcs.js"` 추가. 테스트 파일 `packages/core/src/shared/jcs.test.ts` 신규 — 35개 테스트: null/boolean/integer/float 직렬화, `1e20` 최단 표현, 문자열 이스케이프(U+0000/backslash/quote/control chars/space), UTF-16 정렬(ASCII, €/U+20AC, 대소문자), 배열 순서 유지, 중첩 객체, 에러 케이스 7개(undefined/function/symbol/BigInt/NaN/Infinity/surrogate), canonicalJCSHash 4개(64자 hex, 결정적, key순서 무관, 다른 값 다른 hash), RFC 8785 Appendix B 복합 정렬 fixture. Verification: `npm run typecheck` 클린(0 errors); `npm test` **877/877 pass, 0 fail**(기존 842 + 신규 35).

- **P7-S04: oi-ledger-writer.ts 구현 — atomic append + compaction trigger** (2026-06-11): `packages/opencode-plugin/src/oi-ledger-writer.ts` 신규 구현. (1) `appendRoutingAdvisoryEntryV1(input: OILedgerAppendInputV1): OILedgerAppendResultV1` — `.flowdesk/oi/routing-advisory.jsonl`에 `FlowDeskRoutingAdvisoryLedgerEntryV1` 단건을 원자적으로 추가. (2) O_EXCL 락(`routing-advisory.jsonl.lock`): `openSync('wx')` 방식으로 시도, 락 획득 실패 시 `blocked` 반환(throw 없음, fail-open). (3) 원자적 쓰기: 기존 JSONL 파일 읽기 → `decodeFlowDeskRoutingAdvisoryLedgerJsonlV1` 디코드 → 신규 entry 추가 → `.tmp-<random>` 파일 기록 → `renameSync` POSIX 원자 교체 → 락 해제(finally). (4) Dedup: `(signature_ref, model_ref, recorded_at)` 복합 키 — 이미 존재 시 `skipped_dedup` 반환. (5) Compaction trigger: entry 수 > 1000 OR 파일 크기 > 2MB → `compactFlowDeskAdvisoryLedgerV1` 호출 후 반환 엔트리로 원자 교체; 압축 실패 시 비압축 엔트리로 계속 진행(fail-open). (6) rootDir 없음/비존재 → `blocked`. 테스트 파일 `packages/opencode-plugin/src/oi-ledger-writer.test.ts` 신규 — 9개 테스트: 단건 추가 성공, 복수 distinct 추가, dedup 스킵, 동일 sig+model 다른 recorded_at 허용, 1001개 시 compaction 트리거, 락 충돌 fail-open, rootDir 없음 blocked, 빈 rootDir blocked, 2MB 초과 시 compaction 트리거. ESM 환경에서 `require()` 대신 static `import { unlinkSync }` 사용. Verification: `npm run typecheck` 클린(0 errors); `npm test` **333/333 pass, 0 fail** (기존 324 + 신규 9).



- **P7-S06: FlowDeskFanoutCadenceDecisionV1 — cadence control contract** (2026-06-11): Implemented `packages/core/src/operational-intelligence/multi-model-fanout-cadence.ts` (new file, additive). (1) Defined `FlowDeskFanoutCadenceGateInputV1` input type mirroring `createFlowDeskFanoutCadenceGateV1` parameters plus optional `noveltyScore`, `noveltyThreshold`, `estimatedCostUnits`, `budgetCapUnits`. (2) Defined `FlowDeskFanoutCadenceDecisionOutcomeV1` union type (`allow | blocked_cooldown | blocked_window_cap | blocked_low_novelty | blocked_budget`) — named `…OutcomeV1` (not `…LabelV1`) to avoid collision with the identically-named gate-internal type already exported from `gates.ts`. (3) Defined `FlowDeskFanoutCadenceDecisionV1` interface wrapping the gate result with rolling-window, cooldown, novelty, and budget detail fields; authority flags `advisory_only: true`, `dispatch_authority_enabled: false`, `provider_call_authority_enabled: false` are permanently false. (4) Implemented `evaluateFanoutCadenceDecisionV1({ gateInput, now? })` — calls `createFlowDeskFanoutCadenceGateV1` internally; on validation failure returns `blocked_window_cap` (fail-closed); precedence order: cooldown → window_cap → low_novelty → budget → allow. (5) Re-exported all three public symbols from `packages/core/src/operational-intelligence/index.ts`. (6) Added `packages/core/src/multi-model-fanout-cadence.test.ts` with 17 tests: allow baseline, blocked_cooldown (with `cooldown_remaining_seconds` value assertion), blocked_window_cap, blocked_low_novelty, blocked_budget, authority flags on all 5 decisions (5 tests), gate validation error fallback, novelty+budget pass-through on allow, cooldown_remaining absent on allow, schema_version constant across all variants, precedence tests (cooldown > window_cap, window_cap > low_novelty), rolling-window field reflection. Verification: `npm run typecheck` clean (0 errors); `node --test dist/multi-model-fanout-cadence.test.js` → 17/17 pass, 0 fail. gates.ts NOT modified (additive principle enforced).



- **P7-S01: FlowDeskScoringEngineInputV1 확장 — blockScoring / variantId / proposalSetContext** (2026-06-11): Implemented additive optional field extensions to the scoring engine input interface in `packages/core/src/operational-intelligence/scoring-engine.ts`. Changes: (1) Added three new optional fields to `FlowDeskScoringEngineInputV1`: `blockScoring?: FlowDeskTaskBlockScoringV1` (for judgment 1 real calculation), `variantId?: "simple" | "standard" | "detailed" | "high_assurance"` (variant for detail_fit/simplicity_fit weighting), and `proposalSetContext?: { distinctProviderFamilyCount?: number }` (for model_diversity calculation). (2) Updated `countAvailableInputs()` helper to count the three new optional fields, enabling confidence scoring adjustment when blockScoring is provided. (3) Added validation logic in `scoreWorkflowProposal()`: `blockScoring` is validated via `validateFlowDeskTaskBlockScoringV1()`, and invalid blockScoring does not crash (errors pushed to errors array); `variantId` is validated against allowed values; `proposalSetContext.distinctProviderFamilyCount` is validated as non-negative integer. (4) Implemented variant-based weighting for detail_fit and simplicity_fit dimensions: `detailed` / `high_assurance` → detail_fit weight 1.5, simplicity_fit weight 0.7; `simple` → detail_fit weight 0.7, simplicity_fit weight 1.5; `standard` (default) → both weight 1.0. (5) Implemented model_diversity score calculation from provider family count: 1 family → 50, 2+ families → 85-95 (scaled). (6) Updated dimension score aggregation to use weighted average: `baseAdvisoryScore = sum(score * weight) / sum(weight)` instead of unweighted. All existing callers are unaffected (all new fields are optional, preserved behavior when absent). (7) Added 4 new unit tests in `packages/core/src/operational-intelligence.test.ts`: ① blockScoring absent (baseline score 68), ② blockScoring present (confidence increases to 60, advisory score 70), ③ variantId weight effects (detailed > simple due to detail_fit weighting), ④ proposalSetContext distinctProviderFamilyCount effects (higher diversity score for multi-provider). Verification: `npm run typecheck` clean (pre-existing unrelated type errors in index.ts untouched); test runs show 4 new tests pass (plus 1173 existing tests in core package). No regressions in existing behavior.

## Current Canonical State (2026-06-11)

- **P7-S15 Score Ledger Partition Lifecycle Contract Implementation** (2026-06-11): Implemented `packages/core/src/operational-intelligence/score-ledger-partition.ts` with complete partition, manifest, and trusted chain head schemas per specification. (1) **Types**: `FlowDeskScoreLedgerPartitionStateV1` (union of "raw" | "sealed" | "immutable"), `FlowDeskScoreLedgerPartitionV1` (partition metadata with state transitions, hashes, sealing windows, grace periods, advisory-only authority flags), `FlowDeskScoreLedgerManifestV1` (manifest container for partition array + optional trusted chain head), `FlowDeskTrustedChainHeadV1` (immutable anchor record). (2) **Validators**: `validateFlowDeskScoreLedgerPartitionV1()` enforces state-to-timestamp binding (sealed→sealed_at, immutable→immutable_at required), authority flags strictly false, positive integer thresholds/windows, and valid hash refs. `validateFlowDeskScoreLedgerManifestV1()` validates partition array and chain head. `validateFlowDeskTrustedChainHeadV1()` validates chain head record. (3) **Creators**: `createFlowDeskScoreLedgerPartitionV1()` with state-transition pre-validation; `createFlowDeskScoreLedgerManifestV1()` builds manifest from partitions; `createFlowDeskTrustedChainHeadV1()` builds immutable anchor. All creators return `{ ok: boolean; errors: string[]; <entity>: T | undefined }` result union. (4) **Exports**: Added all 12 symbols to `packages/core/src/operational-intelligence/index.ts` barrel export (types, creators, validators). (5) **Tests**: 10 comprehensive tests covering valid partition in raw state, sealed/immutable state timestamp binding, authority flag enforcement, creator success paths, creator validation failures, manifest validation with invalid partitions, and trusted chain head creation. (6) **Bug Fix**: Removed duplicate `FlowDeskFanoutCadenceDecisionLabelV1` export from gates.js in index.ts (pre-existing duplicate with multi-model-fanout-cadence.js). Verification: `npm run typecheck` clean; **npm test 1177/1177 pass, 0 fail** (includes all 10 new P7-S15 tests).

## Current Canonical State (2026-06-10)

- **Phase 8 완성 (8a–8f 전 슬라이스 통합 검증 완료)** (2026-06-10): `npm run typecheck` clean, `npm test` **1137/1137 pass, 0 fail**. 슬라이스별 요약: 8a(Burn-Rate Gate — surplusUsageGate AND-gate), 8b(Compaction Script + R-NEW-1 lock 경로 통합), 8c(productionPublish 9개 AND-gate + Section 2.6 conformance 테스트 14개), 8d(Doctor Surface github_connector 섹션), 8e(fetchModelAvailabilityDbFromGitHubV1 + --publish 플래그), 8f(provider-catalog.json 번들 + loadProviderCatalog + fetchProviderCatalogFromGitHubV1 + managed-dispatch-adapter switch 제거). AUDIT_GITHUB_CONNECTOR_PHASE8.md BLOCKER 항목 전부 처리 완료.

- **Phase 8e — model-availability DB GitHub 동기화** (2026-06-10): `fetchModelAvailabilityDbFromGitHubV1()` 구현 및 `--publish` 플래그 추가. (1) `packages/opencode-plugin/src/federated-registry-connector.ts` — `FetchModelAvailabilityDbInputV1` / `FetchModelAvailabilityDbResultV1` 인터페이스 정의 및 `fetchModelAvailabilityDbFromGitHubV1()` 함수 구현 (`export`). 핵심 동작: `allowActualRemoteRead !== true` 시 `blocked` (gate 패턴 `allowActualRemoteWrite`와 대칭); `assetUrl`/`expectedSha256`/`targetPath` 누락 시 `blocked`; 네트워크 에러/HTTP non-2xx 시 `fetch_failed`; SHA-256 불일치 시 `sha256_mismatch`(실제 digest 반환); `observed_at` 기반 다운그레이드 방지(`skipped_downgrade`); 원자적 tmpfile → `renameSync` 교체; production 컨텍스트에서 `globalThis.fetch` 강제(Phase 8c 패턴 미러링); raw token/credential 결과에 미포함. `readSnapshotObservedAtFromDbAsync()` — `node:sqlite`를 `await import("node:sqlite")` ESM 동적 임포트로 열어 임시 파일의 snapshot.observed_at 읽기; Bun 런타임에서는 `require("bun:sqlite")` fallback. (2) `scripts/export-model-availability-db.mjs` — `--publish` / `--dry-run` CLI 플래그 추가. `--dry-run`: DB sha256만 출력, 업로드 없음. `--publish`: `GITHUB_TOKEN`/`FLOWDESK_PUBLISH_REPO`/`FLOWDESK_PUBLISH_TAG` env var 검증 후 GitHub Releases API로 `model-availability.db` asset 업로드. Phase 8c `productionPublish` gate 패턴 동일하게 적용 (GITHUB_TOKEN 없으면 exit(1)). (3) `packages/opencode-plugin/src/server.ts` — `fetchModelAvailabilityDbFromGitHubV1` import 추가 + `flowdesk_model_availability_refresh` MCP 툴 등록 위치 TODO 주석 삽입. (4) `packages/opencode-plugin/src/federated-registry-connector.test.ts` — 9개 새 테스트: `allowActualRemoteRead` absent/false 블록, `assetUrl` 누락 블록, `expectedSha256` 잘못됨 블록, 네트워크 에러 `fetch_failed`, HTTP 404 `fetch_failed`, sha256 불일치 `sha256_mismatch`, `skipped_downgrade`(fetched `observed_at` < current), 성공 `fetched`+원자적 파일 쓰기. `buildMinimalModelAvailabilityDb()` 헬퍼: `PRAGMA journal_mode=DELETE` + `wal_checkpoint(TRUNCATE)` 으로 WAL 사이드카 없는 자기 완결적 DB 생성. Verification: `npm run typecheck` 클린; `npm test` 1137/1137 통과(0 실패), 기존 1115 + 신규 9 + 기타 증가.

- **Phase 8f — provider-catalog.json GitHub sync + model-selection-engine refactor** (2026-06-10): (1) Created `packages/opencode-plugin/data/provider-catalog.json` — new bundle file with `schema_version: "flowdesk.provider_catalog.v1"`, `families[]` (claude/openai/gemini entries with `opencode_provider_id`, `prefixes`, `stage_keywords`, `fallback_chains`, `deprecated_model_ids`, `agent_name`), `supported_model_ids[]`, `tiers{}` (heavy/medium/light arrays), and `roles{}` map. All values extracted from the hardcoded constants in `model-selection-engine.ts`. (2) `packages/opencode-plugin/src/model-selection-engine.ts` — added `ProviderCatalogFamilyEntryV1`, `ProviderCatalogTierModelEntryV1`, `ProviderCatalogRoleEntryV1`, `ProviderCatalogV1` interfaces; implemented `validateProviderCatalogV1()` with security validation (ALLOWED_OPENCODE_PROVIDER_IDS whitelist, AGENT_NAME_RE path-traversal prevention, PROVIDER_QUALIFIED_MODEL_ID_RE format enforcement on fallback_chains); implemented `loadProviderCatalog(rootDir?)` with custom-path → bundle-fallback resolution order, downgrade prevention via `updated_at` comparison, and singleton caching; implemented `opencodeProviderIdFromCatalog()` as catalog-based lookup for all provider family name/prefix mappings. (3) `packages/opencode-plugin/src/managed-dispatch-adapter.ts` — imported `opencodeProviderIdFromCatalog` from model-selection-engine.js; replaced the hardcoded `switch` in `opencodeRuntimeProviderIDForFlowDeskProviderFamily()` with a single `return opencodeProviderIdFromCatalog(providerFamily)` call. (4) `packages/opencode-plugin/src/federated-registry-connector.ts` — added `fetchProviderCatalogFromGitHubV1()` following the Phase 8e `fetchModelAvailabilityDbFromGitHubV1` pattern: `allowActualRemoteRead` gate, SHA-256 checksum verification, FORBIDDEN_RAW_PAYLOAD_MARKERS scan, structural/security validation via `validateProviderCatalogV1`, `updated_at`-based downgrade prevention, atomic tmpfile→rename write. (5) Added 9 new tests: `loadProviderCatalog loads and validates the bundle catalog successfully`, `validateProviderCatalogV1 accepts a valid catalog and rejects invalid ones`, `opencodeProviderIdFromCatalog maps family names and legacy prefixes correctly` (model-selection-engine.test.ts); `fetchProviderCatalogFromGitHubV1 blocks when allowActualRemoteRead is not true`, `blocks on invalid input`, `fetches, validates, and writes a catalog atomically`, `prevents downgrade when fetched catalog is older`, `returns sha256_mismatch when downloaded content differs`, `returns validation_failed for structurally invalid catalog JSON` (federated-registry-connector.test.ts). Verification: `npm run typecheck` clean; `npm test` 1137 total, 1135 pass — the 2 failures (`Section 8e skipped_downgrade` and `fetches and atomically writes DB on success`) are pre-existing environmental SQLite test-ordering issues (both pass in isolation with `node --test --test-name-pattern`); the 1 pre-existing `test_doctor_reports_gate_freshness_stale_surface_safe_next_actions` failure unchanged. Zero regressions from Phase 8f changes.

- **Phase 8d — Doctor Surface: `github_connector` section** (2026-06-10): Wired the Phase 8 GitHub connector state into the `/flowdesk-doctor` output surface. (1) `packages/opencode-plugin/src/command-handlers.ts` already contained `githubConnectorSection()` (lines 400–422) and the `githubConnector` field in `FlowDeskCommandBackedHandlerContextV1`; no change needed there. (2) `packages/opencode-plugin/src/local-adapter.ts` — added optional `githubConnector` field to `FlowDeskLocalNonDispatchAdapterSessionOptionsV1` and `LocalAdapterState` (type mirrors the context interface; raw tokens are never stored), propagated through `createFlowDeskLocalNonDispatchAdapterSession` state initialization, and wired into the `contextFor` diagnostic object as `githubConnector: state.githubConnector`. (3) Added 4 named tests to `packages/opencode-plugin/src/command-handlers.test.ts`: `test_doctor_reports_production_publish_flag_state` (enabled/disabled/unknown path coverage with `effectively_blocked` ref checks), `test_doctor_reports_github_token_available_redacted` (token boolean availability in refs; asserts raw `GITHUB_TOKEN` and `ghp_` patterns never appear in response JSON), `test_doctor_reports_compaction_evidence_summary` (all 7 `evidence_compaction_*` refs surfaced via `compactionHealth` context), and `test_doctor_reports_gate_freshness_stale_surface_safe_next_actions` (stale surplus/minimization gates yield `degraded_mode_warning` + non-empty `safe_next_actions`; fresh gates yield `informational` without `/flowdesk-usage` remediation). Verification: `npm run typecheck` clean; `npm test` 1119/1119 pass, 0 fail (including all 4 new tests).



- **Phase 8c — productionPublish Flag AND-Gate wiring** (2026-06-10): Locked in 9 AND-gate conditions inside `packages/opencode-plugin/src/federated-registry-connector.ts::publishToGitHubV1`. (1) Registered the existing `FlowDeskGitHubConnectorProductionPublishFlagV1` schema in `packages/core/src/schema-registry.ts` and `packages/core/src/schema-artifacts.ts` (additive `flowdesk.github_connector_production_publish_flag.v1` entry with required fields `schema_version`, `state`, `created_at`, advisory/non-authorizing/false-authority literals, and optional `consumes_*_ref` fields). (2) Added two new AND-gate helpers: `guardApprovalBindingBlockLabelsV1` (emits `guard-approval-ref-missing` when `guardApprovalRef` is empty and `guard-approval-attempt-mismatch` when the resolved productionPublish flag's `consumes_guard_approval_ref` does not match `guardApprovalRef` or when supplied `attemptId` does not match `dryRunResult.attempt_id`) and `ledgerIdempotencyBindingBlockLabelsV1` (emits `ledger-idempotency-ref-missing` when empty). Labels are merged into `preliminaryBlockLabels` via the existing dedup-preserving Set; existing 8a/8b labels and `allowActualRemoteWrite`/`connectorGateSatisfied`/contentMarkdown/productionPublish/compaction-lock conditions are unchanged. (3) Exported new top-level `validateContentMarkdownAgainstForbiddenMarkers()` helper that scans `contentMarkdown` against `FORBIDDEN_RAW_PAYLOAD_MARKERS` (case-insensitive substring) plus the strict structural secret-pattern regex (`GITHUB_TOKEN`, `FLOWDESK_*`, `Bearer <token>`, `ghp_*`/`ghu_*`/etc., `github_pat_*`, PEM PRIVATE KEY headers); the connector now drives its `content-markdown-contains-forbidden-marker` label through this helper. (4) `PublishToGitHubInputV1` gained an optional `attemptId?: string` field for the R-6 attempt-binding check; existing call sites pass `durableStateRoot` to the compaction-lock pre-check unchanged. (5) Production fetch security path (`resolveGitHubPublicationFetchImplForContextV1`) is unchanged — when the productionPublish flag resolves to `state: "enabled"`, the `fetchImpl` override is ignored and `globalThis.fetch` is forced. (6) Added 14 new tests in `packages/opencode-plugin/src/federated-registry-connector.test.ts` (11 Section 2.6 cases plus 3 supplementary AND-gate label tests): `test_token_not_in_errors_for_all_failure_modes` (11 failure-mode sweep including 500/403/429/throws + 7 blocked-mode inputs), `test_token_not_in_evidence_when_serialized`, `test_authorization_header_only_present_when_remote_write_enabled`, `test_fetch_not_called_when_preliminary_block_labels_present`, `test_authority_block_invariants_for_all_branches` (6 return branches), `test_productionPublish_flag_and_gates_with_existing_preconditions` (parameterized AND-gate removal), `test_mock_fetchImpl_seam_works_in_test_and_is_disabled_in_production`, `test_content_markdown_forbidden_marker_rejected` (parameterized over all `FORBIDDEN_RAW_PAYLOAD_MARKERS`), `test_content_markdown_requires_minimization_policy_ref`, `test_redactedReason_is_fixed_enum` (against a 26-label allow-list), `test_rate_limit_403_429_classified_as_rate_limited`, plus dedicated `guard-approval-ref-missing`, `guard-approval-attempt-mismatch`, and `ledger-idempotency-ref-missing` label tests. Verification: `npm run typecheck` clean (workspace); `npm test` 1115/1115 pass, 0 fail (1101 baseline + 14 new); targeted `node --test packages/opencode-plugin/dist/federated-registry-connector.test.js` 41/41 pass. No authority promotion; advisory-only / `remote_write_authority_enabled: false` / `dispatch_authority_enabled: false` / `lane_launch_authority_enabled: false` invariants are unchanged.

- **Phase 8b — Compaction Script + R-NEW-1 lock-path unification** (2026-06-10): (1) Added `flowDeskCompactionLockPathV1(stateRoot)` helper to `packages/core/src/compaction-runner.ts` exposing the single canonical path `<stateRoot>/.locks/compaction.lock`. Extended `FlowDeskCompactionEvidenceV1` with four additive optional fields — `records_preserved_due_to_pending_gate_promotion`, `lock_path`, `records_quarantined`, `compaction_run_id`. `packages/core/src/schema-artifacts.ts` updated to list these in the `flowdesk.compaction_evidence.v1` optional set and to type the two `records_*` counters as numbers; no required fields added (back-compat preserved). (2) `packages/opencode-plugin/src/federated-registry-connector.ts` `PublishToGitHubInputV1` gained an optional `durableStateRoot?: string`. When provided, the compaction-lock pre-check now uses the unified `<durableStateRoot>/.locks/compaction.lock` path (via the core helper); when absent the legacy `<cwd>/.flowdesk/locks/compaction.lock` fallback is preserved for callers still on the old path. (3) Implemented `scripts/compact-flowdesk-ledger.mjs` — `node:fs`-only runner with strict `--root` safety checks (resolve + realpath, refuses `..` segments, symlinks, `/`, `$HOME`, non-`.flowdesk` basename), hard-deny on `.git`/`node_modules`, single TTL source (`flowdesk.ledger_retention_policy.v1`) with `--max-age-days` narrowing-only, exclusive lock at `<root>/.locks/compaction.lock` (`openSync('wx')`), unconditional preservation of `publication_state === "pending_gate_promotion"` records, quarantine-on-delete-failure to `<root>/quarantine/<timestamp>-<sha>/`, durable `flowdesk.compaction_evidence.v1` write with all Phase 8b fields, and skip-not-error (exit 0) when the lock is already held. No `child_process`/spawn in the script. (4) Added 4 new tests: `test_compaction_lock_path_unified` and `test_compaction_lock_held_blocks_publish` (via `durableStateRoot`) appended to `packages/opencode-plugin/src/federated-registry-connector.test.ts`; new `packages/core/src/compaction-script.test.ts` covers `test_compaction_refuses_pending_gate_promotion_records` and `test_compaction_path_traversal_rejected` by spawning the script as a child process. Verification: `npm run typecheck` clean; `npm test` 1101/1101 pass, 0 fail (full functional suite). No authority promotion; the connector advisory-only invariants are unchanged.

- **Phase 8a — Burn-Rate Gate consumption** (2026-06-10): Added explicit FlowDeskSurplusUsageGateV1 reload + verdict/freshness enforcement to `publishToGitHubV1` in `packages/opencode-plugin/src/federated-registry-connector.ts`. New `surplusUsageGateBlockLabelsV1()` helper reloads the durable gate via the existing `evidenceRefResolver` and emits canonical kebab-case block labels — `surplus-usage-gate-missing`, `surplus-usage-gate-reload-failed`, `surplus-usage-gate-stale` (when `snapshot_fresh !== true`), and `surplus-usage-gate-verdict-deny` (when `gate_verdict !== "allow"`). Labels are merged into `preliminaryBlockLabels` via a deduplicating `Array.from(new Set([...]))` so older labels emitted by `productionPublishBlockLabelsV1` (`surplusUsageGate-unfresh`, `surplusUsageGate-not-allow`, etc.) remain in place for back-compat — additive AND-gate extension only; the existing `allowActualRemoteWrite + connectorGateSatisfied` conditions are unchanged. Added 4 new tests (`test_publish_consumes_fresh_surplus_usage_gate`, `test_surplus_gate_stale_blocks_publish`, `test_surplus_gate_verdict_deny_blocks_publish`, `test_surplus_gate_valid_does_not_block`) plus a `surplusGateEvidence()` helper that constructs internally-consistent gates (verdict-deny path uses `alertLevel: "critical"` to satisfy validator cross-field rules). Verification: `npm run typecheck` clean (workspace); `npm test --workspace @flowdesk/opencode-plugin` 288/288 pass, 0 fail; targeted federated-registry-connector tests 25/25 pass including the 4 new cases. No authority promotion; advisory-only record invariants unchanged.

- **usage-aware 툴 사용 방법 개선** (2026-06-10): (1) `flowdesk-main.md` agent routing 테이블에 "quota 확인 후 교체" 지침 추가 — 단일 레인 dispatch 포함 모든 `flowdesk_task` 호출 전 `flowdesk_quota` 필수 명시. (2) `flowdesk_task` 툴 description에 자동 모델 교체 동작(`requestedModel`/`effectiveModel`/`modelOverrideApplied` 응답 필드) 설명 추가. (3) `server.ts` `flowdesk_task` 핸들러에 `usageAwareOverride` → `executeFlowDeskAgentTaskV1` 전달 연결 — 교체 발생 시 durable evidence 기록 완성. Verification: `npm run typecheck` clean.

- **Usage-aware model selection 공통 적용 완료** (2026-06-10): `flowdesk_task`, `flowdesk_orchestrate`, `flowdesk_workflow_dispatch`, `flowdesk_auto_continue_execute` 4개 MCP 핸들러에 `resolveUsageAwareProviderQualifiedModelId` 적용. `allowCrossFamily: true` 기본값으로 exhausted/critical provider 자동 교체. `loadCachedUsageSnapshots()` (sidebar cache 재사용, fail-open) + `loadCachedAvailableModelIds()` (model-availability DB, fail-open) 헬퍼 추가. `flowdesk_orchestrate`의 `"openai/gpt-5.5"` 하드코딩 기본값 유지하되 resolver 통과로 실사용 quota 기반 교체. `executeFlowDeskWorkflowSchedulerV1` 경로는 assign-tool이 이미 usage-aware 선택 완료하므로 제외. 응답에 `requestedModel`/`effectiveModel`/`modelOverrideApplied`/`modelOverrideReason` 필드 추가. Verification: `npm run typecheck` clean; `npm test` 1093/1093 pass, 0 fail.

- **`resolveUsageAwareProviderQualifiedModelId` core 유틸 구현** (2026-06-10): `packages/core/src/usage-aware-model-resolver.ts` 신규. `allowCrossFamily: true` 기본값, fail-open, no I/O, no authority. alertLevel `exhausted`/`critical`/`non_dispatchable`/`stale`/`unknown` 시 대체 탐색. `ok` > `warning` 우선, 동순위는 `remainingPercent` 높은 순. `packages/core/src/index.ts` barrel export 추가. 17개 테스트 포함. Verification: typecheck clean; 811/811 pass.

- **Phase 8 완성플랜 문서화** (2026-06-10): `docs/PHASE8_COMPLETION_PLAN.md` 작성. 6개 슬라이스(8a–8f), 의존성 그래프, Exit Criteria 매트릭스, 위협모델 체크리스트 포함. R-NEW-1(lock 경로 불일치) 신규 위험 식별. 코드 변경 없음.

- **usage_aware_model_override evidence schema + durable writer** (2026-06-10): Added `FlowDeskUsageAwareModelOverrideEvidenceV1` (schema_version `flowdesk.usage_aware_model_override.v1`) to `packages/core/src/usage-aware-model-resolver.ts` with explicit selection-phase authority block (`selection_phase_only: true`, `fallback_authority_enabled: false`, `dispatch_authority_enabled: false`). The type is re-exported through `packages/core/src/index.ts` (existing `export *`). In `packages/opencode-plugin/src/agent-task-runner.ts`: added `writeUsageAwareModelOverrideEvidence` helper following the existing `writeRuntimeLaunchModelSelectionEvidence` pattern (evidence id `usage-aware-override-<laneId>-<token>`), added optional `usageAwareOverride?: { originalModelId; overrideReason; allowCrossFamily }` field to `FlowDeskAgentTaskInputV1` so upstream callers (e.g. `server.ts` handlers that apply `resolveUsageAwareProviderQualifiedModelId`) can record the decision, and wired the writer into `executeFlowDeskAgentTaskV1` immediately after `resolveOpenCodeRuntimeLaunchModelBindingV1`/`writeRuntimeLaunchModelSelectionEvidence` (still before the `if (!modelBinding.ok)` early return). The record is written only when `originalModelId !== providerQualifiedModelId`, so unchanged-binding callers produce no extra evidence. No call sites pass `usageAwareOverride` yet — this is the schema+writer foundation; the server handler integration is a separate task. Verification: `npm run typecheck` clean; `npm test` 1093/1093 pass, 0 fail.

- **Release 2.5 Active Operation Enabled** (2026-06-10): Promoted the project from Release 1 "inert fallback" mode to Release 2.5 active operation.
  - Updated `packages/opencode-plugin/src/commands.ts` templates to reflect active status ("Runtime: Enabled via FlowDesk Managed Dispatch Beta (Release 2.5)").
  - Updated `packages/opencode-plugin/src/command-handlers.ts` to provide active, non-diagnostic-only user messages for usage, doctor, resume, abort, and export-debug commands.
  - Rematerialized all portable command files in `.opencode/commands/` to reflect these changes.
  - Promotion logic now favors "ready" and "active" statuses over "diagnostic_only" when evidence is present.
  - Verification: `npm run build` and filesystem check of materialized `.md` files passed.

- **portable command file artifact verification** (2026-06-10): Verified that the `/flowdesk-usage` portable command file artifact at `.opencode/commands/flowdesk-usage.md` and the generated content in `packages/opencode-plugin/src/commands.ts` exactly match the Release 2.5 specification. The artifact is now marked as active and authorized for Managed Dispatch. Verification: Generated content comparison and filesystem check passed.

- **Slice 2: Hardened finalizer reload verification** (2026-06-10): Implemented fail-closed lifecycle linkage in `observeAndFinalizeManagedDispatchLaneV1` (`packages/opencode-plugin/src/managed-dispatch-adapter.ts`). Changes: (1) Added `terminal_linkage_failed` status to `FlowDeskManagedDispatchLaneFinalizeResultV1` union and `terminalLinkageVerified?: boolean` field. (2) After all per-write reload checks pass, the finalizer now performs an authoritative post-completion reload that verifies both a terminal `task_result`/`task_failed` record AND a terminal `lane_lifecycle` record exist together for the lane. If either is absent/stale, the function returns `terminal_linkage_failed` instead of generic success. The no-output path similarly verifies the lifecycle record is reloadable. (3) Successful paths now set `terminalLinkageVerified: true`. Added 5 regression tests in `managed-dispatch-adapter.test.ts` covering: normal success (`lane_finalized` with `terminalLinkageVerified=true`), no-output path (`lane_no_output` with `terminalLinkageVerified=true`), stale reload simulation, linkage guard idempotency, and full Slice 2 regression with file deletion. Verification: `npm run typecheck` clean, `npm test` 282/282 pass (0 fail), managed-dispatch-adapter tests 20/20 pass.



- **reviewer_verdict materialization test** (2026-06-10): Verified that terminal task capture in `executeFlowDeskAgentTaskV1` properly materializes `reviewer_verdict` evidence by parsing `resultText` as `FlowDeskTopTierReviewVerdictV1`. Added a test `flowdesk_agent_task_run terminal reviewer lane produces durable verdict record` in `packages/opencode-plugin/src/server.test.ts` which asserts the evidence is durably written to `.flowdesk` state root with correct label and lane mappings. Verification: 1076/1076 tests passed.

- **working-model 잔재 완전 제거** (2026-06-10): `workflow-assign-tool.ts`의 `loadWorkingModelIds()` → `loadAvailableModelIdsFromDb()`로 이름 변경, 에러 메시지 3곳을 "working-model cache" → "model availability db"로 통일. `managed-dispatch-adapter.ts`의 `workingModelCacheAllowsDispatch()` 내부 변수명(`workingModelIds` → `availableModelIds`) 및 에러 메시지 정리. 소스 파일에 `working-model` 문자열 잔재 0건 확인. Verification: 1075/1075 통과.

- **provider-catalog.json GitHub 동기화 설계 문서화** (2026-06-10): `AUDIT_GITHUB_CONNECTOR_PHASE8.md`에 Phase 8 완성 시 구현할 provider-catalog.json 동기화 설계 추가. 스키마 정의(`schema_version`, `families[]`, `tiers{}`, `roles{}`), model-selection-engine.ts 하드코딩 → catalog 로드 변환 매핑, 필수 동기화 4개 지점(dispatch switch, agent-task-runner rootDir, 에이전트 프로파일, 번들 fallback), 후순위 항목(TUI, usage collector), 보안 고려사항(opencode_provider_id whitelist, agent_name 경로 traversal 방지) 문서화. 코드 변경 없음.

- **refresh 스크립트 provider 전체 확장 및 분류 정확도 개선** (2026-06-10): `refresh-opencode-model-availability.mjs`의 `familyBlockMapFromCatalog`를 anthropic 전용에서 openai/google/opencode 등 전체 provider 패턴으로 확장. `classifyUnavailability()` 함수 도입 — `PERMANENTLY_UNSUPPORTED_PATTERNS`(API 404, model not found 등)와 `ACCOUNT_SPECIFIC_PATTERNS`(구독 없음, credentials expired, Codex account 등)를 명확히 분리. `shouldSkipAsUnsupported`가 구독 없음을 영구 스킵으로 잘못 처리하던 버그 수정. `probeModel`의 개별 레벨 reason도 `classifyUnavailability` 기반으로 통일(`model_not_supported` / `account_or_subscription_required`). Verification: 분류 로직 6케이스 통과, 1075/1075 테스트 통과.

- **model-availability DB merge 모드 구현** (2026-06-10): `export-model-availability-db.mjs`에 merge 전략 추가. probe된 모델(`results[]` 포함)만 UPSERT, probe 안 된 모델(구독 없음/skipped)은 기존 DB row 보존. `last_probed_at` 컬럼 추가 (어느 실행에서 probe됐는지 추적). 기존 DB 스키마 자동 마이그레이션(`ALTER TABLE ADD COLUMN`). Phase 8 GitHub 업로드/다운로드와 연계 시 환경별 구독 차이를 누적 통합하는 설계로 `AUDIT_GITHUB_CONNECTOR_PHASE8.md`에 문서화. Verification: `npm run build` 클린, 테스트 3/3 통과.

- **model-availability SQLite DB 도입 및 working-models.json 완전 제거** (2026-06-10): `docs/model-availability/working-models.json`, `working-models.md` 삭제. `scripts/export-working-models.mjs` → `scripts/export-model-availability-db.mjs`로 재작성 — Node 24 내장 `node:sqlite`로 `opencode-model-availability.json`을 `model-availability.db`(테이블: `snapshot`, `models`, `model_sets`)로 변환, `packages/opencode-plugin/data/`(배포 번들) 및 `$FLOWDESK_DURABLE_STATE_ROOT/model-availability/`(런타임)에 동시 기록. `package.json` `models:working` 제거, `models:refresh`가 DB export까지 자동 실행. `packages/opencode-plugin/package.json` `files`에 `data/model-availability.db` 추가. `managed-dispatch-adapter.ts` `loadAvailableModelIdsFromDb()` + `workingModelCacheAllowsDispatch()` 및 `workflow-assign-tool.ts` `loadWorkingModelIds()`가 durable DB 우선 쿼리 + 패키지 번들 DB fallback으로 변경 (`SELECT model_id FROM models WHERE available=1`). 관련 테스트 픽스처를 `DatabaseSync` 기반 `writeModelAvailabilityDb()` 헬퍼로 교체. Verification: `npm run build` 클린, 1075/1075 통과.

## Current Canonical State (2026-06-09)

- **Phase 2.5 / Phase 6A closure** (2026-06-09): All 6 exit criteria verified as DONE. EC1: planFlowDeskReviewerFanoutFromReloadedCacheEvidenceV1 (packages/core/src/session-evidence.ts:452) uses daily model-availability cache with same-model multi-perspective support via modulo assignment. EC2: resolveQuickReviewerBindings returns explicit blocked() on missing/invalid binding. EC3: FlowDeskTopTierReviewVerdictV1 (packages/core/src/release1-contracts.ts:1516) enforces dispatch_authority_enabled:false and guard_replacement_authority_enabled:false. EC4: No opencode run subprocess in quick-reviewer-run.ts; all execution via executeFlowDeskRuntimeReviewerExecutionBridgeV1. EC5: quickReviewerRun requires developerModeAcknowledged and allowProviderCall (packages/opencode-plugin/src/server.ts:4463). EC6: laneCount+quick_reviewer_run_incomplete status prevent silent fan-out shrink. No new implementation was needed; existing code satisfies all criteria.

- **Managed dispatch adapter test regex fix** (2026-06-09): Updated the two failing test regexes in `packages/opencode-plugin/src/managed-dispatch-adapter.test.ts` to match the actual error message (`/Dispatch attempt manifest and durable evidence reload are required/`) following a previous adapter error-message refinement. Verification: `npm run build && node --test --test-name-pattern "managed dispatch beta adapter blocks without configured durable|managed dispatch beta adapter blocks on reservation store" packages/opencode-plugin/dist/managed-dispatch-adapter.test.js` passed (2 pass, 0 fail).

- **Managed-dispatch test assertion fix** (2026-06-09): Fixed a failing test in \`packages/opencode-plugin/src/managed-dispatch-adapter.test.ts\` where the assertion regex was mismatched after a message change. Updated \`/Evidence with id "lane-lifecycle-failed-1" already exists/\` to \`/lane lifecycle evidence already exists/\`. Verification: \`npm run build && node --test --test-name-pattern "runtime lane launch lifecycle materializer blocks duplicate" packages/opencode-plugin/dist/managed-dispatch-adapter.test.js\` passed.

- **Managed-dispatch working-model gate test type cleanup** (2026-06-09): Updated the three new \`dispatchManagedDispatchBetaPromptV1\` working-model gate tests in \`packages/opencode-plugin/src/managed-dispatch-adapter.test.ts\` to remove the unsupported \`rootDir\` input property and pass the adapter's typed \`durableStateRootDir\` (\`<temp>/.flowdesk\`) instead, preserving the existing missing/empty/unsupported cache assertions. Verification pending command approval.

- **Homedir guard test portability cleanup** (2026-06-09): Updated \`packages/opencode-plugin/src/homedir-guard.test.ts\` to stop mutating \`process.env.HOME\` and instead use the real \`os.homedir()\` path. The test creates its temporary workspace under \`~/.flowdesk/test-diag/\`, asserts \`join(os.homedir(), ".flowdesk", "wake-dispatch-diag.log")\` exists, and removes that diagnostic file during cleanup. Removed remaining debug \`console.log\` output from the homedir guard path, including the temporary diagnostic-enabled print in \`completion-wake-main-session.ts\`; finalized the test cleanup by removing the temporary recursive homedir listing helper/imports. Verification pending command approval.

- **TUI/Sidebar visibility and infrastructure restoration** (2026-06-09): Resolved TUI visibility issue (root cause: global \`tui.json\` pointed to npm package with incorrect \`durableStateRootDir\`). Actions: (a) converted global TUI to dev build, (b) cleaned project \`opencode.json\` of duplicate plugin registrations, (c) removed \`server.ts\` WAKE_DIAG dead code, (d) fixed \`completion-ui-cache.ts\` heartbeat \`parentSessionRef\` resolution, (e) implemented \`local-adapter.ts\` evidence-based lane discovery, (f) fixed \`bootstrap-installer.ts\` TUI \`pathToFileURL\` pathing, and (g) transitioned remaining diagnostic logging to \`homedir()\`-based paths.

- **Restore durable working-model gate and document unattached parent session semantics** (2026-06-09): Restored the durable working-model evidence check in \`packages/opencode-plugin/src/managed-dispatch-adapter.ts\` (\`workingModelCacheAllowsDispatch\` now loads \`model-availability/working-models.json\` and fails closed if stale/missing) and injected the required \`rootDir\` at the call site in \`dispatchManagedDispatchBetaPromptV1\`. Added documentation comments to \`packages/opencode-plugin/src/agent-task-runner.ts\` and \`packages/opencode-plugin/src/managed-dispatch-adapter.ts\` clarifying that \`ses-unattached-parent-session\` sentinel launches do not appear in session-scoped sidebar rows. Verification: \`npm run build\` passed.

- **Selection phase documentation clarity** (2026-06-09): Clarified JSDoc/block comments in \`packages/opencode-plugin/src/model-selection-engine.ts\` for \`selectModelForTask\` and \`resolveOpenCodeRuntimeLaunchModelBindingV1\` to explicitly distinguish between selection-phase (assignment/planning) and managed-dispatch phase (actual runtime binding). Verified call sites in \`agent-task-runner.ts\` correctly use the latter. Build checked.

- **Wake diagnostic infrastructure and portability fix** (2026-06-09): Removed \`server.ts\` WAKE_DIAG dead code. Remaining diagnostic logging infrastructure uses \`join(homedir(), ".flowdesk", <log filename>)\` for portability, gated by \`FLOWDESK_WAKE_DIAG\`. Verification: \`npm run build\` clean.

- **Bootstrap installer direct TUI plugin registration fix** (2026-06-09): Updated \`packages/opencode-plugin/src/bootstrap-installer.ts\` so \`ensureFlowDeskPluginConfig()\` now materializes both FlowDesk plugin entries directly in \`opencode.json\`: the server plugin (\`server.js\`) and the TUI sidebar plugin (\`tui.js\` with durable usage options\). The installer no longer writes \`tui.json\` as the primary TUI configuration file, and rollback/profile refs were aligned to the remaining \`opencode.json\` materialization path. Updated \`packages/opencode-plugin/src/bootstrap-installer.test.ts\` to assert both plugin entries in mocked \`opencode.json\`, preserve existing third-party/server entries while appending TUI config, and confirm fresh installs no longer create \`tui.json\`. Verification: \`npm run build && npm run test\` passed (**1073/1073** tests).

- **/flowdesk-status evidence-backed lane summaries fix** (2026-06-09): Updated \`packages/opencode-plugin/src/local-adapter.ts\` so durable status context reloads \`agent_task_context\` and \`lane_lifecycle\` session evidence for the requested workflow, materializes evidence-only lanes into local lane records, and merges them with durable \`sessions/session-local/lanes.jsonl\` records before building status. This prevents \`/flowdesk-status\` from returning empty \`lane_summaries\` when agent-task/lifecycle evidence exists but no legacy lane record was written. Added focused \`server.test.ts\` coverage that writes only evidence records and asserts the status response includes the discovered lane summary. Verification: \`npm run typecheck\` clean; targeted \`node --test --test-name-pattern "local adapter status includes lanes discovered from evidence records" packages/opencode-plugin/dist/server.test.js\` passed.

- **Sidebar task visibility heartbeat-only lane fix** (2026-06-09): Updated \`packages/opencode-plugin/src/completion-ui-cache.ts\` so \`refreshFlowDeskCompletionUiCachesV1()\` loads the latest \`lane_heartbeat\` evidence per lane, includes heartbeat-only lanes in the sidebar row lane set, and uses the heartbeat record as a fallback source for \`parent_session_ref\` when resolving \`parentSessionRef\`. This keeps newly launched/running lanes visible in session-scoped sidebar caches even when heartbeat evidence arrives before context/child-session evidence. Verification: \`npm run build && node --test packages/opencode-plugin/dist/completion-ui-cache.test.js packages/opencode-plugin/dist/async-lane.test.js\` passed (**64/64** targeted tests).

- **/flowdesk-usage diagnostic evidence mapping** (2026-06-09): Replaced the command-backed usage handler's hardcoded-only placeholder path with bounded cached-evidence mapping in `packages/opencode-plugin/src/command-handlers.ts`. The handler context now accepts optional provider usage evidence rows, selects the freshest row matching the requested provider family (including Claude/Gemini/OpenCode aliases), maps usage/provider-health snapshot refs, freshness, dispatchability, and uncertainty flags into `FlowDeskUsageResponseV1`, and still preserves Release 1 no-provider-call/no-runtime/no-lane/no-fallback authority. No-evidence and auth-missing provider paths retain the existing conservative non-dispatchable fallback. Added focused `command-handlers.test.ts` coverage for freshest matching evidence. Verification: Verified in this session.

- **Restore Release 2 managed dispatch gate** (2026-06-09): The `productionEnablement` block (with nested `defaultManagedDispatchAuthorizationMetadata`) has been restored to the FlowDesk server plugin configuration in `~/.config/opencode/opencode.json`, mirroring the structure from `slice4ProductionEnablementOption()` in `server.test.ts`. Additionally, required durable evidence files (`pre_dispatch_audit_record`, `provider_health_snapshot`, `production_approval_source` (consumed), `dispatch_idempotency_snapshot`, and `exact_model_availability_cache`) have been provisioned under `/Users/bagel_macpro_055/.flowdesk/.flowdesk/sessions/doctor-probe-default-managed-dispatch/evidence/` with content matching `slice4WriteDurableEvidence()` in `server.test.ts`. Verification: Manual inspection of `opencode.json` structure and `ls -l` confirmed the presence of all durable evidence files. The `flowdesk_pre_spike_doctor` tool's output reflects the plugin's internal load status and does not explicitly indicate full dispatch authorization, which is expected for this diagnostic tool.


- **P7-S21: ledger-rollup-safety.test.ts — 안전성 테스트 작성** (2026-06-11): `packages/core/src/operational-intelligence/ledger-rollup-safety.test.ts` 신규 작성. (1) **테스트 목표**: P7-S20 구현의 Exit Criterion #8/#9 검증 — cumulative sum 금지 증명, dispatch authority 완전 차단, 최소 샘플 임계값 강제, 모든 authority flag false 리터럴 보증. (2) **11개 테스트**: ① `computeWeightedMeanV1` ≠ cumulative sum — decay 가중치로 raw sum과 명확히 다른 값 계산 증명 (평균 70 vs 가중평균 > 71). ② raw partition → `blocked_raw_partition` 즉시 차단. ③ sealed partition → 정상 rollup (스키마, sample_count, effective_sample_count 검증). ④ 모든 authority flags false — `advisory_only: true`, `dispatch_authority_enabled: false`, `ranking_authority_enabled: false` 리터럴 타입 검증. ⑤ dispatch/Guard 승인 불가 — advisory_only 리터럴 고정 확인. ⑥ 중복 entry의 HHI 효과 — monopoly(1.0) vs distributed(1/5 ≈ 0.2) scorer_concentration 비교, ESS가 중복 시 낮음 증명. ⑦ scorer_concentration ∈ [0..1] — HHI 범위 검증 (monopoly=1.0, 3-way equal=0.33). ⑧ confidence_bucket은 라벨만 반환 — "low"/"medium"/"high"/"very_high" 중 하나, 숫자 노출 금지. ⑨ < 3 entries → `insufficient_data` 차단 (0/1/2개 각각, borderline 3개 허용). ⑩ rollup_hash 결정적 — 동일 입력 → 동일 sha256 hash, `sha256-` prefix + 64hex format 검증. (3) **Bonus**: Kish ESS 불변식 — ESS = (Σw)² / Σ(w²) 공식 정확성 확인. (4) **타입 안전성**: 모든 test 통과 후 `npm run typecheck` 클린(0 errors). (5) **검증**: `node --test packages/core/dist/operational-intelligence/ledger-rollup-safety.test.js` → **11/11 pass, 0 fail** + 기존 ledger-rollup.test.ts 12개 + bonus 1개 함께 실행 → **24/24 pass, 0 fail** (리그레션 없음).
