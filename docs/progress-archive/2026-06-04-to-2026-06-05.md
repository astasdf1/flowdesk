# FlowDesk Progress Archive: 2026-06-04 to 2026-06-05

Source: migrated from `docs/PROGRESS_SNAPSHOT.md` on 2026-06-05.

Preservation note: this archive preserves historical evidence claims from the append-only progress log. Do not silently rewrite, delete, or reinterpret archived claims; append a dated correction note if a historical claim is later superseded.

On 2026-06-05, a focused TUI provider usage sidebar slice added reset-window display priority for low buckets. The compact sidebar now prioritizes a short-window/5h bucket when it is below 20% remaining, prioritizes a long-window/weekly bucket when it is below 5%, chooses the lower remaining percent when both priority thresholds are triggered, and otherwise preserves the existing period-normalized representative-bucket selection. Focused regression coverage was added for short-priority, long-priority, both-triggered lower-percent, and no-trigger representative behavior. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin` and `node --test packages/opencode-plugin/dist/tui-usage-snapshot.test.js` (13/13). `git diff --check` was not run because the active tool permission policy denies `git *` bash commands in this session.

On 2026-06-05, a small TUI readability tweak added one blank line between the provider usage/status block and the auto-next/subtask activity block in `formatFlowDeskTuiSidebarCompactLines`, making the sidebar visually separate usage from subtask rows. The compact sidebar expectation was updated accordingly. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin` and `node --test packages/opencode-plugin/dist/tui-subtask-activity.test.js packages/opencode-plugin/dist/tui-usage-snapshot.test.js` (32/32). The active OpenCode/TUI process needs restart to pick up the rebuilt TUI plugin.

On 2026-06-05, an implementation slice improved completed lane result display for provider-free synthesis/status surfaces. `flowdesk_workflow_synthesis_preview` now returns bounded `taskResultExcerpts` reconstructed from terminal `task_result.result_text`, preserves line breaks, redacts forbidden raw-provider/auth/dispatch markers, and includes safe result excerpts in the provider-free `summaryForUser` when synthesis preview is generated. `flowdesk_status_live` now exposes `latestTaskResultExcerpts` per workflow and includes a bounded preserved-linebreak task-result excerpt in the user-facing status summary. Focused tests were added for line-break preservation and forbidden excerpt omission. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin` and `node --test packages/opencode-plugin/dist/workflow-synthesis-tool.test.js packages/opencode-plugin/dist/status-live-tool.test.js` (21/21), followed by `git diff --check`.

On 2026-06-05, a concise FlowDesk main-agent coordinator-instruction optimization removed the mandatory post-launch status polling rule from the project profile, active global profile, and bootstrap installer template. The new wording treats `task_launched` as launch acknowledgment only, makes wake prompts advisory notification triggers, lists demand-driven `flowdesk_status_live` conditions with Korean status examples, tells the coordinator to continue independent in-scope work without polling, and prevents todo completion from launch acknowledgment alone. Auto-invocation now points status/progress intents back to the Status section, reviewer routing avoids a duplicate quick-reviewer quarantine phrase, and multi-model/multi-perspective reviews explicitly count one lane per perspective within the five-lane cap. Bootstrap installer regression assertions now cover old phrase removal plus the key new phrases. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin` and `node --test packages/opencode-plugin/dist/bootstrap-installer.test.js` (10/10). `git diff --check` was not run because the active tool permission policy denies `git *` bash commands in this session.

On 2026-06-05, a focused test-execution compression slice split npm test entrypoints without deleting or rewriting tests. Root and workspace package scripts now keep the previous full build-plus-all-tests behavior available as `test:full`, make default `npm test` run `test:functional`, and route known slow watchdog/timeout coverage (`async-lane`, `stall-recovery`) to `test:slow` plus heavier plugin adapter/server coverage (`managed-dispatch-adapter`, `quick-reviewer-run`, `server`) to `test:integration` through a small `scripts/run-tests.mjs` selector. Build and typecheck scripts are unchanged. Focused sanity verification passed with core workspace slow/integration no-op routing and core `test:functional` (472/472); the full suite was intentionally not run for this slice.

On 2026-06-04, ADR platform evidence demotion was applied to the conformance plan, threat model, and user manual. `docs/OPENCODE_CONFORMANCE_PLAN.md` now defines conformance as plugin/SDK-observable behavior only, makes runtime echo/telemetry/lane/platform execution facts non-gating diagnostics, and adds `attestation_scope=plugin_observed_only` to conformance completion records. `docs/THREAT_MODEL.md` adds the Plugin Boundary Scope threat section for trusted laundering, host version drift, approval replay, cross-attempt binding, and audit redaction gaps. `docs/USER_MANUAL.md` now tells users that FlowDesk verifies only plugin-side evidence (requested, persisted, and SDK-returned facts) and does not verify platform-internal execution facts.

On 2026-06-04, `docs/IMPLEMENTATION_ROADMAP.md` was updated for the Platform Evidence Demotion ADR. Release 2 and the managed-dispatch gate now use the full 12-item plugin-verifiable evidence set, completion records state `attestation_scope=plugin_observed_only`, and OpenCode platform-internal runtime echo, telemetry correlation, lane conformance, and usage authority facts are retained only as non-gating diagnostics. Phase 4 was renamed to OpenCode Plugin/SDK Compatibility and scoped to plugin-observable load, schema, command-backed behavior, SDK adapter, exposed session/message/lifecycle observation, fail-closed modes, and durable evidence reload validation.

On 2026-06-04, `AGENTS.md` was updated for the Platform Evidence Demotion ADR. The repository guidance now removes OpenCode platform-internal evidence from managed-dispatch and managed-fallback gate/completion criteria, treats runtime mode, internal telemetry, lane conformance, and account-scope usage authority only as non-gating diagnostics when observable, and updates Release 1 conformance wording to Plugin/SDK compatibility reporting for supported OpenCode versions. The current workspace `AGENTS.md` has no Safety Rule 18 section, so the requested Rule 18 wording was not applied there.

On 2026-06-04, a background-lane permission-stall fix removed `ask` permissions from FlowDesk installer-generated subagent bash policies and from the active global FlowDesk subagent profiles. Root cause evidence from `workflow-adr-docs-rewrite-20260604` showed a docs-writer child session stuck at unreachable OpenCode permission UI while running in the background. The policy now uses role-tiered bash allow-lists plus `"*": deny` for all non-`flowdesk-main`/non-`flowdesk-git-master` subagents; `flowdesk-git-master` keeps broad bash with dangerous git denials, and `flowdesk-main` remains the coordinator profile. Regression coverage asserts generated subagent frontmatter contains no `: ask` rules, includes safe read/build/test allow-list entries where appropriate, and defaults to deny for unspecified bash commands. The active `~/.config/opencode/agent/flowdesk-*.md` profiles were updated consistently. Verification passed with the plugin build, focused bootstrap-installer tests, `git diff --check`, and full `npm test` (879/879). Existing lanes already stuck at a prior `ask` prompt still require explicit abort/retry or manual handling; the no-`ask` policy only applies to newly loaded/restarted agent profiles.

On 2026-06-04, the repository test commands were split so ordinary development checks no longer require the full 879-test, roughly 152-second release regression pass. Root `npm test` now delegates to `test:functional`, backed by `scripts/run-tests.mjs --mode functional`, while `test:full`, `test:slow`, and `test:integration` remain available at the root and package workspace level. The functional mode excludes the known slow/integration-heavy plugin files and completed in-session with 617 passing tests in about 30.8 seconds. `test:slow` covers the async/watchdog/stall-recovery files and passed 84 tests in about 90.6 seconds. `test:integration` covers managed-dispatch, quick-reviewer, and server/plugin tool registration paths and passed 178 tests in about 150.7 seconds, reflecting the intentional 90-second no-response/hand-off regression in `server.test.js`. The earlier full-suite state remains 879/879 passing, and the split modes together preserve the existing coverage without deleting tests. The implementation lane that applied the script patch later stalled on a running tool with no terminal result, so the patch was salvaged and verified from the main session; no automatic abort/retry/fallback authority was taken.

On 2026-06-04, a post-restart live permission smoke exposed that project-local `.opencode/agent/flowdesk-*.md` profiles still had `bash: "*": ask`, so OpenCode loaded the project profile ahead of the previously fixed global profile and emitted `permission.asked` wake/sidebar evidence. The project profiles now use `"*": deny` for non-main/non-git-master bash catch-all, matching the installer/global no-ask policy. Build/test-capable profiles now explicitly allow both `node scripts/run-tests.mjs*` and `node ../../scripts/run-tests.mjs*` so the new split test runner can run without permission asks. Research capability was preserved by setting `webfetch: allow` for `flowdesk-explorer-researcher`; other background profiles do not use `webfetch: ask`. The event hook now treats only `permission.asked` as `awaiting_permission`, treats `permission.updated`/`permission.replied` as a response, restores bounded capture relevance for `message.updated` turn-completed and terminal step events, and completion wake cache refresh removes stale awaiting-permission/diagnostic wake rows once later non-permission progress supersedes them. Focused verification passed with plugin build, `git diff --check`, and built tests for bootstrap installer, project agent profiles, event hook observer, completion UI cache, and server/plugin paths (127/127). OpenCode must be restarted again before project/global profile changes and rebuilt plugin event-hook behavior are active for a fresh live permission smoke.

The follow-up post-restart permission-policy smoke (`workflow-live-permission-policy-smoke-20260605`) confirmed no `permission.asked` evidence and no permission wake while testing allowed, explicitly denied, and unspecified commands, and the lane completed as terminal `task_result`. The task result reported a remaining policy-order failure: allowed commands (`git status --short` and `node scripts/run-tests.mjs --mode functional --package core`) were blocked because OpenCode permission maps are last-match-wins and the active allow-list profiles still let catch-all denies override intended allows. The project, global, and installer-generated FlowDesk subagent profiles now put allow-list `"*": deny` first, then explicit safe/read/build/test allow entries, with exact command allow entries for the known smoke/focused-verification commands (`git status --short`, `git diff --check`, `npm run build --workspace @flowdesk/opencode-plugin`, `node scripts/run-tests.mjs --mode functional --package core`, and the focused built profile test command). Broad-allow `flowdesk-main`/`flowdesk-git-master` profiles keep `"*": allow` before dangerous git denials. `project-agent-profiles.test.ts` and `bootstrap-installer.test.ts` now assert last-match-wins-compatible ordering, no `: ask` rules, exact smoke command allows, and broad-allow dangerous-deny ordering. Main-session focused verification passed with `git diff --check`, `npm run build --workspace @flowdesk/opencode-plugin`, and built profile/bootstrap tests (12/12). Earlier verifier lanes remained permission-blocked until OpenCode was restarted, showing that running child sessions can hold stale profile snapshots.

After the restart, final live permission/wake smoke `workflow-permission-post-restart-final-smoke-20260605` completed as durable terminal `task_result` with zero stalled, late, failed, or awaiting-permission counts. The smoke proved the restarted runtime loaded the updated verifier profile: `git status --short` ran without ask, `node scripts/run-tests.mjs --mode functional --package core` ran without ask and passed 472/472, explicitly denied `git reset --hard` was blocked without ask, unspecified `uname -a` was blocked without ask, and the lane completed normally. The main-session wake appeared once for the terminal result, so the completion-wake consumer lock prevented the earlier duplicate `[FlowDesk:done]` wake race in this smoke.

The same smoke also exposed a main-session wake race: `[FlowDesk:done]` was injected three times for one terminal task result. The completion wake cache ended with one consumed row, proving the duplicates came from concurrent event-hook consumers that read the unconsumed row before any caller wrote `consumed=true`. `consumeFlowDeskCompletionWakeForMainSessionV1` now uses an exclusive filesystem lock directory under `.flowdesk/ui/completion-wake-consumer.lock`; concurrent consumers skip with `wake_consumer_lock_active` instead of dispatching another parent prompt. Focused regression coverage asserts the lock suppresses prompt dispatch. Verification passed with plugin build, `git diff --check`, and built wake/event/profile/bootstrap tests (22/22). OpenCode must be restarted before the wake-lock fix is active in the live plugin.

On 2026-06-04, a focused bootstrap installer slice made fresh FlowDesk installs self-configure the OpenCode plugin entry in `opencode.json`. `materializeFlowDeskMainAgentProfileWithTui` now preserves existing `default_agent`, agent profile, and `tui.json` materialization while ensuring `plugin` contains a `file://.../@flowdesk/opencode-plugin/dist/server.js` entry with the active default FlowDesk options, including durable state root, live status/usage, dev/beta agent-task/workflow tools, fallback regate planning, heartbeat writer, controlled write apply, chat stall alerts, and completion wake. Existing FlowDesk plugin entries are detected by server.js path pattern and merged additively so user-set values and user-specific provider/workspace fields are not overwritten; non-FlowDesk plugin entries are preserved. Focused bootstrap-installer regression coverage was added for fresh plugin option creation and existing plugin merge behavior. Verification commands are pending user execution.

On 2026-06-04, a focused main-wake/status slice changed observed child-session tool failures from automatic failure evidence into advisory coordinator attention. `monitorChildSessionsV1` still reconciles terminal tool snapshots (`error`/`aborted`/`cancelled` or exact redacted `Tool execution aborted`) by writing synthetic settled/error progress to close the open-tool wait immediately before `staleToolMs`, but it now writes bounded `tool_execution_aborted_observed` diagnostic progress plus child-session capture diagnostics, refreshes completion UI caches, and produces a parent-session-scoped `diagnostic_attention` wake-ready row for the existing main-session wake consumer. It does not write `task_failed`, terminal lifecycle, abort, retry, fallback/reselect, dispatch, controlled writes, hard-chat actions, or expose raw commands/output/transcripts/provider payloads for this condition. A `tool_execution_aborted` failure category was added to the core schema for coordinator-explicit use only; the watchdog does not auto-write it. Focused monitor/event relevance tests were added. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, focused tests (async-lane, stall-recovery, completion-wake, status-live, event-hook-observer, core task-result), `git diff --check`, and full `npm test` (875/875).

On 2026-06-04, a focused async watchdog/runtime slice added child-session tool-error reconciliation. `monitorChildSessionsV1` now best-effort reads the child `session.messages` snapshot when event-derived tool progress leaves call IDs open; if a matching tool part has terminal `state.status`/`status` (`completed`, `error`, `cancelled`, or `aborted`), it writes bounded synthetic `agent_task_progress` (`agent task tool settled/error callid=...`) so `deriveFlowDeskLaneToolStateV1` closes the open tool set instead of drifting into stale `toolStateUnknown` diagnostics. The record stores only redacted call IDs/status-derived labels and keeps `dispatch_authority_enabled=false`; it does not add a new failure category, main-session wake, abort/retry/fallback, provider/runtime authority, controlled writes, or hard-chat authority. Focused regression coverage was added in `async-lane.test.ts`; verification commands are pending coordinator execution.

On 2026-06-04, post-restart verification found two obsolete full-suite test expectations that still assumed bounded no-response child sessions immediately materialize terminal `task_failed/no_response` or `lane_lifecycle state="no_output"`. The source regressions in `server.test.ts` and `stall-recovery.test.ts` now match the current timeout/backfill policy: when a child session exists but no response text is available during bounded synchronous capture, FlowDesk returns `task_launched`/`retry_launched`, preserves `agent_task_child_session` evidence, marks retry plans launched, and leaves terminalization to watchdog/status backfill instead of writing immediate failure evidence. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, the two focused fixed tests (2/2), focused managed-dispatch/async/status/profile tests (100/100 combined), `git diff --check`, and full `npm test` (872/872).

On 2026-06-04, a follow-up audit of the explicit opt-in managed-dispatch auto-open slice tightened the server-side option shape for derived default managed-dispatch authorization. The route continues to preserve explicit top-level `defaultManagedDispatchAuthorization` precedence, but the derived path now only recognizes nested `productionEnablement.defaultManagedDispatchAuthorizationMetadata.enabled=true` and no longer accepts a legacy nested `defaultManagedDispatchAuthorization` alias. The derived metadata type now requires `sdkClientRef`, matching the existing runtime guard for an injected SDK client plus plugin-satisfiable production enablement, promotion readiness refs, inactive kill switch, default enablement request, non-expired authorization, and durable evidence reload. Focused verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, `node --test --test-name-pattern="/flowdesk-run managed-dispatch|default managed-dispatch authorization|derived default|explicit opt-in" packages/opencode-plugin/dist/managed-dispatch-adapter.test.js` (8/8), and `git diff --check`. This completes the explicit opt-in auto-open path only: default managed dispatch remains closed without the nested opt-in metadata and required evidence, and fallback/reselection, controlled writes, hard-chat/noReply, and approval-less automatic execution remain closed.

On 2026-06-04, the managed-dispatch authority-opening continuation added direct regression coverage for the derived default managed-dispatch authorization route. `managed-dispatch-adapter.test.ts` now writes a plugin-satisfiable evidence bundle, configures nested production-enablement default-authorization metadata, proves `/flowdesk-run managed-dispatch` can derive the default authorization when no explicit `defaultManagedDispatchAuthorization` is present, and proves invalid derived metadata blocks before dispatch with disabled authority and no adapter/reservation calls. Focused verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, `node --test --test-name-pattern="/flowdesk-run managed-dispatch|default managed-dispatch authorization|derived default" packages/opencode-plugin/dist/managed-dispatch-adapter.test.js` (7/7), and `git diff --check`. This remains a guarded route/evidence test; it does not enable default managed dispatch in active config and does not open fallback/reselection, controlled writes, hard-chat/noReply, or automatic execution.

The same continuation exposed two live-lane reliability gaps that are now tracked separately from managed-dispatch readiness. First, child-lane permission asks still do not reliably surface an actionable parent/main approval UI: live evidence showed both `external_directory` asks for `~/.config/opencode/*` and read-only git/bash asks can leave child tools pending while the coordinator sees only durable status. The policy direction was changed so simple external-directory read access is allowed by default for FlowDesk/OpenCode profiles, while edit/bash/write/delete remain separately controlled; a focused lane updated the config/profile/installer policy, but OpenCode restart is required before running agents load those changes. Second, several edit-capable lanes that had already applied patches and run focused commands still ended as `inconsistent_finalizing_without_terminal` with a final bash part left `running`; the patches had to be salvaged and verified from the main session. This is a lane terminalization/tool-state reliability blocker, not a managed-dispatch authority grant.

On 2026-06-04, a focused OpenCode permission configuration slice made external-directory read boundaries explicit for FlowDesk lanes without promoting privileged actions. Project `opencode.json` now grants `read`, `glob`, `grep`, and `list` plus `external_directory: { "*": "allow" }` at the project level, while leaving `edit`, `bash`, delete-like behavior, managed dispatch, fallback/reselection, controlled writes, and hard-chat authority untouched. All project FlowDesk agent profiles now include explicit `external_directory: "*": allow`; subagent bash/edit policies remain the existing separate allow/ask/deny policies, and the bootstrap installer template/regressions were updated so materialized profiles preserve the same external-directory boundary policy. Verification commands were not run in-session; OpenCode must be restarted to load changed config and agent frontmatter.

On 2026-06-04, a focused completion-wake UI/status slice addressed a coordinator visibility gap where an individual delegated child lane could persist durable `task_result` while the parent workflow was not yet fully terminal, leaving no actionable wake row for the main coordinator. `completion-ui-cache.ts` now writes parent-session-scoped, lane-scoped `completion-wake-ready` rows for completed terminal lanes when the whole workflow is not yet auto-next/synthesis ready; those rows carry bounded workflow/lane/task identifiers and redacted labels only. `status-live-tool.ts` now exposes a `collect_result` next-action kind when at least one usable result is ready but the workflow is not fully synthesis-ready. This remains advisory/status-only and does not enable automatic execution, auto-continue, fallback/reselection, abort, controlled writes, hard chat/noReply, or managed-dispatch authority. Focused regression coverage was added; verification commands are pending user approval in-session.

On 2026-06-04, a focused permission-attention surfacing patch addressed child FlowDesk lanes whose OpenCode `permission.asked` events can leave tools running while the parent/main UI receives no actionable wake. `completion-wake-main-session.ts` now sends advisory parent-session wake prompts with the OpenCode 1.15.x flat `{ sessionID }` shape first and falls back to the older structured `{ path: { id } }` shape, so a prompt-shape mismatch no longer silently drops the wake. Permission wake wording now explicitly tells the coordinator/user that approval or denial must happen through OpenCode's permission UI and still forbids auto-approval, auto-denial, retry, fallback, dispatch, write, or hard-cancel. `status-live-tool.ts` now suppresses `finalizing_without_terminal` materialization while a lane has an unresolved newer `awaiting_permission` progress marker, and status/chat card wording highlights permission attention instead of treating it like ordinary progress. A small completion-wake regression covers flat-first prompt delivery plus structured fallback. Verification commands are pending user approval in-session.

On 2026-06-04, the earlier plugin-boundary documentation alignment was superseded by the Platform Evidence Demotion ADR above. FlowDesk still must not self-attest OpenCode platform-internal proof, but those platform-internal facts are no longer gate blockers or completion criteria. Current normative docs require the 12-item plugin-verifiable evidence bundle, use `attestation_scope=plugin_observed_only` for completion records, and treat runtime echo issuer, telemetry correlation, lane conformance, account-scope usage authority, reviewer-lane platform conformance, hard-chat/noReply platform authority, fallback platform proof, and remote write/apply platform proof only as non-gating diagnostic scope when observable. This documentation alignment does not enable default dispatch, fallback/reselection, controlled writes, or hard-chat authority.

The same session then began the managed-dispatch authority-opening sequence but paused implementation because multiple fresh `flowdesk_agent_task_run` lanes returned durable `task_failed/no_response` despite heavy `agent_task_progress` event counts. A docs audit lane (`workflow-plugin-boundary-design-audit-20260604`), a narrower docs retry (`workflow-plugin-boundary-design-audit-retry1-20260604`), and a code-inspection lane (`workflow-managed-dispatch-gate-inspect-20260604`) all reached terminal `lane_lifecycle` state `no_output` with no `task_result`. The coordinator therefore stopped launching additional FlowDesk lanes and switched to bounded direct inspection. Partial direct changes now present in the workspace include broader plugin-boundary wording in normative docs plus a small diagnostic/code clarity patch that exposes `production_managed_dispatch_ready_basis`, `production_plugin_satisfiable_gate_passed`, plugin-boundary skipped counts, and `production_skipped_platform_dependent=*` refs in `command-handlers.ts`, while clarifying that a fresh `provider_usage_snapshot` can clear plugin-visible provider readiness and that trusted `usage_authority` remains platform-dependent. These changes are not yet fully verified or committed; the immediate blocker is to diagnose why the explicit dev/beta `flowdesk_agent_task_run` capture path is returning `no_response` for new lanes.

On 2026-06-04, a focused backend/plugin slice added a safe derived default managed-dispatch authorization path for `/flowdesk-run managed-dispatch` in `server.ts`. Explicit `defaultManagedDispatchAuthorization` remains unchanged and takes precedence. When no explicit authorization is configured, the route can now derive a blocked-or-authorized default authorization only from nested production-enablement default-authorization metadata, a durable session evidence reload, plugin-satisfiable production enablement, promotion readiness refs, default enablement request state, inactive kill switch, non-expired authorization window, durable precall ref, adapter profile ref, SDK client ref, and default release enablement ref. The path stays fail-closed and does not enable default authority without that metadata/evidence bundle. Verification commands were intentionally not run in-session; focused build/regression tests remain a follow-up slice.

Initial blocker diagnosis: the failed lanes were not simple provider-output absence. OpenCode DB metadata for the child sessions shows many assistant/tool/step-finish parts after FlowDesk had already persisted terminal `no_output`; in the code-inspection lane (`ses_16ea8a3fbffepkWPpe8h95Xs8V`) the child session later contained a non-empty final text part plus `step-finish`, while FlowDesk status had already recorded `task_failed/no_response`. The two docs lanes had last observed pending tool parts after FlowDesk terminalization, which is consistent with the same premature terminalization pattern for long tool-using subagents. Current hypothesis: the synchronous `flowdesk_agent_task_run` capture path in `agent-task-runner.ts` applies bounded first-token/quiet-period capture and can write terminal `no_response` before a long-running OpenCode child session has actually stopped; event/watchdog progress continues afterward but terminal no-output has already been materialized. Logs also show repeated OpenCode generated-client `%7Bid%7D` session.messages errors from the structured `{ path: { id } }` probe shape, but the stronger evidence is the premature `no_output` terminalization while the child later produced output. Next fix should be a narrowly scoped agent-task capture patch: do not persist terminal `task_failed/no_response` while the child session still has recent progress/open tool activity, and allow late final text/step-finish backfill to supersede premature no-output classification.

The first timeout-policy patch was then applied directly because new FlowDesk lanes were unsafe to rely on. `executeFlowDeskAgentTaskV1` no longer materializes terminal `task_failed/no_response` or `lane_lifecycle state="no_output"` when bounded synchronous capture exhausts without text but a child session id exists. Instead it records waiting progress (`agent task still awaiting child output after bounded sync capture; watchdog/status may backfill result`), refreshes UI caches, and returns `task_launched` with the child session id so the existing watchdog/status backfill path can later capture final text plus `step-finish`. The old terminal `no_response` path remains only for cases where no child session id exists after launch. A focused regression proves a sync capture timeout with an existing child session leaves no `task_failed`, no `no_output` lifecycle, and preserves `agent_task_child_session` for watchdog/status recovery. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, `node --test packages/opencode-plugin/dist/async-lane.test.js` (34/34), and `git diff --check`. This patch changes timeout semantics from failure authority to diagnostic/backfill handoff; it does not enable managed dispatch, fallback/reselection, controlled writes, or hard-chat authority.

After restarting OpenCode/plugin, the coordinator resumed the managed-dispatch authority-opening work. A tiny post-restart `flowdesk_agent_task_run` smoke (`workflow-post-timeout-policy-restart-smoke-20260604`) initially surfaced `Tool execution aborted` to the coordinator, but durable status reload showed the lane completed normally with terminal `task_result`, no failed/late/stalled/inconsistent counts, and watchdog-backed result evidence. This means the restarted runtime loaded the timeout-policy fix well enough for durable lane capture, while the immediate tool-return surface can still misreport an aborted call and should be judged from durable status evidence. The managed-dispatch direct continuation then confirmed that default managed-dispatch authorization and `/flowdesk-run managed-dispatch` routing already exist behind the `defaultManagedDispatchAuthorization` option; the remaining product gap is the safe evidence/authorization generation path rather than the route itself. A small diagnostic patch now exposes `production_managed_dispatch_ready_basis`, `production_plugin_satisfiable_gate_passed`, plugin-boundary skipped counts, and each `production_skipped_platform_dependent=*` label through command-backed doctor/status refs, and focused tests assert the new refs. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, `node --test packages/opencode-plugin/dist/async-lane.test.js packages/opencode-plugin/dist/command-handlers.test.js packages/opencode-plugin/dist/managed-dispatch-adapter.test.js` (111/111), and `git diff --check`. No default managed-dispatch authorization was enabled in active config by this continuation; fallback/reselection, controlled writes, and hard-chat authority remain closed.

On 2026-06-04, a focused permission-attention slice closed the remaining advisory wake gap for FlowDesk-owned child lanes that emit `permission.asked`. The event hook already wrote `agent_task_progress` with `phase="awaiting_permission"`; the patch now refreshes status/UI caches on that evidence, writes a parent-session-scoped `completion-wake-ready` row with `completionKind="awaiting_permission"`, and lets the existing main-session wake consumer prompt the coordinator to inspect durable status without auto-approving, auto-denying, retrying, fallbacking, dispatching, writing, or hard-cancelling. `flowdesk_status_live` summary text now surfaces awaiting-permission lane counts and explicitly states no permission was auto-approved. Focused regression coverage was added to assert the event hook persists the awaiting-permission progress and the bounded wake-ready row. Verification commands are pending user approval in-session.

On 2026-06-04, a focused advisory main-wake slice extended the event-poked async capture path in `server.ts`: after a finalization-relevant child-session event runs the bounded direct `monitorChildSessionsV1` pass for the affected workflow, the hook now consumes `completion-wake-ready` rows through `consumeFlowDeskCompletionWakeForMainSessionV1` when `completionWakeMainSessionConfig` is configured. The wake remains advisory and parent-session-scoped: it prompts the main coordinator to inspect durable FlowDesk status after `task_result`/cache refresh, but does not auto-continue, synthesize, dispatch, fallback, write files, or claim hard chat cancellation. Verification was not run in-session; suggested follow-up is the plugin TypeScript build plus a focused server/event-hook regression.

Also on 2026-06-04, the default FlowDesk coordinator lane fan-out cap was raised from two to five concurrent lanes in the project `flowdesk-main` profile, the active global `~/.config/opencode/agent/flowdesk-main.md`, and the bootstrap installer template so reinstall does not revert the limit. The same session inspected the non-terminal Claude security-policy review lane `lane-task-mpyw9pwp`: durable evidence shows only the initial running lifecycle plus `agent_task_inconsistency/finalizing_without_terminal`, while the OpenCode child session `ses_16f791c94ffeArSyBG2f3lwBkO` contains multiple completed tool-call turns and then a final assistant turn left open on a `bash` tool part marked `running`; the OS process was no longer present. This points to an OpenCode/tool-state persistence or terminal-event gap for the last shell call rather than a provider-output absence. Verification for the profile/template edits and a regression for stale `running` tool-state salvage remain pending.

The subsequent permission/log inspection showed that the Claude review lane's last bash tool had actually stopped at an OpenCode permission ask for pipeline utilities (`head`, `grep`, `echo`) even though `git diff*` itself was allowed. A direct main-session patch now adds read-only utility permissions (`"head *": allow`, `"grep *": allow`, `"echo *": allow`) to FlowDesk subagent profile materialization, the project `.opencode/agent/flowdesk-*` subagent files, the active global `~/.config/opencode/agent/flowdesk-*` subagent files, and the profile/installer regression expectations. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, focused built tests `node --test packages/opencode-plugin/dist/bootstrap-installer.test.js packages/opencode-plugin/dist/project-agent-profiles.test.js` (11/11), and `git diff --check`. OpenCode must still be restarted before running agents use the updated global profile permissions.

The permission-attention wake path was then focused-verified with `npm run build --workspace @flowdesk/opencode-plugin`, `node --test packages/opencode-plugin/dist/server.test.js packages/opencode-plugin/dist/completion-wake-main-session.test.js` (102/102), and `git diff --check`. A follow-up architecture review for stale non-bash tool states concluded that handling `glob`/tool calls stuck in `state=running` is safe only as diagnostic/advisory behavior: keep `tool_run_overdue_observed` as the minimal inconsistency kind, do not synthesize `task_result`, terminal lifecycle, abort, retry, fallback, permission approval, or write authority, and add clearer redaction-safe capture diagnostics plus a deduplicated `diagnostic_attention` advisory wake when configured. The review also noted that existing comments around `unknownStateMaxMs` imply a two-stage timeout while current behavior is effectively one-stage, so the next implementation slice should either align comments/config or implement the two-stage threshold explicitly.

The stale open-tool diagnostic implementation slice now aligns the async watchdog with a one-stage `staleToolMs=150000ms` default. When event-derived child-lane tool progress leaves any tool call (including generic/non-bash tools such as `glob`) open past that threshold, `monitorChildSessionsV1` writes idempotent advisory `agent_task_inconsistency` evidence with `inconsistency_kind="tool_run_overdue_observed"`, redaction-safe capture diagnostic fields on the child-session record, and a deduplicated `diagnostic_attention` completion-wake row when UI/wake caches are configured. The path remains advisory-only: it does not write `task_result`, `task_failed`, terminal lifecycle, abort, retry, fallback, permission approve/deny, dispatch, write, or hard-cancel. Status wording now distinguishes stale open-tool diagnostic attention from awaiting permission and finalizing-without-terminal inconsistency. Focused tests were added/updated for non-bash/generic stale running tool diagnostics, no terminal/failure side effects, idempotent inconsistency/progress/wake evidence, and permission-awaiting separation. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, focused built tests `node --test packages/opencode-plugin/dist/async-lane.test.js packages/opencode-plugin/dist/completion-wake-main-session.test.js packages/opencode-plugin/dist/status-live-tool.test.js` (52/52), `git diff --check`, and full `npm test` (867/867).

On 2026-06-04, a bounded agent-profile alignment slice updated the project and installer-generated FlowDesk profiles. The project `flowdesk-main` profile and bootstrap materialization now use broad coordinator permissions (`read/edit/glob/grep/list/todowrite/question/skill/webfetch/lsr` allow, `task` deny, external directory wildcard allow, broad bash allow with mutating git denials) so reinstall does not regress the main coordinator to `edit: deny` or `bash: ask`. The `flowdesk-main` prompt was then optimized in project, active global, and installer-template copies: the mandatory FlowDesk dispatch boundary is followed immediately by a concise `Lane Size Gate` that requires exactly one primary objective, one deliverable, compact file/subsystem/failure-mode scope, explicit slice statements before launch, and sequential splitting for agent/profile/config, installer/materialization, tests, docs, runtime/session, TUI/status, and authority-sensitive dimensions. This replaces duplicated operational prose with a shorter prompt while preserving `flowdesk_agent_task_run` as the mandatory delegated-work path and keeping `flowdesk_quick_reviewer_run` quarantined. The new `flowdesk-algorithm-architect` design-only profile is now part of installer materialization and project-profile expectations with edit denied, read/glob/grep/list allowed, read-only git commands allowed, and mutating git denied. Profile wording was adjusted away from forbidden external autonomous runtime/prompt-injection/nested-CLI language that would trip no-unsafe-authority tests. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, focused built tests `node --test packages/opencode-plugin/dist/bootstrap-installer.test.js packages/opencode-plugin/dist/project-agent-profiles.test.js` (11/11), and `git diff --check` for the touched main-agent/installer/test files.

Also on 2026-06-04, a narrow OpenCode 1.15.x compatibility cleanup changed the async watchdog child-session abort path in `stall-recovery.ts` from the older structured `{ path: { id } }` SDK shape to the current flat `{ sessionID }` shape. This targets only `monitorChildSessionsV1`'s `abortChildSession()` helper and does not alter managed-dispatch prompt/launch fallback paths, provider routing, fallback authority, or hard-chat claims. A regression assertion now verifies the abort call uses `{ sessionID: <child session id> }`. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, focused built tests `node --test packages/opencode-plugin/dist/async-lane.test.js packages/opencode-plugin/dist/stall-recovery.test.js` (80/80), and `git diff --check` for the touched watchdog files.

The remaining managed-dispatch OpenCode 1.15.x SDK-shape audit then found legacy structured `path:{id}` defaults in prompt-backed provider acquisition, injected runtime lane launch, managed dispatch prompt calls, reviewer/lane observation reads, and session abort control. A focused compatibility patch in `managed-dispatch-adapter.ts` now prefers the current flat `{ sessionID }` call shape for prompt/promptAsync/messages/children/abort paths while retaining bounded legacy fallback where the adapter already supports it. Regression expectations in `managed-dispatch-adapter.test.ts`, `server.test.ts`, and the quarantined quick-reviewer regression were updated to assert the flat-first contract, including `query.directory` and `body` envelope shapes. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, `node --test packages/opencode-plugin/dist/managed-dispatch-adapter.test.js` (64/64), `node --test packages/opencode-plugin/dist/server.test.js` (99/99, run with a longer timeout for deliberate no-output cases), `node --test packages/opencode-plugin/dist/quick-reviewer-run.test.js` (13/13), `git diff --check`, and full `npm test` (867/867). This is a compatibility hardening only; it does not enable default managed dispatch, fallback/reselection, write/apply, or hard-chat authority.

After restarting OpenCode/plugin with the rebuilt local workspace, live restart smokes on 2026-06-04 all completed as durable terminal `task_result` evidence with zero stalled, late, or inconsistent lanes: OpenAI sync/no-tool (`workflow-live-restart-smoke-openai-sync-notool-20260604`, `FLOWDESK-RESTART-SYNC-NOTOOL-OK`), OpenAI async/tool-read (`workflow-live-restart-smoke-openai-async-toolread-20260604`), Claude Opus async/no-tool (`workflow-live-restart-smoke-claude-async-notool-20260604`), Gemini Flash async/no-tool (`workflow-live-restart-smoke-gemini-async-notool-20260604`), and OpenAI async structured multi-line final output (`workflow-live-restart-smoke-openai-async-structured-20260604`). Fresh usage evidence before the smoke showed Claude/OpenAI/Gemini all dispatchable with `alertLevel="ok"`. The workspace packages were then bumped for the next patch release: `@flowdesk/core` and `@flowdesk/opencode-plugin` are prepared as `0.1.22`, and the plugin package now depends on `@flowdesk/core@^0.1.22`. Release verification after the bump passed with full `npm test` (867/867).

The operator then requested ordinary commit/push authority for `flowdesk-main` and `flowdesk-git-master` while keeping dangerous rollback/history-rewrite operations blocked. The project and active global agent files now allow normal `git add`, `git commit`, and non-force `git push` through their bash permissions, while continuing to deny destructive or high-risk commands such as `git commit --amend*`, `git push --force*`, `git push -f*`, `git reset*`, `git clean*`, checkout/switch, branch deletion, rebase, cherry-pick, merge, restore, rm, stash, delete-tag, and `gh pr merge*`. The bootstrap installer template and profile tests were updated so reinstall preserves this policy for main/git-master without broadening other subagents. Focused verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, `node --test packages/opencode-plugin/dist/bootstrap-installer.test.js packages/opencode-plugin/dist/project-agent-profiles.test.js` (11/11), and `git diff --check`. OpenCode must be restarted before the running coordinator can use the new git permissions.

After that restart, post-permission live smoke coverage was rerun on 2026-06-04 with fresh provider usage showing Claude/OpenAI/Gemini all dispatchable and `alertLevel="ok"`. All five smoke workflows completed as durable terminal `task_result` evidence with zero stalled, progressing-late, inconsistent-finalizing, or capture-diagnostic counts: OpenAI sync/no-tool (`workflow-live-post-gitperm-restart-openai-sync-notool-20260604`, `FLOWDESK-POST-GITPERM-RESTART-SYNC-NOTOOL-OK`), OpenAI async/tool-read (`workflow-live-post-gitperm-restart-openai-async-toolread-20260604`), Claude Opus async/no-tool (`workflow-live-post-gitperm-restart-claude-async-notool-20260604`), Gemini Flash async/no-tool (`workflow-live-post-gitperm-restart-gemini-async-notool-20260604`), and OpenAI async structured multi-line output (`workflow-live-post-gitperm-restart-openai-async-structured-20260604`). This confirms the restarted runtime loaded the profile/config updates and the explicit dev/beta agent-task lane capture path remains healthy across sync, async, tool-use, and multi-provider cases.

The 0.1.22 release was then published to npm after commit `4883138` (`feat(agent-task): harden live lane capture`). Published latest versions are `@flowdesk/core@0.1.22` and `@flowdesk/opencode-plugin@0.1.22`, verified with `npm view` after publish. The first publish attempt with `NODE_AUTH_TOKEN` failed with `ENEEDAUTH`, then the retry using an explicit npm registry auth token option succeeded for both packages without recording token contents. Active local OpenCode config still uses local `file:`/file-URL development packages until the operator chooses to switch back to npm latest.

The restarted live runtime then exposed a remaining sync `flowdesk_agent_task_run` capture bug: `workflow-post-restart-agent-smoke-20260604` launched successfully and OpenCode's local DB contained the expected assistant text plus `step-finish`, but FlowDesk persisted `task_failed/no_response`. The concurrent OpenCode log showed repeated `Expected a string starting with "ses", got "%7Bid%7D"` errors during sync capture polling. Root cause: the sync `extractAssistantTextFromResponse()` path in `agent-task-runner.ts` called `session.messages` with only the flat `{ sessionID }` shape, while the active OpenCode 1.15.13 generated client path for this method requires the structured `{ path: { id } }` shape. The patch now tries structured `{ path: { id: childSessionId } }` first and falls back to flat `{ sessionID: childSessionId }` only when structured throws or returns an SDK error response. This preserves compatibility with both tested fake SDK shapes and the active OpenCode generated-client shape. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, focused built tests `node --test packages/opencode-plugin/dist/async-lane.test.js packages/opencode-plugin/dist/server.test.js` (131/131), full `npm test` (865/865), and `git diff --check`. OpenCode/plugin restart is still required before a live re-smoke can prove the rebuilt sync capture path in the running runtime.

After restart on 2026-06-04, two sync live `flowdesk_agent_task_run` smokes proved the rebuilt sync capture path in the active runtime: `workflow-live-sync-final-smoke-20260604` captured `FLOWDESK-LIVE-SYNC-FINAL-OK`, and `workflow-live-tool-read-smoke-20260604` captured `FLOWDESK-LIVE-TOOL-READ-OK @flowdesk`, both as durable `task_result` evidence. A separate async smoke (`workflow-live-async-smoke-20260604`, child session `ses_16fb99cb4ffenPIBungII78oWt`) launched successfully and the OpenCode DB contains the expected assistant text `FLOWDESK-LIVE-ASYNC-OK` plus `step-finish`, but FlowDesk status classified the lane as `inconsistent_finalizing_without_terminal` with no durable `task_result`. This narrows the remaining live blocker to async watchdog/finalization capture rather than provider output or child-session execution. Per the lane-size gate, further live lanes were paused instead of expanding scope while this inconsistent async lane remains unresolved.

After another OpenCode/plugin restart on 2026-06-04, additional live smokes confirmed the split remains: provider usage was fresh/healthy for Claude/OpenAI/Gemini, sync no-tool smoke `workflow-post-restart-sync-notool-smoke-20260604` captured `FLOWDESK-POST-RESTART-SYNC-NOTOOL-OK` as terminal `task_result`, and sync tool-read smoke `workflow-post-restart-sync-toolread-smoke-20260604` captured `FLOWDESK-POST-RESTART-SYNC-TOOLREAD-OK flowdesk` as terminal `task_result`. The async no-tool smoke `workflow-post-restart-async-notool-smoke-20260604` launched and the child session `ses_16f9f93baffef5UunIvNXIAyAr` contains `FLOWDESK-POST-RESTART-ASYNC-NOTOOL-OK` plus `step-finish` in OpenCode DB, but FlowDesk status did not persist `task_result` and progressed to `progressing_late`. The async tool-read smoke was intentionally cancelled to avoid widening scope while the async no-tool capture failure remains. This means Slice A's structured-first polling tests pass in-process, but the live async runtime still needs a narrower follow-up focused on why watchdog capture is not being invoked/observed after final text is present.

The follow-up root cause was then narrowed to scheduling rather than message-shape parsing: `server.ts` only created `pokeWatchdogCycle` and the interval watchdog when `watchdogConfig.enabled === true`, `guardedAutoAbortForWatchdog` existed, and a durable root was configured. In the active dev/beta agent-task setup, the event hook was writing finalization-relevant child-session progress, but without guarded auto-abort config there was no capture owner invoked by those events; `status_live` only projected the missing terminal evidence and eventually marked the lane late/inconsistent. The patch now lets the event hook, when it observes a matched finalization-relevant child-session event and the guarded watchdog poke is unavailable, run a bounded `monitorChildSessionsV1` pass for that workflow using the injected SDK client. This path uses wide abort/absolute-lane-age clocks so it can capture completed child output without acting as a hidden auto-abort loop. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, focused built tests `node --test packages/opencode-plugin/dist/server.test.js packages/opencode-plugin/dist/async-lane.test.js` (132/132), and `git diff --check`. OpenCode/plugin restart is required before another async live smoke can prove this scheduling fix in the active runtime.

After the scheduling fix was loaded by restarting OpenCode/plugin, two focused async live smokes proved the repaired capture path in the active runtime. `workflow-post-scheduling-fix-async-notool-smoke-20260604` completed as terminal `task_result` via watchdog evidence (`task-result-task-mpyva7jy-watchdog-9f47bb4f`) with no stalled, late, failed, or inconsistent lane counts. A second async tool-read smoke, `workflow-post-scheduling-fix-async-toolread-smoke-20260604`, also completed as terminal `task_result` via watchdog evidence (`task-result-task-mpyvapd0-watchdog-951fd2fd`) after reading `package.json`, again with no stalled, late, failed, or inconsistent lane counts. This closes the live async no-tool/tool-read capture blocker introduced by the previous guarded-watchdog scheduling dependency. Remaining release work is packaging/versioning and any broader full-suite/release-gate verification, not the async finalization capture bug.

Broader post-fix multi-model live coverage on 2026-06-04 kept provider usage fresh and healthy (Claude/OpenAI/Gemini all dispatchable) and then exercised the repaired async capture path across providers and task shapes. OpenAI `openai/gpt-5.5` longer no-tool async smoke `workflow-multimodel-openai-long-async-20260604` completed as terminal `task_result` through watchdog evidence with no late/stalled/inconsistent counts. Claude Opus `anthropic/claude-opus-4-7` real-failed-task-style async analysis `workflow-multimodel-claude-realfailed-analysis-20260604` also completed as terminal `task_result` after inspecting the prior non-capture failure mode, again without late/stalled/inconsistent counts. Gemini Flash `google/gemini-3-flash-preview` async smoke `workflow-multimodel-gemini-flash-async-20260604` completed as terminal `task_result` and confirms a working exact Gemini Flash-family binding; the previously observed unavailable `google/gemini-3.1-flash-lite-preview` remains avoided. This provides live cross-provider evidence for no-tool, longer-response, and real-failed-task-style async capture after the scheduling fix.

On 2026-06-04, a live `flowdesk_agent_task_run` verifier retry investigation found two practical lane blockers: read-only git verifier commands were stuck behind subagent `bash: ask`, and the OpenCode 1.15.x `session.messages({ path: { id } })` fallback could surface repeated literal `%7Bid%7D` server errors. The local patch now treats `permission.asked` events the same as `permission.updated` for FlowDesk child-session progress (`awaiting_permission`), removes the `{ path: { id } }` messages fallback from agent-task/monitor/managed reviewer polling in favor of the live `sessionID` shape, and expands FlowDesk subagent profiles with safer default permissions: read/glob/grep/list allow, bounded implementation/doc edit roles, explicit bash allowlists for read-only git/status/diff and selected build/test commands, and mutating git command denials. The bootstrap installer now materializes the full FlowDesk agent set (main plus 13 subagents) so fresh installs/updates receive the same permission profile changes. The active global `~/.config/opencode/agent/flowdesk-*.md` files were updated as well, but OpenCode still requires a restart to reload agent/config changes. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, focused built regression tests (`async-lane`, `bootstrap-installer`, `project-agent-profiles`, `server`: 142/142), and `git diff --check`.

On 2026-06-04, Slice A of the async watchdog polling fix realigned `monitorChildSessionsV1` body/diagnostic reads with the structured-first sync capture path. `stall-recovery.ts` now routes async monitor message reads through a shared helper that tries `session.messages({ path: { id: childSessionId } })` first, falls back to `{ sessionID: childSessionId }` only when the structured call throws or returns an SDK error response, and keeps the existing per-call timeout race. The helper is used by final output polling, candidate polling, idle-confirmed polling, and capture-failure diagnostics. Focused `async-lane` regressions now cover structured-only final text plus `step-finish` capture and flat fallback after a structured SDK error. This slice intentionally does not relax turn-completed gating and does not change provider usage, fallback/reselection, dispatch authority, hard-chat authority, or TUI behavior. Verification passed with `npm run build --workspace @flowdesk/opencode-plugin`, `node --test packages/opencode-plugin/dist/async-lane.test.js` (33/33), and `git diff --check`. The implementation lane that applied this patch (`workflow-async-session-messages-reader-sliceA-20260604`) also reproduced the existing live async finalization problem: its child session had final text and `step-finish`, but FlowDesk status classified the lane as `inconsistent_finalizing_without_terminal`; active OpenCode/plugin restart is still required before a live re-smoke can prove the rebuilt async polling path.

On 2026-06-04, implementation slice 3 added evidence-only coordinator-attention timer bookkeeping for async agent-task lanes. `monitorChildSessionsV1` now persists a per-child-session attention timer (`coordinator_attention_timer_reset_at`, due time, interval, reset reason/status) that resets on meaningful lane state/progress changes and, when overdue, writes `coordinator-review-requested` progress evidence plus a redaction-safe capture diagnostic snapshot. The diagnostic records only childSessionId, last part kind, final-text presence, step-finish presence, running tool call id/status, and a recommended next action; it is exposed through `flowdesk_status_live` workflow/card/subtask summaries and summary text. The same redaction-safe diagnostic fields are also attached to bounded body-capture failure paths (`awaiting_body_capture`, turn-completed-empty terminalization, and task_result persistence failure). This slice does not auto-abort, auto-retry, auto-fallback, change provider usage, widen managed-dispatch authority, or claim hard chat cancellation. Focused tests cover coordinator-review diagnostic evidence and status exposure. Verification commands are pending user approval in-session.

On 2026-06-04, implementation slice 2 changed overdue/unknown tool handling from automatic terminalization to coordinator-wake diagnostics. When `monitorChildSessionsV1` observes an event-derived open tool past `staleToolMs` (`toolStateUnknown`) and no final text is available, it now writes idempotent `agent_task_inconsistency` evidence with `inconsistency_kind="tool_timeout_review_requested"` plus advisory `agent_task_progress` (`tool_run_overdue_observed; coordinator review requested`) instead of writing `task_failed`, terminal lifecycle, aborting, retrying, fallbacking, or nudging. The redacted wording records that the underlying child abort is not confirmed and the coordinator should re-check status/messages/process before deciding. Final text backfill remains higher precedence: if a re-poll finds final text plus `step-finish`, the watchdog captures `task_result` and does not write the diagnostic. Focused async-lane regressions cover the non-terminal diagnostic path, idempotent re-poll behavior, and final-text precedence. Verification commands are pending user approval in-session.

On 2026-06-04, implementation slice 1 stabilized completed async agent-task lane capture/backfill. `monitorChildSessionsV1` now writes a terminal non-verdict `lane_lifecycle` (`state="incomplete"`, `output_ref` to the task result) whenever it persists an async watchdog `task_result` without a typed reviewer verdict, so a completed lane is not left with only the original running lifecycle. Existing typed reviewer verdicts still write the verdict-linked `complete` lifecycle. Focused regressions cover a prior `agent_task_inconsistency/finalizing_without_terminal` lane being backfilled when the child session later exposes final text plus `step-finish`, no-reviewer-verdict task results getting terminal lifecycle, and idempotent second monitor passes avoiding duplicate `task_result`. This slice intentionally did not implement tool-timeout wake/diagnostic behavior and did not change provider usage, managed-dispatch auth, TUI, fallback, or command surfaces. Verification commands are pending user approval in-session.

Earlier on 2026-06-04, a focused dev/beta `flowdesk_agent_task_run` capture patch was added for OpenCode SDK `session.messages` responses that surface the completed child turn as a single message object rather than an array/wrapper. `observeFlowDeskAgentTaskOutputV1` now treats message-shaped records (`parts`, `content[]`, `info`, role/finish/status fields) as a one-item transcript, so assistant text followed by `step-finish` is captured as `task_result` instead of being missed and later classified as `agent_task_inconsistency` / `finalizing_without_terminal`. Regression coverage was added for both synchronous `executeFlowDeskAgentTaskV1` capture and async `monitorChildSessionsV1` watchdog capture of single-message step-finish shapes. This is a capture-shape fix only; it does not broaden provider usage, dispatch authorization, TUI behavior, fallback, or docs authority. Verification commands are pending user approval in-session.
