# AGENTS.md - Background Docs Warning

This folder contains historical and research documents only.

Do not implement production FlowDesk behavior directly from this folder.

## Non-Normative Files

1. `OPENCODE_FIRST_PLUGIN_DESIGN.md`
2. `OPENCODE_PLUGIN_RESEARCH.md`

## Normative Sources

This folder is not normative. Use the main `docs/` implementation documents when implementation context is needed.

## Legacy Terms

Treat these as historical unless a normative document explicitly says otherwise:

1. DEX Conductor.
2. `@dex-conductor/*`.
3. `.conductor/`.
4. `/flowdesk:*` as default command syntax.
5. Command-first or developer-preview wording for Release 1.

FlowDesk implementation targets are `FlowDesk`, `@flowdesk/*`, `.flowdesk/`, a chat-primary Release 1 General-Use MVP, and command-backed routing and fallback. Historical command-first wording in this folder is superseded by the normative docs.
