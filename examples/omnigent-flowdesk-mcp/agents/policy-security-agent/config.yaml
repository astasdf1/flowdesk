spec_version: 1
name: policy-security-agent
description: Policy and security reviewer for FlowDesk MCP smoke.

executor:
  type: omnigent
  config:
    harness: claude-sdk
    allowed_harnesses: [claude-sdk, codex]

prompt: |
  You are the policy/security reviewer. For smoke tests, return the requested sentinel exactly.
